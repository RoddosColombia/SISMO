---
type: quick
id: 260330-fs8
title: "Hotfix ERROR-017: Corregir URL base de Alegra en todo el codebase"
created: "2026-03-30"
estimated_effort: "15-25 min Claude execution"
---

<objective>
Fix all incorrect Alegra API base URLs (`app.alegra.com/api/r1` -> `api.alegra.com/api/v1`) and consolidate all duplicated URL definitions to import from a single canonical constant in `alegra_service.py`.

Purpose: Alegra API calls are hitting the wrong domain/version, causing 404s or empty results. This is a production-blocking bug.
Output: All Alegra API calls use the correct URL from a single source of truth; verified by a live GET /invoices returning real data.
</objective>

<context>
@backend/alegra_service.py (canonical ALEGRA_BASE_URL definition — line 16, already correct)
@backend/migrations/migrate_inventario_tvs.py (WRONG URL line 29)
@backend/routers/alegra_webhooks.py (WRONG URL line 26, duplicate correct URL line 27)
@backend/services/dian_service.py (WRONG hardcoded URLs lines 143, 206)
@backend/routers/auditoria.py (correct but hardcoded inline, lines 64, 259)
@backend/routers/conciliacion.py (correct but hardcoded inline, lines 558, 1195)
@backend/services/bank_reconciliation.py (correct but hardcoded inline, line 501)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix wrong URLs and consolidate all Alegra base URL references to single import</name>
  <files>
    backend/alegra_service.py,
    backend/migrations/migrate_inventario_tvs.py,
    backend/routers/alegra_webhooks.py,
    backend/services/dian_service.py,
    backend/routers/auditoria.py,
    backend/routers/conciliacion.py,
    backend/services/bank_reconciliation.py
  </files>
  <action>
    The canonical constant already exists and is correct:
    `backend/alegra_service.py:16` -> `ALEGRA_BASE_URL = "https://api.alegra.com/api/v1"`

    Ensure it is exported (it already is, just verify the import works).

    For each file below, remove the local URL definition and import from alegra_service:

    1. **backend/migrations/migrate_inventario_tvs.py** (line 29):
       - WRONG: `ALEGRA_BASE = "https://app.alegra.com/api/r1"`
       - Replace with: `from backend.alegra_service import ALEGRA_BASE_URL`
       - Update all references from `ALEGRA_BASE` to `ALEGRA_BASE_URL` (lines 151, 166)
       - NOTE: This is a migration script. If import path issues arise (scripts run standalone), use a try/except fallback:
         ```python
         try:
             from backend.alegra_service import ALEGRA_BASE_URL
         except ImportError:
             ALEGRA_BASE_URL = "https://api.alegra.com/api/v1"
         ```

    2. **backend/routers/alegra_webhooks.py** (lines 26-27):
       - WRONG: `ALEGRA_BASE = "https://app.alegra.com/api/r1"` (line 26)
       - DUPLICATE: `ALEGRA_API_V1 = "https://api.alegra.com/api/v1"` (line 27)
       - Remove both lines. Add: `from backend.alegra_service import ALEGRA_BASE_URL`
       - Replace all `ALEGRA_BASE` and `ALEGRA_API_V1` references with `ALEGRA_BASE_URL`
       - Search the file for all usages of both variables to catch every reference.

    3. **backend/services/dian_service.py** (lines 143, 206):
       - WRONG: hardcoded `"https://app.alegra.com/api/r1/bills"`
       - Add import: `from backend.alegra_service import ALEGRA_BASE_URL`
       - Replace with: `f"{ALEGRA_BASE_URL}/bills"`

    4. **backend/routers/auditoria.py** (lines 64, 259):
       - Correct URL but hardcoded inline: `base_url = "https://api.alegra.com/api/v1"`
       - Add import: `from backend.alegra_service import ALEGRA_BASE_URL`
       - Replace both with: `base_url = ALEGRA_BASE_URL`

    5. **backend/routers/conciliacion.py** (lines 558, 1195):
       - Correct URL but hardcoded inline
       - Add import: `from backend.alegra_service import ALEGRA_BASE_URL`
       - Replace hardcoded URLs with f-strings using `ALEGRA_BASE_URL`
       - Line 558: `url = f"{ALEGRA_BASE_URL}/journals?limit=1"`
       - Line 1195: `url = f"{ALEGRA_BASE_URL}/journals"`

    6. **backend/services/bank_reconciliation.py** (line 501):
       - Correct URL but hardcoded inline
       - Add import: `from backend.alegra_service import ALEGRA_BASE_URL`
       - Replace with: `base_url = ALEGRA_BASE_URL`
  </action>
  <verify>
    <automated>cd C:/Users/AndresSanJuan/roddos-workspace/SISMO/.claude/worktrees/nostalgic-boyd && grep -rn "app\.alegra\.com/api/r1" --include="*.py" backend/ | grep -v "^#" | grep -v "comment" && echo "FAIL: still found wrong URLs" || echo "PASS: no wrong URLs found"</automated>
  </verify>
  <done>Zero occurrences of "app.alegra.com/api/r1" in any .py file. All files import ALEGRA_BASE_URL from alegra_service.py.</done>
</task>

<task type="auto">
  <name>Task 2: Run COMMIT PROTOCOL greps and verify date formats</name>
  <files>None (read-only verification)</files>
  <action>
    Run these 5 verification greps:

    1. Confirm no wrong URLs remain:
       `grep -rn "app\.alegra\.com/api/r1" --include="*.py" backend/`
       Expected: 0 results

    2. Confirm no duplicate ALEGRA_BASE_URL definitions (only in alegra_service.py):
       `grep -rn "ALEGRA_BASE_URL\s*=" --include="*.py" backend/`
       Expected: exactly 1 result (alegra_service.py line 16)

    3. Confirm no other hardcoded "api.alegra.com" outside imports:
       `grep -rn "api\.alegra\.com" --include="*.py" backend/`
       Expected: only alegra_service.py:16 has the literal URL; all others use the imported constant

    4. Verify date formats sent to Alegra use strftime('%Y-%m-%d'), not isoformat():
       `grep -rn "isoformat" --include="*.py" backend/alegra_service.py`
       Expected: 0 results (isoformat should not appear in alegra_service.py for dates sent to Alegra)

    5. Check no `api/r1` path segment remains anywhere:
       `grep -rn "api/r1" --include="*.py" backend/`
       Expected: 0 results

    If any grep shows unexpected results, fix before proceeding.
  </action>
  <verify>
    <automated>cd C:/Users/AndresSanJuan/roddos-workspace/SISMO/.claude/worktrees/nostalgic-boyd && echo "=== CHECK 1: Wrong URLs ===" && (grep -rn "app\.alegra\.com/api/r1" --include="*.py" backend/ && echo "FAIL" || echo "PASS") && echo "=== CHECK 2: Duplicate defs ===" && (test $(grep -rn "ALEGRA_BASE_URL\s*=" --include="*.py" backend/ | wc -l) -eq 1 && echo "PASS" || echo "FAIL") && echo "=== CHECK 3: api/r1 remnants ===" && (grep -rn "api/r1" --include="*.py" backend/ && echo "FAIL" || echo "PASS")</automated>
  </verify>
  <done>All 5 greps pass. No wrong URLs, no duplicated constants, no hardcoded inline URLs, no isoformat dates sent to Alegra, no api/r1 remnants.</done>
</task>

<task type="auto">
  <name>Task 3: Live verification — GET /invoices from Alegra API</name>
  <files>None (read-only verification)</files>
  <action>
    Write and run a small Python script that:
    1. Loads ALEGRA_EMAIL and ALEGRA_TOKEN from environment (use python-dotenv to load .env)
    2. Makes GET https://api.alegra.com/api/v1/invoices?limit=5 with Basic auth (email:token base64)
    3. Prints status code and number of invoices returned
    4. Expected: HTTP 200, at least 1 invoice returned (confirms the URL works)

    If .env is not available in the worktree, check for it in the main repo root.
    The script should use httpx or requests (already in deps).

    IMPORTANT: Do NOT commit this test script. Delete it after verification.
  </action>
  <verify>
    <automated>The script prints HTTP 200 and returns invoice data. If env vars are missing, the task documents what manual step is needed.</automated>
  </verify>
  <done>Live GET /invoices returns HTTP 200 with real invoice data from Alegra, confirming the correct URL works. Script deleted after verification.</done>
</task>

</tasks>

<verification>
- `grep -rn "app\.alegra\.com/api/r1" --include="*.py" backend/` returns 0 results
- `grep -rn "ALEGRA_BASE_URL\s*=" --include="*.py" backend/` returns exactly 1 result
- `grep -rn "api/r1" --include="*.py" backend/` returns 0 results
- All Alegra API calls use imported ALEGRA_BASE_URL from alegra_service.py
- Live API test confirms correct URL returns real data
</verification>

<success_criteria>
1. Zero wrong Alegra URLs in the codebase (app.alegra.com/api/r1 completely eliminated)
2. Single source of truth: ALEGRA_BASE_URL defined only in alegra_service.py, imported everywhere else
3. Live GET /invoices returns real Alegra data (HTTP 200, non-empty results)
</success_criteria>
