---
phase: quick
plan: 260401-d5z
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/knowledge_base_service.py
  - backend/routers/admin_kb.py
  - backend/server.py
  - backend/ai_chat.py
  - init_mongodb_sismo.py
  - backend/tests/test_knowledge_base.py
autonomous: true
requirements: [KB-SERVICE, KB-SEED, KB-INTEGRATION, KB-ADMIN]
must_haves:
  truths:
    - "Al enviar 'registra el arriendo de marzo', el system prompt incluye ReteFuente 3.5% y ReteICA 0.414% desde MongoDB"
    - "Las 12+ reglas de negocio existen en sismo_knowledge con tags buscables"
    - "El admin puede crear/actualizar reglas via API REST"
    - "get_context_for_operation retorna bloque formateado con reglas relevantes por tags"
  artifacts:
    - path: "backend/services/knowledge_base_service.py"
      provides: "Knowledge base service con get_context_for_operation, get_all_rules_by_category, upsert_rule"
      exports: ["get_context_for_operation", "get_all_rules_by_category", "upsert_rule"]
    - path: "backend/routers/admin_kb.py"
      provides: "Admin endpoints POST upsert y GET by category"
    - path: "backend/tests/test_knowledge_base.py"
      provides: "Tests del service y seed"
  key_links:
    - from: "backend/ai_chat.py"
      to: "backend/services/knowledge_base_service.py"
      via: "get_context_for_operation() call in process_chat()"
      pattern: "get_context_for_operation"
    - from: "backend/services/knowledge_base_service.py"
      to: "sismo_knowledge collection"
      via: "MongoDB find by tags"
      pattern: "sismo_knowledge\\.find"
---

<objective>
Construir el Knowledge Base Service de SISMO — capa RAG que permite a los agentes consultar reglas de negocio institucionales de RODDOS desde MongoDB antes de ejecutar operaciones contables.

Purpose: Eliminar reglas hardcodeadas del prompt gigante de ai_chat.py y migrarlas a MongoDB consultable por tags, permitiendo que los agentes reciban solo las reglas relevantes a cada operacion.

Output: Service + seed de 12+ reglas + integracion en process_chat() + admin API
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@backend/ai_chat.py (lines 3019-3065 — system_prompt assembly in process_chat())
@init_mongodb_sismo.py (SISMO_KNOWLEDGE array and seed_sismo_knowledge() function)
@backend/server.py (router registration pattern)

IMPORTANT CODEBASE FACTS:
- La coleccion ya existe como `sismo_knowledge` (NO `sismo_knowledge_base` como dice la spec) — usar el nombre existente
- Ya tiene 10 reglas seed en SISMO_KNOWLEDGE array (line 518-643 de init_mongodb_sismo.py)
- Ya tiene indexes: rule_id (unique) y categoria
- NO tiene index en tags — hay que agregarlo
- El schema existente usa: rule_id, categoria, titulo, contenido, tags, actualizado_en
- El system_prompt se ensambla en process_chat() linea 3019-3027
- Los routers se registran con try/except pattern en server.py (ver lineas 22-55 para el patron)
- PREFIX = "/api" (linea 145 de server.py)

<interfaces>
<!-- Existing sismo_knowledge document schema (from init_mongodb_sismo.py): -->
{
  "rule_id": str,          // unique identifier
  "categoria": str,        // 'cartera' | 'impuestos' | 'contabilidad' | 'loanbook'
  "titulo": str,
  "contenido": str,        // Natural language rule text
  "tags": [str],           // Keyword search tags
  "actualizado_en": str    // ISO datetime
}

<!-- process_chat() signature (ai_chat.py line 2814): -->
async def process_chat(user_message: str, db, user: dict, ...)

<!-- Router registration pattern (server.py): -->
from routers import admin_kb as admin_kb_router
# ... with try/except ...
app.include_router(admin_kb_router.router, prefix=PREFIX)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Knowledge Base Service + Seed de 12+ reglas + Tests</name>
  <files>
    backend/services/knowledge_base_service.py,
    backend/tests/test_knowledge_base.py,
    init_mongodb_sismo.py
  </files>
  <behavior>
    - get_context_for_operation('registrar_gasto', db) retorna string con reglas que tengan tags ['retenciones','autoretenedores']
    - get_context_for_operation('crear_factura_moto', db) retorna string con reglas que tengan tags ['VIN','factura','moto']
    - get_context_for_operation('registrar_pago_cartera', db) retorna string con reglas que tengan tags ['cartera','duplicado']
    - get_context_for_operation('registrar_nomina', db) retorna string con reglas que tengan tags ['nomina','ReteFuente']
    - get_context_for_operation('unknown_operation', db) retorna string vacio ""
    - get_all_rules_by_category('impuestos', db) retorna lista de dicts con reglas de esa categoria
    - upsert_rule({'rule_id': 'test_rule', 'categoria': 'test', ...}, db) crea o actualiza regla
    - Despues de seed, sismo_knowledge tiene >= 12 documentos
  </behavior>
  <action>
    1. Crear backend/services/knowledge_base_service.py con:
       - OPERATION_TAG_MAP dict que mapea operation_type a lista de tags:
         'registrar_gasto' -> ['retenciones', 'autoretenedores', 'retefuente']
         'crear_factura_moto' -> ['VIN', 'factura', 'moto', 'motor']
         'registrar_pago_cartera' -> ['cartera', 'duplicado', 'pago']
         'registrar_nomina' -> ['nomina', 'retefuente', 'nómina']
         'registrar_arriendo' -> ['retenciones', 'retefuente', 'arrendamiento', 'reteica']
         'conciliacion' -> ['bancos', 'endpoints_alegra', 'contabilidad']
       - async get_context_for_operation(operation_type: str, db) -> str:
         Busca tags del mapeo, hace db.sismo_knowledge.find({"tags": {"$in": relevant_tags}})
         Formatea resultado como bloque "== REGLAS APLICABLES ==\n" con titulo + contenido de cada regla
         Si no hay tags o no hay resultados, retorna ""
       - async get_all_rules_by_category(categoria: str, db) -> list:
         Simple find por categoria, retorna lista de dicts (sin _id)
       - async upsert_rule(rule_dict: dict, db) -> str:
         update_one con upsert=True por rule_id, agrega actualizado_en=now
         Retorna rule_id

    2. Ampliar SISMO_KNOWLEDGE en init_mongodb_sismo.py — agregar estas reglas NUEVAS (las 10 existentes se mantienen):
       - rule_id: "auteco_autoretenedor" — Auteco NIT 860024781 autoretenedor, NUNCA ReteFuente. tags: ['autoretenedores','retefuente','auteco','proveedores']
       - rule_id: "endpoint_journals" — Usar /journals, NUNCA /journal-entries (403). tags: ['endpoints_alegra','journals','asientos']
       - rule_id: "endpoint_categories" — Usar /categories, NUNCA /accounts (403). tags: ['endpoints_alegra','categories','cuentas']
       - rule_id: "fechas_alegra" — Formato yyyy-MM-dd, NUNCA ISO-8601 con timezone. tags: ['endpoints_alegra','fechas','formato']
       - rule_id: "socios_cxc" — Andres CC 80075452 e Ivan CC 80086601 = CXC socios (5329), NUNCA gasto operativo. tags: ['socios','cxc','gastos']
       - rule_id: "retefuente_arriendo" — ReteFuente arriendo 3.5%. tags: ['retefuente','retenciones','arrendamiento']
       - rule_id: "retefuente_servicios" — ReteFuente servicios 4%. tags: ['retefuente','retenciones','servicios']
       - rule_id: "retefuente_compras" — ReteFuente compras 2.5% (base minima $1.344.573). tags: ['retefuente','retenciones','compras']
       - rule_id: "reteica_bogota" — ReteICA Bogota 0.414% en toda operacion. tags: ['reteica','retenciones','bogota']
       - rule_id: "global66_alegra" — Global66 Alegra ID 11100507. tags: ['bancos','global66','alegra']
       - rule_id: "vin_motor_factura" — VIN y motor OBLIGATORIOS en factura moto. tags: ['VIN','motor','factura','moto','inventario']
       - rule_id: "mora_diaria" — Mora $2.000/dia desde jueves post-vencimiento. tags: ['mora','cartera','cobro','cobranza']
       NOTA: Algunas de estas ya existen parcialmente (autoretenedor_regla, iva_cuatrimestral, ica_bogota, cuenta_fallback) — las nuevas agregan especificidad con tags mas granulares. NO borrar las existentes.

    3. Agregar index de tags en init_mongodb_sismo.py dentro de create_all_indexes():
       _safe_index(db.sismo_knowledge, [("tags", 1)], name="sismo_knowledge_tags")

    4. Crear backend/tests/test_knowledge_base.py:
       - Usar mongomock o mock de Motor (patron: mock AsyncIOMotorCollection con side_effect)
       - Test get_context_for_operation con mock que retorna reglas, verifica formato del string
       - Test get_context_for_operation con operation desconocido retorna ""
       - Test upsert_rule llama update_one con upsert=True
       - Test get_all_rules_by_category filtra por categoria
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_knowledge_base.py -v</automated>
  </verify>
  <done>
    - knowledge_base_service.py existe con 3 metodos async
    - SISMO_KNOWLEDGE tiene 22+ reglas (10 existentes + 12 nuevas)
    - Index tags agregado en init_mongodb_sismo.py
    - Tests pasan: get_context_for_operation retorna bloque formateado, upsert funciona, category filter funciona
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin API + Integracion en process_chat()</name>
  <files>
    backend/routers/admin_kb.py,
    backend/server.py,
    backend/ai_chat.py
  </files>
  <action>
    1. Crear backend/routers/admin_kb.py:
       - from fastapi import APIRouter, Depends
       - from database import db
       - from dependencies import get_current_user, require_admin
       - router = APIRouter(tags=["admin-knowledge-base"])

       POST /admin/knowledge-base/upsert:
         Body: dict con rule_id (required), categoria, titulo, contenido, tags
         Calls knowledge_base_service.upsert_rule(body, db)
         Returns {"ok": True, "rule_id": result}
         Protected: require_admin dependency

       GET /admin/knowledge-base/{categoria}:
         Calls knowledge_base_service.get_all_rules_by_category(categoria, db)
         Returns lista de reglas
         Protected: require_admin dependency

    2. Registrar router en backend/server.py:
       - Usar patron try/except como los otros routers nuevos (lineas 22-55):
         try:
             from routers import admin_kb as admin_kb_router
             print("[OK] admin_kb router loaded successfully")
         except Exception as e:
             print(f"[ERROR] Failed to load admin_kb router: {e}")
             admin_kb_router = None
       - En la seccion de include_router:
         if admin_kb_router:
             app.include_router(admin_kb_router.router, prefix=PREFIX)

    3. Integrar en ai_chat.py process_chat() — insertar DESPUES de linea 3027 (despues del .replace("{cfo_context}")) y ANTES de MODULE 4 (pending_topics, linea 3029):

       # -- MODULE KB: Inyectar reglas de negocio relevantes desde Knowledge Base --
       from services.knowledge_base_service import get_context_for_operation
       # Detectar intent basico del mensaje para buscar reglas relevantes
       _msg_lower_kb = user_message.lower()
       _kb_operation = None
       if any(kw in _msg_lower_kb for kw in ["arriendo", "arrendamiento", "alquiler"]):
           _kb_operation = "registrar_arriendo"
       elif any(kw in _msg_lower_kb for kw in ["factura", "moto", "vin", "motor"]):
           _kb_operation = "crear_factura_moto"
       elif any(kw in _msg_lower_kb for kw in ["pago", "cuota", "cartera", "abonar"]):
           _kb_operation = "registrar_pago_cartera"
       elif any(kw in _msg_lower_kb for kw in ["nomina", "nómina", "salario", "sueldo"]):
           _kb_operation = "registrar_nomina"
       elif any(kw in _msg_lower_kb for kw in ["gasto", "servicio", "honorario", "compra", "proveedor"]):
           _kb_operation = "registrar_gasto"
       elif any(kw in _msg_lower_kb for kw in ["conciliacion", "extracto", "banco"]):
           _kb_operation = "conciliacion"

       if _kb_operation:
           _kb_context = await get_context_for_operation(_kb_operation, db)
           if _kb_context:
               system_prompt += "\n\n" + _kb_context

       NOTA CRITICA: El import de knowledge_base_service debe ir al inicio del archivo (junto con los otros imports de services/). NO hacer import inline dentro de process_chat — mover al top-level imports de ai_chat.py.
  </action>
  <verify>
    <automated>cd backend && python -c "from routers import admin_kb; print('admin_kb OK')" && python -c "from services.knowledge_base_service import get_context_for_operation; print('kb_service OK')"</automated>
  </verify>
  <done>
    - admin_kb.py tiene 2 endpoints (POST upsert, GET by category)
    - server.py registra admin_kb router con try/except
    - ai_chat.py process_chat() inyecta kb_context en system_prompt basado en intent del mensaje
    - Import de knowledge_base_service esta al top-level de ai_chat.py
  </done>
</task>

<task type="auto">
  <name>Task 3: Test de integracion E2E — "registra el arriendo" produce ReteFuente + ReteICA en prompt</name>
  <files>
    backend/tests/test_knowledge_base.py
  </files>
  <action>
    Agregar test de integracion al final de test_knowledge_base.py:

    1. test_arriendo_produces_retefuente_and_reteica_context():
       - Preparar mock db.sismo_knowledge con las reglas de arriendo:
         retefuente_arriendo (3.5%), reteica_bogota (0.414%), auteco_autoretenedor
       - Llamar get_context_for_operation("registrar_arriendo", mock_db)
       - Assert "3.5%" in resultado
       - Assert "0.414%" in resultado
       - Assert "REGLAS APLICABLES" in resultado

    2. test_factura_moto_produces_vin_context():
       - Mock con regla vin_motor_factura
       - Llamar get_context_for_operation("crear_factura_moto", mock_db)
       - Assert "VIN" in resultado
       - Assert "motor" in resultado (case insensitive)

    3. test_unknown_operation_returns_empty():
       - Llamar get_context_for_operation("operacion_inexistente", mock_db)
       - Assert resultado == ""

    4. test_seed_knowledge_count():
       - Importar SISMO_KNOWLEDGE de init_mongodb_sismo
       - Assert len(SISMO_KNOWLEDGE) >= 22

    Estos tests validan el criterio de exito principal: "Al enviar 'registra el arriendo de marzo', el system prompt incluye ReteFuente 3.5% y ReteICA 0.414% desde MongoDB"
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_knowledge_base.py -v --tb=short</automated>
  </verify>
  <done>
    - 6+ tests pasan (4 del Task 1 + 4 del Task 3, con overlap en el archivo)
    - El test "arriendo" confirma que ReteFuente 3.5% y ReteICA 0.414% aparecen en el contexto retornado
    - El test de seed confirma >= 22 reglas en SISMO_KNOWLEDGE
  </done>
</task>

</tasks>

<verification>
1. cd backend && python -m pytest tests/test_knowledge_base.py -v
2. cd backend && python -c "from services.knowledge_base_service import get_context_for_operation, get_all_rules_by_category, upsert_rule; print('All KB exports OK')"
3. cd backend && python -c "from routers.admin_kb import router; print(f'Admin KB routes: {len(router.routes)}')"
4. python -c "from init_mongodb_sismo import SISMO_KNOWLEDGE; assert len(SISMO_KNOWLEDGE) >= 22, f'Only {len(SISMO_KNOWLEDGE)} rules'; print(f'OK: {len(SISMO_KNOWLEDGE)} rules')"
5. grep -c "get_context_for_operation" backend/ai_chat.py  # debe ser >= 1
</verification>

<success_criteria>
- knowledge_base_service.py existe con 3 metodos async funcionales
- 22+ reglas en SISMO_KNOWLEDGE (10 existentes + 12 nuevas)
- process_chat() inyecta reglas relevantes en system_prompt basado en intent
- Admin API funcional (2 endpoints protegidos con require_admin)
- Test E2E: get_context_for_operation("registrar_arriendo") retorna string con "3.5%" y "0.414%"
- Todos los tests pasan
</success_criteria>

<output>
After completion, create `.planning/quick/260401-d5z-knowledge-base-service-rag-para-agentes-/260401-d5z-SUMMARY.md`
</output>
