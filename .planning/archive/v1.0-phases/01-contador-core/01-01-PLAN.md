---
phase: 01-contador-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/bank_reconciliation.py
  - .planning/phases/01-contador-core/flujos_cobertura_matrix.md
autonomous: true
requirements: [CONT-00, CONT-01]

must_haves:
  truths:
    - "El campo proveedor se propaga correctamente desde extract_proveedor hasta clasificar_movimiento en los 4 parsers de banco"
    - "Los 4 bloques duplicados de extraccion de proveedor estan consolidados en una sola funcion helper"
    - "Las 30+ reglas de clasificacion en accounting_engine.py reciben un proveedor valido (no desc[:30] truncado)"
    - "Existe una matriz de cobertura que clasifica los 32 flujos contables como Funcional/Parcial/No implementado"
  artifacts:
    - path: "backend/services/bank_reconciliation.py"
      provides: "Proveedor extraction consolidado y propagacion correcta"
      contains: "_extraer_proveedor"
    - path: ".planning/phases/01-contador-core/flujos_cobertura_matrix.md"
      provides: "Matriz de cobertura de 32 flujos contables RODDOS"
      contains: "| Flujo |"
  key_links:
    - from: "backend/services/bank_reconciliation.py"
      to: "backend/services/accounting_engine.py"
      via: "extract_proveedor() llamada desde helper consolidado"
      pattern: "extract_proveedor"
    - from: "backend/services/bank_reconciliation.py"
      to: "clasificar_movimiento"
      via: "campo proveedor pasado como argumento"
      pattern: "proveedor=.*proveedor"
---

<objective>
Fix the proveedor extraction bug that silently disables 30+ classification rules, and audit all 32 accounting flows from Flujos_contables_Roddos.svg to produce the coverage matrix.

Purpose: CONT-01 unblocks bank reconciliation (highest-volume flow). CONT-00 establishes the strategic map that guides this phase and all subsequent phases. Both are foundational with no dependencies.
Output: Fixed bank_reconciliation.py with consolidated proveedor extraction, coverage matrix document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contador-core/01-CONTEXT.md
@.planning/phases/01-contador-core/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix proveedor extraction and consolidate 4 duplicate blocks (CONT-01)</name>
  <files>backend/services/bank_reconciliation.py</files>
  <read_first>
    - backend/services/bank_reconciliation.py (the file being modified — read ALL of it to find the 4 duplicate blocks between lines 119-260 and the clasificar_movimiento calls around lines 357-389)
    - backend/services/accounting_engine.py (source of extract_proveedor function — read to understand what it returns and its fallback behavior with desc[:30].lower())
  </read_first>
  <action>
Per D-01 and D-01b from CONTEXT.md:

1. **Diagnose the bug:** Read `bank_reconciliation.py` completely. Identify the 4 parser blocks (Bancolombia, BBVA, Davivienda, Nequi) between lines 119-260 that have duplicate proveedor extraction logic. Also read `accounting_engine.py` to understand what `extract_proveedor` returns — particularly its fallback of `desc[:30].lower()` which may cause classification rules to fail by returning a truncated description instead of a real supplier name.

2. **Create consolidated helper function** at module level in `bank_reconciliation.py`:
```python
def _extraer_proveedor(descripcion: str) -> str:
    """Helper consolidado — unica fuente de verdad para extraccion de proveedor.
    Retorna el nombre del proveedor o string vacio si no se identifica.
    NUNCA retorna desc[:30] truncado — eso interfiere con reglas de clasificacion.
    """
    from services.accounting_engine import extract_proveedor
    resultado = extract_proveedor(descripcion)
    # Si el resultado es simplemente la descripcion truncada (fallback),
    # retornar string vacio para que las reglas de clasificacion no hagan
    # match contra basura
    if resultado == descripcion[:30].lower():
        return ""
    return resultado
```

3. **Replace all 4 duplicate blocks** with calls to `_extraer_proveedor(descripcion)`. Each parser block should have exactly one line for proveedor extraction: `proveedor = _extraer_proveedor(descripcion)`.

4. **Verify proveedor propagation:** Confirm that the `proveedor` variable extracted by the helper is correctly passed to `clasificar_movimiento(proveedor=proveedor, ...)` in all code paths (around lines 357-389). If any path passes `mov.proveedor` or a different variable, fix it to use the consolidated extraction result.

5. **Add logging:** Add `logger.info(f"Proveedor extraido: '{proveedor}' de descripcion: '{descripcion[:50]}'")` in the helper to make proveedor extraction observable.

IMPORTANT per R-04: Verify that accounting_engine.py contains the Auteco NIT 860024781 autoretenedor constraint. If not present, add: `if nit == "860024781": skip_retefuente = True` in the appropriate classification function. Do NOT modify accounting_engine.py otherwise — only verify the constraint exists.
  </action>
  <verify>
    <automated>cd C:/Users/AndresSanJuan/roddos-workspace/SISMO && python -c "import ast; tree = ast.parse(open('backend/services/bank_reconciliation.py').read()); funcs = [n.name for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]; assert '_extraer_proveedor' in funcs, f'Missing _extraer_proveedor, found: {funcs}'; print('OK: _extraer_proveedor exists')"</automated>
  </verify>
  <acceptance_criteria>
    - bank_reconciliation.py contains `def _extraer_proveedor(descripcion: str)` as a module-level function
    - bank_reconciliation.py has exactly 1 definition of _extraer_proveedor (not 4 duplicate blocks)
    - All 4 parser blocks (Bancolombia, BBVA, Davivienda, Nequi) call `_extraer_proveedor(` instead of inline extraction
    - The helper calls `from services.accounting_engine import extract_proveedor` internally
    - The helper does NOT return `desc[:30].lower()` as a valid proveedor — returns empty string instead
    - clasificar_movimiento calls receive the proveedor value from the consolidated helper
    - logger.info call exists inside _extraer_proveedor for observability
  </acceptance_criteria>
  <done>The 4 duplicate proveedor extraction blocks are consolidated into _extraer_proveedor(), the fallback bug is fixed (returns "" instead of truncated desc), and proveedor propagates correctly to clasificar_movimiento in all code paths.</done>
</task>

<task type="auto">
  <name>Task 2: Audit 32 accounting flows and produce coverage matrix (CONT-00)</name>
  <files>.planning/phases/01-contador-core/flujos_cobertura_matrix.md</files>
  <read_first>
    - backend/services/accounting_engine.py (the classification engine — understand which flows it handles)
    - backend/alegra_service.py (the Alegra API client — understand which operations are implemented: create_contact, create_invoice, register_payment, create_journal, etc.)
    - backend/ai_chat.py (the monolith — search for Alegra-related function calls and tool definitions to map what the agent can do)
    - backend/routers/conciliacion.py (bank reconciliation router — understand bancario flows)
    - backend/routers/alegra_webhooks.py (webhook handler — understand event-driven flows)
    - backend/event_bus.py (event types emitted — understand what events exist)
    - backend/services/scheduler.py (scheduled jobs — understand automated flows)
    - backend/run_smoke_test.py (existing smoke test — understand which flows are already tested)
  </read_first>
  <action>
Per D-00, D-00b, D-00c, D-00d from CONTEXT.md:

1. **Map the 32 flows from the 4 categories defined in D-00c:**
   - **Ingresos:** ventas motos, recaudo cuotas, prestamos, rendimientos
   - **Egresos:** gastos, nomina, activos, anticipos, intereses
   - **Bancario:** conciliacion 4 bancos (Bancolombia, BBVA, Davivienda, Nequi), 4x1000, comisiones
   - **Analisis:** CXC (cuentas por cobrar), CXP (cuentas por pagar), P&L (estado de resultados), flujo de caja

2. **For each flow, search the codebase** to determine:
   - Does a function/endpoint/tool exist that handles this flow?
   - Does it create the correct journal entry in Alegra via `/journals` (per R-02)?
   - Does it use account IDs from `plan_cuentas_roddos` MongoDB collection (per R-03)?
   - Does it verify HTTP 200 from Alegra (per R-01)?

3. **Classify each flow as:**
   - **Funcional:** Endpoint exists, creates correct journal in Alegra, tested in smoke test
   - **Parcial:** Endpoint exists but journal creation is incomplete, untested, or uses wrong accounts
   - **No implementado:** No endpoint or function handles this flow

4. **NOTE about SVG:** Per Pitfall 6 in RESEARCH.md, the SVG file `Flujos_contables_Roddos.svg` may not be in the repo. Use the 4 categories from D-00c as the definitive source for the 32 flows. If the SVG IS available, cross-reference to ensure completeness. If not, note in the matrix that the audit was based on D-00c categories and needs SVG verification when available.

5. **Produce the matrix** as a markdown file at `.planning/phases/01-contador-core/flujos_cobertura_matrix.md` with this structure:
```markdown
# Matriz de Cobertura - Flujos Contables RODDOS

## Resumen
- Funcional: N/32
- Parcial: N/32
- No implementado: N/32

## Ingresos
| # | Flujo | Estado | Archivo/Funcion | Journal Alegra | Notas |
|---|-------|--------|-----------------|----------------|-------|
| 1 | Venta moto contado | Funcional/Parcial/No impl | path:function | Si/No | ... |

## Egresos
(same table format)

## Bancario
(same table format)

## Analisis
(same table format)

## Prioridades (per D-00b)
Ordenar flujos Parcial/No implementado por impacto financiero:
1. Ingresos > Egresos > Analisis
```
  </action>
  <verify>
    <automated>cd C:/Users/AndresSanJuan/roddos-workspace/SISMO && test -f .planning/phases/01-contador-core/flujos_cobertura_matrix.md && grep -c "| Flujo |" .planning/phases/01-contador-core/flujos_cobertura_matrix.md | xargs -I{} test {} -ge 4 && echo "OK: Matrix exists with 4+ table headers"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01-contador-core/flujos_cobertura_matrix.md` exists
    - Contains sections for all 4 categories: "## Ingresos", "## Egresos", "## Bancario", "## Analisis"
    - Contains a "## Resumen" section with counts of Funcional/Parcial/No implementado totaling 32 or close
    - Each flow row has columns: #, Flujo, Estado, Archivo/Funcion, Journal Alegra, Notas
    - Contains "## Prioridades" section ordering non-functional flows by financial impact
    - Every "Funcional" flow references a specific file:function
    - Every "Parcial" flow describes what is missing
  </acceptance_criteria>
  <done>Coverage matrix exists with all 32 flows classified as Funcional/Parcial/No implementado, with specific file:function references for implemented flows and a priority list for gaps ordered by financial impact (ingresos > egresos > analisis).</done>
</task>

</tasks>

<verification>
- `_extraer_proveedor` function exists in bank_reconciliation.py
- No duplicate proveedor extraction blocks remain (4 blocks replaced by 1 helper call each)
- Coverage matrix exists with 32 flows classified across 4 categories
- R-01, R-02, R-03, R-04 constraints verified during audit
</verification>

<success_criteria>
- bank_reconciliation.py has consolidated proveedor extraction that does not return truncated descriptions
- All 4 bank parsers use the consolidated helper
- Coverage matrix classifies all 32 accounting flows with specific code references
- Priority list identifies highest-impact gaps for subsequent work
</success_criteria>

<output>
After completion, create `.planning/phases/01-contador-core/01-01-SUMMARY.md`
</output>
