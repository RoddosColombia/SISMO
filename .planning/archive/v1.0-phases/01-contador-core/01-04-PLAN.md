---
phase: 01-contador-core
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - backend/ai_chat.py
  - backend/agents/__init__.py
  - backend/agents/prompt_templates.py
  - backend/agents/file_parser.py
  - backend/agents/context_builder.py
  - backend/agents/contador_agent.py
autonomous: true
requirements: [CONT-02]

must_haves:
  truths:
    - "ai_chat.py es un thin router/dispatcher que importa de backend/agents/"
    - "prompt_templates.py contiene todos los system prompts y templates de mensajes extraidos de ai_chat.py"
    - "file_parser.py contiene las funciones de parsing de extractos, facturas y documentos"
    - "context_builder.py contiene las funciones de construccion de contexto desde DB/Alegra/loanbooks"
    - "contador_agent.py contiene la logica del agente Contador (tool calls, decision making)"
    - "El sistema funciona identicamente despues de la decomposicion — smoke test 20/20 pasa"
  artifacts:
    - path: "backend/agents/__init__.py"
      provides: "Package init para modulos de agentes"
    - path: "backend/agents/prompt_templates.py"
      provides: "System prompts y templates extraidos de ai_chat.py"
      contains: "def "
    - path: "backend/agents/file_parser.py"
      provides: "Funciones de parsing de archivos"
      contains: "_tabular_to_text"
    - path: "backend/agents/context_builder.py"
      provides: "Construccion de contexto para prompts"
      contains: "def "
    - path: "backend/agents/contador_agent.py"
      provides: "Logica del agente Contador"
      contains: "def "
    - path: "backend/ai_chat.py"
      provides: "Thin router/dispatcher que importa de agents/"
      contains: "from agents."
  key_links:
    - from: "backend/ai_chat.py"
      to: "backend/agents/prompt_templates.py"
      via: "import"
      pattern: "from agents.prompt_templates import"
    - from: "backend/ai_chat.py"
      to: "backend/agents/file_parser.py"
      via: "import"
      pattern: "from agents.file_parser import"
    - from: "backend/ai_chat.py"
      to: "backend/agents/context_builder.py"
      via: "import"
      pattern: "from agents.context_builder import"
    - from: "backend/ai_chat.py"
      to: "backend/agents/contador_agent.py"
      via: "import"
      pattern: "from agents.contador_agent import"
---

<objective>
Decompose ai_chat.py (5,217 lines) into 4 focused modules under backend/agents/, leaving ai_chat.py as a thin router/dispatcher.

Purpose: Per P-03, this decomposition happens AFTER the smoke test 20/20 passes (Plan 02), so we have a regression safety net. The decomposition is purely structural — functions move as-is without logic changes (per D-02c). This eliminates the single point of failure and enables future unit testing per module.
Output: 4 new modules in backend/agents/ + slimmed ai_chat.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contador-core/01-CONTEXT.md
@.planning/phases/01-contador-core/01-RESEARCH.md
@.planning/phases/01-contador-core/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract prompt_templates.py and file_parser.py from ai_chat.py</name>
  <files>backend/agents/__init__.py, backend/agents/prompt_templates.py, backend/agents/file_parser.py, backend/ai_chat.py</files>
  <read_first>
    - backend/ai_chat.py (the source monolith — read ALL 5,217 lines. Run `grep -n "^def \|^async def \|^class " backend/ai_chat.py` first to map all functions. Identify: 1) string constants, system prompts, prompt templates -> prompt_templates.py, 2) _tabular_to_text, _is_tabular_file, and parsing functions -> file_parser.py)
  </read_first>
  <action>
Per D-02, D-02b, D-02c from CONTEXT.md and Pattern 2 from RESEARCH.md:

**Extraction order matters.** Extract easiest modules first (least dependencies).

**Step 1: Create backend/agents/ directory:**
```python
# backend/agents/__init__.py
"""Modulos de agentes SISMO — descompuestos de ai_chat.py (Phase 1, CONT-02)."""
```

**Step 2: Extract prompt_templates.py (strings/templates, zero dependencies):**
- Search ai_chat.py for: string constants used as system prompts, message templates, instruction blocks, any variable that is a multi-line string used in prompt construction
- Move ALL prompt-related strings and template functions to `backend/agents/prompt_templates.py`
- The functions/constants move AS-IS — do not refactor their content (per D-02c)
- In ai_chat.py, replace with: `from agents.prompt_templates import <function_names>`
- If a function has internal imports (inside the function body), keep them in the new module — do not move to top-level (per Pitfall 1 in RESEARCH.md — avoid circular imports)

**Step 3: Extract file_parser.py (parsing functions):**
- Search ai_chat.py for: `_tabular_to_text`, `_is_tabular_file`, and any function that parses uploaded files (extractos bancarios, facturas, PDFs, Excel files)
- Move ALL file parsing functions to `backend/agents/file_parser.py`
- The functions move AS-IS — do not refactor (per D-02c)
- In ai_chat.py, replace with: `from agents.file_parser import <function_names>`
- Keep lazy imports inside function bodies to prevent circular imports

**Step 4: Verify after each extraction:**
- After extracting prompt_templates.py: `python -c "from agents.prompt_templates import *; print('OK')"` from the backend directory
- After extracting file_parser.py: `python -c "from agents.file_parser import *; print('OK')"` from the backend directory
- Check for ImportError — if found, the function has a hidden dependency that needs a lazy import

**CRITICAL per D-02c:** Functions are moved EXACTLY as they are. No renaming, no parameter changes, no logic improvements. The goal is separation, not improvement.
  </action>
  <verify>
    <automated>cd C:/Users/AndresSanJuan/roddos-workspace/SISMO && test -f backend/agents/__init__.py && test -f backend/agents/prompt_templates.py && test -f backend/agents/file_parser.py && python -c "content = open('backend/ai_chat.py').read(); assert 'from agents.prompt_templates import' in content or 'from agents.file_parser import' in content, 'ai_chat.py not importing from agents/'; print('OK: modules extracted and imported')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/agents/__init__.py exists
    - backend/agents/prompt_templates.py exists and contains at least one function or constant definition
    - backend/agents/file_parser.py exists and contains `_tabular_to_text` or equivalent file parsing function
    - backend/ai_chat.py contains `from agents.prompt_templates import`
    - backend/ai_chat.py contains `from agents.file_parser import`
    - No `ImportError` when running `python -c "from agents.prompt_templates import *"` from backend/
    - No `ImportError` when running `python -c "from agents.file_parser import *"` from backend/
  </acceptance_criteria>
  <done>prompt_templates.py and file_parser.py extracted from ai_chat.py with all functions moved as-is. ai_chat.py imports from both new modules. No circular imports.</done>
</task>

<task type="auto">
  <name>Task 2: Extract context_builder.py and contador_agent.py, finalize thin router</name>
  <files>backend/agents/context_builder.py, backend/agents/contador_agent.py, backend/ai_chat.py</files>
  <read_first>
    - backend/ai_chat.py (read the current state AFTER Task 1 extraction — it should be smaller now. Identify: 1) functions that build context from DB/Alegra/loanbooks -> context_builder.py, 2) tool call handling, decision making, agent logic -> contador_agent.py, 3) what remains as the thin dispatcher)
    - backend/agents/prompt_templates.py (already extracted — understand what's already out)
    - backend/agents/file_parser.py (already extracted — understand what's already out)
  </read_first>
  <action>
Per D-02, D-02b, D-02c from CONTEXT.md and Pattern 2 from RESEARCH.md:

**Step 1: Extract context_builder.py (data fetching + context construction):**
- Search remaining ai_chat.py for functions that:
  - Query MongoDB for context data (loanbooks, clients, transactions, etc.)
  - Fetch data from Alegra API to build prompt context
  - Construct the context dict/object that gets passed to the LLM
  - Build summaries or descriptions of data for prompts
- Move ALL context-building functions to `backend/agents/context_builder.py`
- Functions move AS-IS (per D-02c)
- In ai_chat.py, replace with: `from agents.context_builder import <function_names>`
- Keep lazy imports inside function bodies

**Step 2: Extract contador_agent.py (agent logic):**
- Search remaining ai_chat.py for functions that:
  - Handle tool calls (save_account_mapping, create_plan, etc.)
  - Make decisions based on LLM output
  - Execute accounting actions
  - Manage conversation state for the Contador agent specifically
- Move ALL Contador agent logic to `backend/agents/contador_agent.py`
- Functions move AS-IS (per D-02c)
- In ai_chat.py, replace with: `from agents.contador_agent import <function_names>`

**Step 3: Finalize ai_chat.py as thin router/dispatcher:**
- What should REMAIN in ai_chat.py:
  - The main entry point function(s) that routers call (e.g., `execute_chat_action`, `process_message`)
  - Import statements from the 4 agent modules
  - Routing logic that dispatches to the correct module based on request type
  - Any FastAPI/Pydantic model definitions that routers import directly from ai_chat.py (these can stay to avoid breaking router imports)
- ai_chat.py should be significantly smaller (target: under 500 lines, ideally under 300)

**Step 4: Verify each extraction:**
- After context_builder.py: `python -c "from agents.context_builder import *; print('OK')"`
- After contador_agent.py: `python -c "from agents.contador_agent import *; print('OK')"`
- Final: `python -c "import ai_chat; print('OK')"` to verify ai_chat.py still loads

**Step 5: Run full import chain verification:**
```python
python -c "
from agents.prompt_templates import *
from agents.file_parser import *
from agents.context_builder import *
from agents.contador_agent import *
import ai_chat
print('All modules load successfully')
"
```

**CRITICAL:** If any function in ai_chat.py is imported by OTHER files in the codebase (not just the routers), those imports must still work. Search for `from ai_chat import` and `import ai_chat` across the codebase to find all consumers. If external files import specific functions, either:
- Keep a re-export in ai_chat.py: `from agents.context_builder import build_context_for_agent  # re-export for backward compat`
- Or update the importing file to use the new path

Per D-02d: This decomposition is protected by the smoke test from Plan 02. After completing both tasks, run the smoke test to verify no regressions.
  </action>
  <verify>
    <automated>cd C:/Users/AndresSanJuan/roddos-workspace/SISMO && test -f backend/agents/context_builder.py && test -f backend/agents/contador_agent.py && python -c "content = open('backend/ai_chat.py').read(); assert 'from agents.' in content, 'ai_chat.py not importing from agents/'; lines = len(content.splitlines()); print(f'ai_chat.py: {lines} lines'); assert lines < 2000, f'ai_chat.py still too large: {lines} lines (target: under 500)'"</automated>
  </verify>
  <acceptance_criteria>
    - backend/agents/context_builder.py exists and contains at least one function definition
    - backend/agents/contador_agent.py exists and contains at least one function definition
    - backend/ai_chat.py contains `from agents.context_builder import` and `from agents.contador_agent import`
    - backend/ai_chat.py is under 2000 lines (significant reduction from 5,217)
    - Running `python -c "import ai_chat"` from backend/ does NOT produce ImportError
    - Running `python -c "from agents.prompt_templates import *; from agents.file_parser import *; from agents.context_builder import *; from agents.contador_agent import *"` from backend/ produces no errors
    - No other file in the codebase has broken imports (search for `from ai_chat import` and verify all still resolve)
  </acceptance_criteria>
  <done>ai_chat.py decomposed into 4 modules: prompt_templates.py, file_parser.py, context_builder.py, contador_agent.py. ai_chat.py is now a thin router/dispatcher under 2000 lines. All imports resolve without errors. All external consumers of ai_chat.py still work via re-exports or updated imports.</done>
</task>

</tasks>

<verification>
- All 4 module files exist in backend/agents/
- ai_chat.py imports from all 4 modules
- ai_chat.py is significantly smaller than 5,217 lines
- No ImportError in any module
- No broken imports in rest of codebase
- Smoke test: `python backend/run_smoke_test.py` passes 20/20 (regression check per D-02d)
</verification>

<success_criteria>
- 4 new modules exist under backend/agents/ with clear responsibilities
- ai_chat.py is a thin router/dispatcher
- System behavior is identical — smoke test 20/20 still passes
- No circular imports
- Functions were moved as-is without logic changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-contador-core/01-04-SUMMARY.md`
</output>
