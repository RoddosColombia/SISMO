---
phase: 02-consolidacion-capa-alegra
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alegra_service.py
  - backend/tests/test_alegra_service.py
autonomous: true
requirements:
  - ALEGRA-06
gap_closure: true

must_haves:
  truths:
    - "request('journal-entries', 'GET') lanza HTTPException(400) con mensaje 'prohibido' antes de emitir llamada HTTP"
    - "request('accounts', 'GET') lanza HTTPException(400) con mensaje 'prohibido' antes de emitir llamada HTTP"
    - "_mock('journal-entries', 'GET') lanza HTTPException(400) — mismo comportamiento que produccion"
    - "_mock('accounts', 'GET') lanza HTTPException(400) — mismo comportamiento que produccion"
    - "test_request_rejects_journal_entries y test_request_rejects_accounts pasan"
  artifacts:
    - path: "backend/alegra_service.py"
      provides: "Pre-flight guard en request() antes del bloque httpx"
      contains: "Endpoint /journal-entries prohibido"
    - path: "backend/alegra_service.py"
      provides: "Guard en _mock() que lanza HTTPException para endpoints prohibidos"
      contains: "Endpoint /accounts prohibido"
    - path: "backend/tests/test_alegra_service.py"
      provides: "Tests que verifican bloqueo pre-vuelo"
      contains: "test_request_rejects_journal_entries"
  key_links:
    - from: "backend/tests/test_alegra_service.py"
      to: "backend/alegra_service.py"
      via: "pytest raises HTTPException"
      pattern: "pytest.raises.*HTTPException"
---

<objective>
Gap closure para ALEGRA-06: agregar guards pre-vuelo en `request()` y `_mock()` de AlegraService para que los endpoints prohibidos (`/journal-entries`, `/accounts`) fallen de forma explicita con HTTPException(400) antes de emitir cualquier llamada HTTP hacia Alegra.

Purpose: ALEGRA-06 exige error explicito con mensaje descriptivo en espanol antes del contacto HTTP. Actualmente `_mock()` retorna `{}` silenciosamente y `request()` en produccion silencia el 403 de Alegra con `return []`. Esto oculta llamadas accidentales sin feedback al desarrollador.

Output: alegra_service.py con guards en dos puntos de entrada + suite de 19 tests todos pasando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-consolidacion-capa-alegra/02-VERIFICATION.md
</context>

<interfaces>
<!-- Contratos relevantes extraidos de backend/alegra_service.py -->

```python
# Linea 7 — HTTPException ya importado, no agregar import
from fastapi import HTTPException

# Linea 201 — firma de request()
async def request(self, endpoint: str, method: str = "GET", body: dict = None, params: dict = None):
    is_demo = await self.is_demo_mode()
    if is_demo:
        return self._mock(endpoint, method, body, params)
    # ... bloque httpx empieza en linea 212

# Linea 361 — firma de _mock() con su rama "accounts" actual
def _mock(self, endpoint: str, method: str, body: dict = None, params: dict = None):
    body = body or {}
    if endpoint == "accounts" or (endpoint.startswith("accounts") and "bank" not in endpoint):
        return MOCK_ACCOUNTS          # <- NO existe rama "categories" — cae en return {} al final
    # ...
    if "journals" in endpoint:        # <- captura "journal-entries" tambien (bug)
        ...
    return {}                         # <- journal-entries cae aqui silenciosamente
```

```python
# Linea 240-244 — bloque 403 actual en request() (PRODUCCION):
if resp.status_code == 403:
    if method == "GET":
        return []  # <- silencioso, debe eliminarse
    msg = self._translate_error_to_spanish(403, error_data, endpoint, method)
    raise HTTPException(status_code=403, detail=msg)
```

<!-- Estado actual de tests relevantes (lineas 69-73 y 205-214) -->

```python
# test_get_categories_demo — linea 69 (llama "accounts", debe cambiar a "categories")
def test_get_categories_demo(self, alegra_svc):
    result = _run(alegra_svc.request("accounts", "GET"))
    assert isinstance(result, list)

# test_mock_rejects_journal_entries — linea 205 (assert result == {}, debe cambiar a pytest.raises)
def test_mock_rejects_journal_entries(self, alegra_svc):
    result = alegra_svc._mock("journal-entries", "GET")
    assert result == {}, "..."
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Actualizar tests — RED phase (TDD)</name>
  <files>backend/tests/test_alegra_service.py</files>
  <read_first>
    - backend/tests/test_alegra_service.py (completo — especialmente test_get_categories_demo en linea 69 y test_mock_rejects_journal_entries en linea 205)
  </read_first>
  <behavior>
    - test_get_categories_demo: debe llamar request("categories", "GET") en lugar de request("accounts", "GET") — categories es el endpoint correcto en produccion
    - test_mock_rejects_journal_entries: debe usar pytest.raises(HTTPException) en lugar de assert result == {} — porque Task 2 convierte _mock("journal-entries") en raise HTTPException
    - test_request_rejects_journal_entries: llama request("journal-entries", "GET") en modo demo, espera HTTPException con status_code=400
    - test_request_rejects_accounts: llama request("accounts", "GET") en modo demo, espera HTTPException con status_code=400
    - Ambos tests nuevos deben estar en una clase nueva: TestProhibitedEndpoints
    - Los tests usan el fixture alegra_svc existente (modo demo — is_demo=True)
    - Usar pytest.raises(HTTPException) como context manager
  </behavior>
  <action>
    Hacer TRES cambios en backend/tests/test_alegra_service.py:

    **Cambio 1 — Corregir test_get_categories_demo (linea 71):**

    Cambiar la linea:
    ```python
        result = _run(alegra_svc.request("accounts", "GET"))
    ```
    Por:
    ```python
        result = _run(alegra_svc.request("categories", "GET"))
    ```

    Tambien actualizar el comentario en la linea 70 de:
    ```python
        # /categories es el reemplazo de /accounts — el mock responde por 'accounts'
    ```
    A:
    ```python
        # /categories es el endpoint correcto — /accounts es prohibido (ALEGRA-06)
    ```

    El assert `isinstance(result, list)` y `len(result) > 0` siguen siendo validos — Task 2
    agrega una rama "categories" en _mock() que retorna MOCK_ACCOUNTS.

    **Cambio 2 — Actualizar test_mock_rejects_journal_entries en clase TestMockBugALEGRA03:**

    Reemplazar el metodo completo test_mock_rejects_journal_entries (lineas 205-214):
    ```python
    def test_mock_rejects_journal_entries(self, alegra_svc):
        """journal-entries NO debe retornar datos de journals — debe retornar {} (fallback).

        Este test falla (RED) hasta que Task 2 aplique el fix en alegra_service.py.
        """
        result = alegra_svc._mock("journal-entries", "GET")
        assert result == {}, (
            "_mock('journal-entries', 'GET') debe retornar {} (endpoint invalido en produccion). "
            "Si retorna lista, el bug ALEGRA-03 aun no esta corregido."
        )
    ```
    Por:
    ```python
    def test_mock_rejects_journal_entries(self, alegra_svc):
        """journal-entries debe lanzar HTTPException(400) — mismo comportamiento que produccion.

        Per ALEGRA-06: el mock refleja el comportamiento real de produccion.
        Este test falla (RED) hasta que Task 2 aplique el guard en alegra_service.py.
        """
        from fastapi import HTTPException
        with pytest.raises(HTTPException) as exc_info:
            alegra_svc._mock("journal-entries", "GET")
        assert exc_info.value.status_code == 400
    ```

    **Cambio 3 — Agregar clase TestProhibitedEndpoints al final del archivo:**

    Agregar despues de la ultima clase existente al final del archivo:

    ```python
    # ── Seccion: Test ALEGRA-06 — Endpoints Prohibidos ───────────────────────────


    class TestProhibitedEndpoints:
        """Verifica que endpoints prohibidos generan HTTPException ANTES de llamar a la API.

        Per ALEGRA-06: /journal-entries y /accounts deben bloquearse pre-vuelo con
        HTTPException(400) y mensaje descriptivo en espanol.
        """

        def test_request_rejects_journal_entries(self, alegra_svc):
            """request('journal-entries') debe lanzar HTTPException(400) — nunca llega a httpx."""
            from fastapi import HTTPException
            with pytest.raises(HTTPException) as exc_info:
                _run(alegra_svc.request("journal-entries", "GET"))
            assert exc_info.value.status_code == 400
            assert "journal-entries" in exc_info.value.detail.lower() or "journal-entries" in exc_info.value.detail

        def test_request_rejects_accounts(self, alegra_svc):
            """request('accounts') debe lanzar HTTPException(400) — nunca llega a httpx."""
            from fastapi import HTTPException
            with pytest.raises(HTTPException) as exc_info:
                _run(alegra_svc.request("accounts", "GET"))
            assert exc_info.value.status_code == 400
            assert "accounts" in exc_info.value.detail.lower() or "accounts" in exc_info.value.detail
    ```

    NO modificar TestRequestWithVerify, TestPostWithMock, ni TestTranslateErrors — esas clases
    no cambian su comportamiento. Solo se tocan: TestDemoModeEndpoints (un test), TestMockBugALEGRA03
    (un test), y se agrega TestProhibitedEndpoints al final.
  </action>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && python -m pytest tests/test_alegra_service.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    TDD RED phase confirmada. Estado esperado despues de Task 1 antes de Task 2:

    - test_get_categories_demo FALLA — _mock("categories") aun no existe, retorna {} que no es lista
    - test_mock_rejects_journal_entries FALLA — _mock("journal-entries") retorna {} en lugar de raise HTTPException
    - test_request_rejects_journal_entries FALLA — guard aun no existe en request()/_mock()
    - test_request_rejects_accounts FALLA — guard aun no existe en request()/_mock()

    Exactamente 4 FAILED confirmados antes de continuar con Task 2. Los otros 13 tests pasan sin cambios.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Agregar pre-flight guards en request() y _mock() — GREEN phase (TDD)</name>
  <files>backend/alegra_service.py</files>
  <read_first>
    - backend/alegra_service.py (completo — especialmente lineas 201-272 para request() y 361-428 para _mock())
  </read_first>
  <behavior>
    - request("journal-entries", "GET") lanza HTTPException(status_code=400, detail contiene "journal-entries prohibido")
    - request("accounts", "GET") lanza HTTPException(status_code=400, detail contiene "accounts prohibido")
    - _mock("journal-entries", "GET") lanza HTTPException(status_code=400, detail contiene "journal-entries prohibido")
    - _mock("accounts", "GET") lanza HTTPException(status_code=400, detail contiene "accounts prohibido")
    - _mock("categories", "GET") retorna MOCK_ACCOUNTS (sin bloqueo — endpoint correcto)
    - request("journals", "GET") NO es bloqueado — sigue funcionando
    - request("bank-accounts", "GET") NO es bloqueado — "bank" en endpoint lo excluye del guard de accounts
    - _mock("journals", "GET") NO es bloqueado — sigue retornando MOCK_JOURNAL_ENTRIES
  </behavior>
  <action>
    Hacer tres cambios en backend/alegra_service.py:

    **Cambio 1 — Guard en request() (antes del bloque httpx, linea 208):**

    Insertar INMEDIATAMENTE despues de la linea `logger.info(f"[Alegra REAL] {method} {endpoint} → Calling production API")` (linea 208) y ANTES de `headers = await self.get_auth_header()` (linea 209):

    ```python
        # Pre-flight: bloquear endpoints prohibidos antes de emitir la llamada HTTP
        if "journal-entries" in endpoint:
            raise HTTPException(status_code=400, detail="Endpoint /journal-entries prohibido — usa /journals")
        if endpoint == "accounts" or (endpoint.startswith("accounts") and "bank" not in endpoint):
            raise HTTPException(status_code=400, detail="Endpoint /accounts prohibido — usa /categories")
    ```

    **Cambio 2 — Guard en _mock() (al inicio del metodo, linea 362):**

    Las primeras dos lineas de _mock() son:
    ```python
        body = body or {}
        if endpoint == "accounts" or (endpoint.startswith("accounts") and "bank" not in endpoint):
            return MOCK_ACCOUNTS
    ```

    Reemplazar esas lineas con:
    ```python
        body = body or {}
        # Pre-flight: endpoints prohibidos deben lanzar error explicito — mismo comportamiento que produccion
        if "journal-entries" in endpoint:
            raise HTTPException(status_code=400, detail="Endpoint /journal-entries prohibido — usa /journals")
        if endpoint == "accounts" or (endpoint.startswith("accounts") and "bank" not in endpoint):
            raise HTTPException(status_code=400, detail="Endpoint /accounts prohibido — usa /categories")
    ```

    **Cambio 3 — Agregar rama "categories" en _mock() (DESPUES de los guards del Cambio 2):**

    Esta rama NO existe en _mock() — confirmar con grep antes de agregar. Agregar INMEDIATAMENTE
    despues del guard de accounts del Cambio 2:

    ```python
        if endpoint == "categories":
            return MOCK_ACCOUNTS
    ```

    NO tocar el bloque 403 en request() (lineas 240-244) — ese manejo cubre otros endpoints que
    legitimamente retornan 403 de Alegra. El guard pre-vuelo es suficiente para los dos endpoints
    prohibidos.
  </action>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && python -m pytest tests/test_alegra_service.py -v --tb=short 2>&1 | tail -25</automated>
    <automated>grep -n '"categories"' /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend/alegra_service.py</automated>
  </verify>
  <done>
    TDD GREEN phase: `python -m pytest tests/test_alegra_service.py -v` retorna "19 passed".
    Desglose: 17 tests originales (con test_get_categories_demo y test_mock_rejects_journal_entries
    ahora corregidos) + 2 nuevos tests en TestProhibitedEndpoints = 19 total.
    `grep -n '"categories"' backend/alegra_service.py` retorna al menos 1 linea (rama en _mock()).
    `grep -n "journal-entries prohibido" backend/alegra_service.py` retorna 2 lineas.
    `grep -n "accounts prohibido" backend/alegra_service.py` retorna 2 lineas.
    ALEGRA-06 satisfecho.
  </done>
</task>

</tasks>

<verification>
```bash
# Verificacion completa del gap closure
cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend

# 1. Guards presentes en alegra_service.py
grep -n "journal-entries prohibido" alegra_service.py
grep -n "accounts prohibido" alegra_service.py

# 2. Rama "categories" presente en _mock()
grep -n '"categories"' alegra_service.py

# 3. Suite de tests completa — debe ser 19 passed
python -m pytest tests/test_alegra_service.py -v --tb=short

# 4. Protocolo de commit — ningun patron prohibido
grep -rn "app.alegra.com/api/r1" .
grep -rn "journal-entries" routers/ services/ 2>/dev/null | grep -v test | grep -v "prohibido"
```
</verification>

<success_criteria>
- `grep -n "journal-entries prohibido" backend/alegra_service.py` retorna exactamente 2 lineas
- `grep -n "accounts prohibido" backend/alegra_service.py` retorna exactamente 2 lineas
- `grep -n '"categories"' backend/alegra_service.py` retorna al menos 1 linea (rama en _mock())
- `python -m pytest backend/tests/test_alegra_service.py -v` retorna "19 passed"
- ALEGRA-06 satisfecho: ambos endpoints prohibidos generan HTTPException(400) pre-vuelo en produccion Y demo
</success_criteria>

<output>
After completion, create `.planning/phases/02-consolidacion-capa-alegra/02-04-SUMMARY.md`
</output>
