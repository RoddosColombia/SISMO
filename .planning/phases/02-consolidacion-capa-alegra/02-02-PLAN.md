---
phase: 02-consolidacion-capa-alegra
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/routers/auditoria.py
  - backend/routers/conciliacion.py
  - backend/services/dian_service.py
autonomous: true
requirements:
  - ALEGRA-01
  - ALEGRA-02
  - ALEGRA-05
must_haves:
  truths:
    - "auditoria.py usa AlegraService.request() en vez de httpx directo"
    - "conciliacion.py usa AlegraService.request() en vez de httpx directo"
    - "dian_service.py usa AlegraService.request() en vez de httpx directo"
    - "Errores de Alegra en estos 3 modulos se traducen al espanol automaticamente"
  artifacts:
    - path: "backend/routers/auditoria.py"
      provides: "Audit endpoints via AlegraService"
      contains: "AlegraService"
    - path: "backend/routers/conciliacion.py"
      provides: "Conciliacion endpoints via AlegraService"
      contains: "AlegraService"
    - path: "backend/services/dian_service.py"
      provides: "DIAN service via AlegraService"
      contains: "AlegraService"
  key_links:
    - from: "backend/routers/auditoria.py"
      to: "backend/alegra_service.py"
      via: "import AlegraService, instantiate with db"
      pattern: "AlegraService\\(db\\)"
    - from: "backend/routers/conciliacion.py"
      to: "backend/alegra_service.py"
      via: "import AlegraService, use request()"
      pattern: "alegra\\.request\\("
    - from: "backend/services/dian_service.py"
      to: "backend/alegra_service.py"
      via: "import AlegraService, use request()"
      pattern: "alegra\\.request\\("
---

<objective>
Migrar 3 modulos con bypass directo (auditoria.py, conciliacion.py, dian_service.py) para que usen AlegraService.request() en vez de httpx directo contra ALEGRA_BASE_URL.

Purpose: Estos 3 modulos tienen endpoints HTTP normales (con request context y acceso a db), lo que hace la migracion directa. Despues de este plan, solo quedan 2 modulos complejos (background tasks) para Plan 03.
Output: 3 archivos modificados, 0 llamadas httpx directas a Alegra en estos archivos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ALEGRA-AUDIT.md
@.planning/phases/02-consolidacion-capa-alegra/02-01-SUMMARY.md
@backend/alegra_service.py
@backend/routers/auditoria.py
@backend/routers/conciliacion.py
@backend/services/dian_service.py
</context>

<interfaces>
<!-- AlegraService API que los modulos migraran a usar -->

```python
# backend/alegra_service.py
class AlegraService:
    def __init__(self, db):
        self.db = db

    async def request(self, endpoint: str, method: str = "GET", body: dict = None, params: dict = None):
        """HTTP generico con manejo de errores en espanol, retry-ready."""
        ...

    async def request_with_verify(self, endpoint: str, method: str, body: dict = None) -> dict:
        """POST + GET verificacion obligatoria. Para operaciones de escritura."""
        ...

    async def get_auth_header(self):
        """Retorna headers con Basic Auth. Usa credenciales de env vars > MongoDB > demo."""
        ...
```

<!-- Patron de uso correcto (ya implementado en 20 modulos): -->
```python
from alegra_service import AlegraService
from database import db

alegra = AlegraService(db)
result = await alegra.request("journals", "GET", params={"limit": 100})
# Para escritura:
result = await alegra.request_with_verify("bills", "POST", body=payload)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrar auditoria.py — eliminar httpx directo</name>
  <files>backend/routers/auditoria.py</files>
  <read_first>backend/routers/auditoria.py (completo), backend/alegra_service.py (lineas 201-267 — metodo request()), backend/database.py (para import db)</read_first>
  <action>
    Per ALEGRA-05: Migrar auditoria.py de httpx directo a AlegraService.

    **Cambios en imports (inicio del archivo):**
    - Cambiar `from alegra_service import ALEGRA_BASE_URL` a `from alegra_service import AlegraService`
    - Agregar `from database import db` si no existe
    - ELIMINAR `import httpx` (ya no necesario si no se usa en ningun otro lugar del archivo)
    - ELIMINAR `import base64` si solo se usaba para auth de Alegra
    - ELIMINAR `import os` si solo se usaba para ALEGRA_EMAIL/ALEGRA_TOKEN

    **Cambio en funcion `get_alegra_journals()` (linea 51):**
    Reemplazar todo el bloque httpx con:
    ```python
    async def get_alegra_journals(limit: int = 100) -> List[dict]:
        """Obtiene journals de Alegra via AlegraService."""
        alegra = AlegraService(db)
        all_journals = []
        offset = 0
        while True:
            batch = await alegra.request("journals", "GET", params={"limit": limit, "offset": offset})
            if not batch or not isinstance(batch, list):
                break
            all_journals.extend(batch)
            if len(batch) < limit:
                break
            offset += limit
        return all_journals
    ```
    - ELIMINAR `get_alegra_credentials()` si solo era usada por `get_alegra_journals()`
    - Actualizar callers de `get_alegra_journals()` para no pasar email/token (ya no necesarios)

    **Cambio en endpoint test-alegra (linea ~547):**
    El endpoint de test que hace GET directo: reemplazar httpx block con:
    ```python
    alegra = AlegraService(db)
    result = await alegra.request("journals", "GET", params={"limit": 1})
    ```
    Adaptar la respuesta para mantener la misma estructura de retorno.

    **REGLA:** No cambiar la logica de negocio de auditoria (busqueda de journals por codigo, etc.). Solo cambiar la capa de transporte HTTP.
  </action>
  <verify>
    <automated>cd backend && grep -n "httpx" routers/auditoria.py | grep -v "^#" | head -5; grep -c "ALEGRA_BASE_URL" routers/auditoria.py; grep -c "AlegraService" routers/auditoria.py</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ALEGRA_BASE_URL" backend/routers/auditoria.py retorna 0
    - grep "AlegraService" backend/routers/auditoria.py retorna match
    - grep "from alegra_service import AlegraService" backend/routers/auditoria.py retorna match
    - grep -c "httpx.AsyncClient" backend/routers/auditoria.py retorna 0 (si httpx ya no se usa)
  </acceptance_criteria>
  <done>auditoria.py usa AlegraService.request() para todas las llamadas a Alegra. Zero httpx directo. Errores en espanol automaticos via _translate_error_to_spanish().</done>
</task>

<task type="auto">
  <name>Task 2: Migrar conciliacion.py — eliminar httpx directo en 2 funciones</name>
  <files>backend/routers/conciliacion.py</files>
  <read_first>backend/routers/conciliacion.py (lineas 540-600 y 1180-1240 — las dos funciones con bypass), backend/alegra_service.py (lineas 201-267)</read_first>
  <action>
    Per ALEGRA-05: Migrar conciliacion.py de httpx directo a AlegraService.

    **Contexto:** conciliacion.py ya importa AlegraService en linea 706 para otros usos. El bypass esta en 2 funciones especificas que construyen httpx calls directos.

    **Cambio en imports:**
    - Si `ALEGRA_BASE_URL` se importa en linea 24, cambiar a solo `AlegraService` (o dejarlo si AlegraService ya esta importado en otra linea)
    - Verificar: `from alegra_service import AlegraService` debe existir. `ALEGRA_BASE_URL` no debe importarse.
    - Agregar `from database import db` si no existe

    **Funcion 1 — test-alegra-journals (linea ~559):**
    Esta funcion hace `GET {ALEGRA_BASE_URL}/journals?limit=1` con httpx.
    Reemplazar con:
    ```python
    alegra = AlegraService(db)
    result = await alegra.request("journals", "GET", params={"limit": 1})
    ```
    Adaptar el return para mantener la misma estructura.

    **Funcion 2 — backfill journals (linea ~1196):**
    Esta funcion hace `GET {ALEGRA_BASE_URL}/journals` con httpx y procesa la respuesta.
    Reemplazar el bloque httpx (lineas ~1185-1213) con:
    ```python
    alegra = AlegraService(db)
    journals_response = await alegra.request("journals", "GET")
    if isinstance(journals_response, list):
        journals_list = journals_response
    elif isinstance(journals_response, dict) and "results" in journals_response:
        journals_list = journals_response["results"]
    else:
        journals_list = []
    ```
    - ELIMINAR la construccion manual de headers, base64, creds_str, creds_b64
    - ELIMINAR la validacion de credenciales (AlegraService.request() ya lanza HTTPException si no hay credenciales)
    - Mantener TODA la logica de procesamiento de journals que sigue despues (filtrado por fecha, parsing, etc.)

    **REGLA:** No cambiar la logica de negocio de conciliacion. Solo la capa de transporte HTTP. Mantener logs existentes.
  </action>
  <verify>
    <automated>cd backend && grep -c "ALEGRA_BASE_URL" routers/conciliacion.py; grep -c "AlegraService" routers/conciliacion.py</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ALEGRA_BASE_URL" backend/routers/conciliacion.py retorna 0
    - grep "AlegraService" backend/routers/conciliacion.py retorna match (2+ veces — import + uso)
    - grep "alegra.request" backend/routers/conciliacion.py retorna match (en las 2 funciones migradas)
    - La logica de procesamiento de journals (filtrado por fecha, parsing entries) NO cambio
  </acceptance_criteria>
  <done>conciliacion.py usa AlegraService.request() para journals GET y POST. Zero bypass directo. Errores en espanol automaticos.</done>
</task>

<task type="auto">
  <name>Task 3: Migrar dian_service.py — eliminar httpx directo en 2 funciones</name>
  <files>backend/services/dian_service.py</files>
  <read_first>backend/services/dian_service.py (lineas 137-216 — _ya_existe_en_alegra y causar_factura_en_alegra), backend/alegra_service.py (lineas 201-314)</read_first>
  <action>
    Per ALEGRA-05: Migrar dian_service.py de httpx directo a AlegraService.

    **Cambio en imports (inicio del archivo):**
    - Cambiar `from alegra_service import ALEGRA_BASE_URL` a `from alegra_service import AlegraService`
    - Agregar `from database import db`
    - ELIMINAR las variables locales ALEGRA_EMAIL/ALEGRA_TOKEN de lineas 28-29 (ya no necesarias — AlegraService lee del entorno)
    - Mantener `import httpx` SOLO si se usa para DIAN API calls (no Alegra)

    **Funcion 1 — `_ya_existe_en_alegra()` (linea 137):**
    Reemplazar httpx GET con:
    ```python
    async def _ya_existe_en_alegra(numero_factura: str) -> bool:
        """Capa 3 — verifica si el bill ya existe en Alegra."""
        try:
            alegra = AlegraService(db)
            if await alegra.is_demo_mode():
                return False
            bills = await alegra.request("bills", "GET", params={"fields": "id,numberTemplate", "limit": 30})
            if not isinstance(bills, list):
                return False
            for b in bills:
                nt = b.get("numberTemplate", {})
                num = nt.get("number", "") if isinstance(nt, dict) else str(nt)
                if str(num) == str(numero_factura):
                    return True
        except Exception as e:
            logger.warning("Alegra bills check failed: %s", e)
        return False
    ```

    **Funcion 2 — `causar_factura_en_alegra()` (linea 179):**
    Reemplazar httpx POST con:
    ```python
    async def causar_factura_en_alegra(factura: dict) -> dict | None:
        """Crea el bill en Alegra via AlegraService.request_with_verify()."""
        try:
            alegra = AlegraService(db)
            if await alegra.is_demo_mode():
                logger.warning("DIAN: Alegra en modo demo — bill no creado")
                return None
            due_date = (date.fromisoformat(factura["fecha"]) + timedelta(days=30)).isoformat()
            payload = {
                "date": factura["fecha"],
                "dueDate": due_date,
                "provider": {
                    "identification": factura["nit_emisor"],
                    "name": factura["nombre_emisor"],
                },
                "numberTemplate": {"number": factura["numero_factura"]},
                "items": [{
                    "description": factura["descripcion"],
                    "price": factura["subtotal"],
                    "quantity": 1,
                }],
                "observations": (
                    f"Causada automaticamente desde DIAN. "
                    f"CUFE: {factura['cufe'][:16]}..."
                ),
            }
            result = await alegra.request_with_verify("bills", "POST", body=payload)
            return result
        except Exception as e:
            logger.error("DIAN causar_en_alegra error: %s", e)
            return None
    ```

    **NOTA CRITICA:** `causar_factura_en_alegra` ahora usa `request_with_verify()` en vez de `request()` simple. Esto agrega verificacion POST+GET automatica (per ALEGRA-03). La funcion original no verificaba.

    **REGLA:** No cambiar la logica de simulacion DIAN ni la estructura de proveedores. Solo la capa de transporte HTTP hacia Alegra.
  </action>
  <verify>
    <automated>cd backend && grep -c "ALEGRA_BASE_URL" services/dian_service.py; grep -c "AlegraService" services/dian_service.py; grep "request_with_verify" services/dian_service.py</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ALEGRA_BASE_URL" backend/services/dian_service.py retorna 0
    - grep "AlegraService" backend/services/dian_service.py retorna match
    - grep "request_with_verify" backend/services/dian_service.py retorna match (causar_factura_en_alegra usa verify)
    - grep "from alegra_service import AlegraService" backend/services/dian_service.py retorna match
    - Las variables locales ALEGRA_EMAIL y ALEGRA_TOKEN eliminadas del inicio del archivo
  </acceptance_criteria>
  <done>dian_service.py usa AlegraService para bills GET y POST. causar_factura_en_alegra() ahora usa request_with_verify(). Zero bypass directo.</done>
</task>

</tasks>

<verification>
- `grep -rn "ALEGRA_BASE_URL" backend/routers/auditoria.py backend/routers/conciliacion.py backend/services/dian_service.py` — 0 resultados
- `grep -rn "httpx.AsyncClient" backend/routers/auditoria.py backend/services/dian_service.py` — 0 resultados (conciliacion puede mantener httpx para otros usos no-Alegra)
- `cd backend && python -m pytest tests/test_alegra_service.py -v` — todos pasan (no rompimos nada)
- `grep -rn "app.alegra.com/api/r1" backend/` — 0 resultados en codigo ejecutable (COMMIT PROTOCOL)
</verification>

<success_criteria>
1. auditoria.py, conciliacion.py, dian_service.py usan AlegraService — no httpx directo a Alegra (per ALEGRA-05)
2. Ninguno de los 3 archivos importa ALEGRA_BASE_URL (per ALEGRA-02)
3. dian_service.py usa request_with_verify() para escritura (per ALEGRA-03)
4. Errores de Alegra en estos modulos se traducen automaticamente al espanol via AlegraService (per ALEGRA-04)
5. Tests de Plan 01 siguen pasando
</success_criteria>

<output>
After completion, create `.planning/phases/02-consolidacion-capa-alegra/02-02-SUMMARY.md`
</output>
