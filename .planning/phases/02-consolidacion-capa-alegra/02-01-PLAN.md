---
phase: 02-consolidacion-capa-alegra
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_alegra_service.py
  - backend/alegra_service.py
autonomous: true
requirements:
  - ALEGRA-06
  - ALEGRA-03
must_haves:
  truths:
    - "Tests cubren GET y POST para invoices, categories, payments, journals, contacts"
    - "Tests cubren errores HTTP 400, 403, 404, 429, 500 con mensajes en espanol"
    - "Mock de AlegraService rechaza journal-entries y solo acepta journals"
    - "request_with_verify() tiene test que confirma POST+GET obligatorio"
  artifacts:
    - path: "backend/tests/test_alegra_service.py"
      provides: "Test suite completa para AlegraService"
      min_lines: 200
    - path: "backend/alegra_service.py"
      provides: "Mock corregido — journal-entries eliminado"
      contains: "journals"
  key_links:
    - from: "backend/tests/test_alegra_service.py"
      to: "backend/alegra_service.py"
      via: "import AlegraService, ALEGRA_BASE_URL"
      pattern: "from alegra_service import"
---

<objective>
Crear la suite de tests para AlegraService que cubra los 5 endpoints principales (invoices, categories, payments, journals, contacts) con mocks HTTP, y corregir el bug del mock que acepta `journal-entries` ademas de `journals`.

Purpose: Esta suite es el safety net antes de migrar los 5 modulos bypass en planes 02 y 03. Sin tests, no hay forma de verificar que las migraciones no rompen nada.
Output: backend/tests/test_alegra_service.py con tests pasando + mock corregido en alegra_service.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ALEGRA-AUDIT.md
@backend/alegra_service.py
</context>

<interfaces>
<!-- Executor needs these contracts from alegra_service.py -->

```python
# backend/alegra_service.py — lineas clave
ALEGRA_BASE_URL = "https://api.alegra.com/api/v1"  # linea 16

class AlegraService:
    def __init__(self, db): ...
    async def request(self, endpoint: str, method: str = "GET", body: dict = None, params: dict = None): ...
    async def request_with_verify(self, endpoint: str, method: str, body: dict = None) -> dict: ...
    async def retry_request(self, endpoint: str, method: str, body: dict = None, max_retries: int = 3, delay_base: float = 2.0) -> dict: ...
    def _translate_error_to_spanish(self, status_code: int, error_data: dict, endpoint: str, method: str) -> str: ...
    async def is_demo_mode(self): ...
    async def get_auth_header(self): ...
    def _mock(self, endpoint: str, method: str, body: dict = None, params: dict = None): ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests para AlegraService — 5 endpoints + errores en espanol</name>
  <files>backend/tests/test_alegra_service.py</files>
  <read_first>backend/alegra_service.py (completo — entender request(), request_with_verify(), _translate_error_to_spanish(), _mock())</read_first>
  <behavior>
    - Test GET /invoices mock retorna lista de facturas (MOCK_INVOICES)
    - Test GET /categories mock retorna lista de cuentas (MOCK_ACCOUNTS)
    - Test GET /payments mock retorna datos (verificar que endpoint existe en mock)
    - Test GET /journals mock retorna lista de journals (MOCK_JOURNAL_ENTRIES)
    - Test POST /journals mock retorna dict con id
    - Test GET /contacts mock retorna lista de contactos (MOCK_CONTACTS)
    - Test request_with_verify POST+GET: mock POST retorna {id: "123"}, mock GET retorna {id: "123"} → resultado tiene _verificado=True
    - Test request_with_verify sin id: POST retorna {} → resultado sin _verificado
    - Test _translate_error_to_spanish(401, ...) contiene "Credenciales de Alegra incorrectas"
    - Test _translate_error_to_spanish(400, {message: "debit"}, "journals", "POST") contiene "balance"
    - Test _translate_error_to_spanish(403, ..., "journals", "POST") contiene "permisos"
    - Test _translate_error_to_spanish(404, ...) contiene "No encontrado"
    - Test _translate_error_to_spanish(429, ...) contiene "Limite de requests"
    - Test _translate_error_to_spanish(500, ...) contiene "no disponible temporalmente"
    - Test _mock("journals", "GET") retorna datos (journals funciona)
    - Test _mock("journal-entries", "GET") NO retorna datos de journals (bug fix — per ALEGRA-03)
  </behavior>
  <action>
    Crear backend/tests/test_alegra_service.py con pytest + unittest.mock.AsyncMock para mockear MongoDB.

    Estructura del test file:
    1. Fixture `alegra_svc` que crea AlegraService con db mock (AsyncMock). Configurar db mock para que `alegra_credentials.find_one` retorne {"email": "", "token": "", "is_demo_mode": True} (demo mode activo para usar _mock()).
    2. Seccion "Test Demo Mode Endpoints" — 6 tests para GET de cada endpoint en demo mode (invoices, categories=accounts, payments, journals, contacts, company). Usar `await alegra_svc.request("invoices", "GET")` y assert que retorna lista no vacia o dict no vacio.
    3. Seccion "Test POST con Mock" — Test POST /journals retorna dict con "id" key.
    4. Seccion "Test request_with_verify" — Mockear is_demo_mode() para retornar True, verificar que retorna dict con _verificado=True y _fuente="demo".
    5. Seccion "Test Traduccion Errores" — 6 tests directos a _translate_error_to_spanish() con assert en substring esperado.
    6. Test critico ALEGRA-03: `_mock("journal-entries", "GET")` debe retornar dict vacio `{}` (no datos de journals). Este test FALLARA hasta que se aplique el fix en Task 2.

    Usar `import pytest` y `@pytest.mark.asyncio` para tests async. Agregar `sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))` al inicio para imports.

    Per ALEGRA-06: Cada test tiene nombre descriptivo: `test_get_invoices_demo`, `test_get_categories_demo`, etc.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_alegra_service.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_" backend/tests/test_alegra_service.py retorna >= 14
    - grep "test_get_invoices_demo" backend/tests/test_alegra_service.py retorna match
    - grep "test_get_categories_demo" backend/tests/test_alegra_service.py retorna match
    - grep "test_get_journals_demo" backend/tests/test_alegra_service.py retorna match
    - grep "test_post_journals_demo" backend/tests/test_alegra_service.py retorna match
    - grep "test_get_contacts_demo" backend/tests/test_alegra_service.py retorna match
    - grep "test_translate_error_401" backend/tests/test_alegra_service.py retorna match
    - grep "test_mock_rejects_journal_entries" backend/tests/test_alegra_service.py retorna match
    - grep "request_with_verify" backend/tests/test_alegra_service.py retorna match
  </acceptance_criteria>
  <done>14+ tests escritos, todos pasan excepto test_mock_rejects_journal_entries (que falla — RED phase para Task 2)</done>
</task>

<task type="auto">
  <name>Task 2: Fix mock journal-entries y hacer pasar todos los tests</name>
  <files>backend/alegra_service.py</files>
  <read_first>backend/alegra_service.py lineas 418-428 (la funcion _mock, seccion de journals)</read_first>
  <action>
    Per ALEGRA-03 y HALLAZGO-04 de la auditoria:

    En backend/alegra_service.py linea 418, cambiar:
    ```python
    if "journal-entries" in endpoint or "journals" in endpoint:
    ```
    a:
    ```python
    if "journals" in endpoint:
    ```

    Esto elimina la aceptacion de `journal-entries` en el mock. En produccion, `/journal-entries` da error (403). El mock debe reflejar el comportamiento real.

    VERIFICAR despues del cambio: `_mock("journal-entries", "GET")` debe retornar `{}` (el fallback al final de _mock). `_mock("journals", "GET")` debe seguir retornando MOCK_JOURNAL_ENTRIES.

    NO tocar ninguna otra linea de alegra_service.py en esta tarea.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_alegra_service.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "journal-entries" backend/alegra_service.py NO debe aparecer en la linea del if de _mock (puede aparecer en MOCK_JOURNAL_ENTRIES import que es diferente)
    - grep 'if "journals" in endpoint' backend/alegra_service.py retorna match (linea ~418)
    - Todos los tests de test_alegra_service.py pasan (0 failures)
  </acceptance_criteria>
  <done>Mock corregido: solo acepta "journals", rechaza "journal-entries". Todos los tests pasan (GREEN).</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_alegra_service.py -v` — todos pasan
- `grep -c "journal-entries" backend/alegra_service.py` — solo aparece en import de MOCK_JOURNAL_ENTRIES, no en logica de _mock
- `grep -c "def test_" backend/tests/test_alegra_service.py` — >= 14
</verification>

<success_criteria>
1. Suite de tests cubre GET para invoices, categories, payments, journals, contacts (per ALEGRA-06)
2. Suite de tests cubre errores HTTP traducidos al espanol (per ALEGRA-04 verification)
3. Suite de tests cubre request_with_verify POST+GET (per ALEGRA-03)
4. Mock corregido: journal-entries rechazado (per ALEGRA-03 cleanup)
5. Todos los tests pasan
</success_criteria>

<output>
After completion, create `.planning/phases/02-consolidacion-capa-alegra/02-01-SUMMARY.md`
</output>
