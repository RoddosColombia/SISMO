---
phase: 01-contador-core
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/run_smoke_test.py
autonomous: false
requirements: [CONT-06]

must_haves:
  truths:
    - "El smoke test ejecuta los 20 pasos del ciclo contable completo contra IDs reales de Alegra"
    - "Cada paso del smoke test verifica HTTP 200 antes de reportar exito"
    - "El smoke test pasa 20/20 estableciendo el baseline de calidad pre-decomposicion"
  artifacts:
    - path: "backend/run_smoke_test.py"
      provides: "Smoke test 20/20 con verificacion HTTP 200"
      contains: "status_code"
  key_links:
    - from: "backend/run_smoke_test.py"
      to: "Alegra API"
      via: "HTTP requests con IDs reales"
      pattern: "requests\\.(get|post|put)"
---

<objective>
Establish the smoke test 20/20 baseline by verifying and hardening the existing run_smoke_test.py to ensure all 20 steps pass with real Alegra IDs, with HTTP 200 verification on every Alegra call.

Purpose: Per P-02, the smoke test must pass BEFORE decomposition (CONT-02) begins. This is the quality gate that protects all subsequent refactoring. CONT-06 was pulled from Phase 2 into Phase 1 by explicit user decision.
Output: Hardened smoke test that passes 20/20, establishing regression baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contador-core/01-CONTEXT.md
@.planning/phases/01-contador-core/01-RESEARCH.md
@.planning/phases/01-contador-core/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden smoke test with HTTP 200 verification and run 20/20 baseline (CONT-06)</name>
  <files>backend/run_smoke_test.py</files>
  <read_first>
    - backend/run_smoke_test.py (the file being modified — read ALL of it to understand current 20 test steps, their structure, assertions, and which Alegra IDs they use)
    - SMOKE_TEST_INSTRUCTIONS.md (instructions for the existing smoke test — understand the 20 steps of the complete accounting cycle)
    - backend/alegra_service.py (understand Alegra API endpoints used — confirm /journals not /journal-entries per R-02)
  </read_first>
  <action>
Per D-06 and D-06b from CONTEXT.md, and R-01 (NEVER report success without verifying HTTP 200):

1. **Read the existing smoke test** (`run_smoke_test.py`) completely to understand its 20 steps in 5 blocks. The test runs against `sismo-backend-40ca.onrender.com` (production).

2. **Audit every step for R-01 compliance:** For each HTTP call in the smoke test:
   - Verify that the response status code is checked explicitly with `assert response.status_code == 200` (or 201 for creation)
   - If any step logs success without checking status_code, add the check
   - If a step catches exceptions silently, make it fail explicitly

3. **Audit for R-02 compliance:** Search for any reference to `/journal-entries` in the smoke test. Replace with `/journals` if found. The correct Alegra endpoint is ALWAYS `/journals`.

4. **Audit for R-03 compliance:** If the smoke test uses hardcoded account IDs, add a comment noting they should come from `plan_cuentas_roddos`. If the test calls the backend API (not Alegra directly), the backend should handle this — just verify the test doesn't bypass the backend to call Alegra with hardcoded IDs.

5. **Add clear pass/fail reporting:** At the end of the test:
```python
print(f"\n{'='*50}")
print(f"SMOKE TEST RESULTS: {passed}/{total} passed")
if passed == total:
    print("STATUS: PASS - 20/20 baseline established")
else:
    print(f"STATUS: FAIL - {total - passed} tests failed")
    for name, error in failures:
        print(f"  FAILED: {name} - {error}")
print(f"{'='*50}")
```

6. **Add R-04 verification step** if not present: If any test step involves Auteco (NIT 860024781), verify that ReteFuente is NOT applied. If no Auteco step exists, add a comment at the top: `# R-04: Auteco NIT 860024781 = autoretenedor. Tests involving Auteco must verify no ReteFuente.`

7. **Do NOT modify the actual test logic** beyond adding status_code checks and reporting. The 20 steps must remain functionally identical — this is about hardening verification, not changing behavior.

Per Pitfall 4 in RESEARCH.md: The smoke test creates real data in Alegra. Note in a comment at the top of the file that running multiple times may create duplicate data unless idempotency (CONT-03, Plan 03) is active.
  </action>
  <verify>
    <automated>cd C:/Users/AndresSanJuan/roddos-workspace/SISMO && python -c "content = open('backend/run_smoke_test.py').read(); assert 'status_code' in content, 'No status_code checks found'; assert 'journal-entries' not in content, 'Found forbidden /journal-entries endpoint (R-02)'; print('OK: HTTP status checks present, no /journal-entries')"</automated>
  </verify>
  <acceptance_criteria>
    - run_smoke_test.py contains `status_code` checks (assert or if-check) for every HTTP call to Alegra
    - run_smoke_test.py does NOT contain the string `journal-entries` anywhere (per R-02)
    - run_smoke_test.py contains a summary block printing "SMOKE TEST RESULTS: {passed}/{total}"
    - run_smoke_test.py contains a comment about R-04 (Auteco autoretenedor)
    - run_smoke_test.py contains a comment warning about duplicate data on re-runs
  </acceptance_criteria>
  <done>Smoke test hardened with HTTP 200 verification on every Alegra call, R-02 compliance confirmed, clear pass/fail summary reporting added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run smoke test 20/20 and verify baseline</name>
  <files>backend/run_smoke_test.py</files>
  <action>Human runs the hardened smoke test against production and verifies 20/20 pass. This establishes the regression baseline that protects CONT-02 decomposition in Plans 03-04.</action>
  <what-built>Hardened smoke test with HTTP 200 verification on all 20 steps. The test runs against the production backend at sismo-backend-40ca.onrender.com with real Alegra IDs.</what-built>
  <how-to-verify>
    1. Run: `cd C:/Users/AndresSanJuan/roddos-workspace/SISMO && python backend/run_smoke_test.py`
    2. Verify output shows "SMOKE TEST RESULTS: 20/20 passed"
    3. If any tests fail, review the failure messages — they indicate which accounting cycle step is broken
    4. If failures relate to the proveedor fix (Plan 01), verify Plan 01 was deployed first
    5. The 20/20 result establishes the baseline that protects CONT-02 decomposition in Plan 04
  </how-to-verify>
  <verify>
    <automated>cd C:/Users/AndresSanJuan/roddos-workspace/SISMO && python backend/run_smoke_test.py 2>&1 | tail -5</automated>
  </verify>
  <done>Smoke test passes 20/20 against production with real Alegra IDs. Baseline established for decomposition.</done>
  <resume-signal>Type "20/20 passed" to confirm baseline, or describe which tests failed and why</resume-signal>
</task>

</tasks>

<verification>
- Every HTTP call in run_smoke_test.py has explicit status_code verification
- No reference to `/journal-entries` (R-02)
- Summary reporting shows pass/fail count
- Smoke test passes 20/20 against production (confirmed by human)
</verification>

<success_criteria>
- Smoke test runs 20/20 with all steps passing
- Every Alegra call verifies HTTP 200 (R-01)
- Baseline is established, unlocking CONT-02 decomposition
</success_criteria>

<output>
After completion, create `.planning/phases/01-contador-core/01-02-SUMMARY.md`
</output>
