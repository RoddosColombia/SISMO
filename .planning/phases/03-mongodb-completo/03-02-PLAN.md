---
phase: 03-mongodb-completo
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/server.py
  - backend/routers/loanbook.py
  - backend/routers/gastos.py
autonomous: true
requirements:
  - MDB-01
  - MDB-04
  - MDB-05

must_haves:
  truths:
    - "server.py startup only connects to MongoDB and starts schedulers — no index creation or seed data"
    - "loanbook.py no longer contains CATALOGO_DEFAULT constant — reads from MongoDB only"
    - "gastos.py no longer contains PLAN_CUENTAS_RODDOS constant — reads from MongoDB only"
    - "Backend still starts and runs correctly after cleanup"
  artifacts:
    - path: "backend/server.py"
      provides: "Clean startup without index creation"
    - path: "backend/routers/loanbook.py"
      provides: "Catalog read from MongoDB, no inline default"
    - path: "backend/routers/gastos.py"
      provides: "Plan de cuentas read from MongoDB, no inline default"
  key_links:
    - from: "backend/server.py"
      to: "init_mongodb_sismo.py"
      via: "init script runs before server starts"
      pattern: "No create_index in server.py startup"
    - from: "backend/routers/loanbook.py"
      to: "catalogo_planes collection"
      via: "MongoDB read"
      pattern: "db.catalogo_planes.find"
    - from: "backend/routers/gastos.py"
      to: "plan_cuentas_roddos collection"
      via: "MongoDB read"
      pattern: "db.plan_cuentas_roddos.find"
---

<objective>
Remove all index creation and seed data from server.py startup, loanbook.py, and gastos.py — init_mongodb_sismo.py is now the single source of truth (per D-01, D-04).

Purpose: Eliminate duplicated index creation and seed data scattered across server.py startup, loanbook.py CATALOGO_DEFAULT, and gastos.py PLAN_CUENTAS_RODDOS. After this, server.py startup only connects to MongoDB and starts schedulers.

Output: Clean server.py, loanbook.py, and gastos.py with no inline seed data or index creation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-mongodb-completo/03-CONTEXT.md
@.planning/phases/03-mongodb-completo/03-01-SUMMARY.md

Source files to modify:
@backend/server.py
@backend/routers/loanbook.py
@backend/routers/gastos.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove index creation and seed data from server.py startup</name>
  <files>backend/server.py</files>
  <read_first>
    - backend/server.py (full file — understand all startup logic)
    - .planning/phases/03-mongodb-completo/03-01-SUMMARY.md (what init script now handles)
  </read_first>
  <action>
    Edit server.py startup() function (lines 94-283) to REMOVE:

    1. **User seed block** (lines ~96-119): Remove the `if count == 0: insert_many(users)` block and the alegra_credentials seed. These are now in init_mongodb_sismo.py.

    2. **catalogo_motos seed** (lines ~131-169): Remove the `if not await db.catalogo_motos.find_one` block. Now in init script.

    3. **ALL create_index calls** (lines ~171-283): Remove every `await db.*.create_index(...)` call. This includes:
       - agent_memory indices (2)
       - audit_logs indices (2)
       - chat_messages index
       - inventario_motos indices (2)
       - catalogo_motos index
       - roddos_events index
       - loanbook indices (2)
       - conciliacion_* indices (6)
       - cxc_socios indices (2)
       - cxc_clientes indices (2)
       - ingresos_registrados index
       - agent_pending_topics indices (3, including TTL)
       - cfo_alertas indices (2)

    4. **proveedores_config seed** (lines ~199-213): Remove AUTECO seed. Now in init script.

    5. **IVA migration block** (lines ~215-231): Remove the bimestral-to-cuatrimestral migration. This is a one-time migration that should have run already. If needed, it belongs in init script.

    6. **plan_cuentas_roddos seed** (lines ~235-252): Remove the entire block that imports PLAN_CUENTAS_RODDOS from gastos and seeds/upserts. Now in init script.

    **KEEP in startup():**
    - `start_scheduler()` and `start_loanbook_scheduler()`
    - `app.state.event_bus = EventBusService(db)` (or equivalent bus init from Phase 2)
    - `await run_migration_v24(db)` if it exists and does more than index/seed work
    - Any other non-index, non-seed startup logic

    **Add a comment at top of startup():**
    ```python
    # MongoDB collections, indices, and seed data are managed by init_mongodb_sismo.py
    # Run: MONGO_URL="..." python init_mongodb_sismo.py
    # This startup only connects and initializes runtime services.
    ```

    Per D-01: server.py startup only connects to MongoDB — no index creation, no seeding.
  </action>
  <verify>
    <automated>cd "C:/Users/AndresSanJuan/roddos-workspace/SISMO/.claude/worktrees/nostalgic-boyd" && python -c "import ast; ast.parse(open('backend/server.py').read()); print('Syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "create_index" backend/server.py returns 0
    - grep -c "insert_many" backend/server.py startup section returns 0
    - grep -c "insert_one" backend/server.py startup section returns 0 (except webhook handler)
    - grep "init_mongodb_sismo" backend/server.py finds the comment referencing the init script
    - grep "start_scheduler\|start_loanbook_scheduler\|EventBusService" backend/server.py still finds scheduler/bus init
    - python -c "import ast; ast.parse(open('backend/server.py').read())" succeeds
  </acceptance_criteria>
  <done>
    server.py startup() contains zero create_index calls, zero seed inserts, and only initializes runtime services (schedulers, event bus). A comment references init_mongodb_sismo.py as the source of truth.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove CATALOGO_DEFAULT from loanbook.py and PLAN_CUENTAS_RODDOS from gastos.py</name>
  <files>backend/routers/loanbook.py, backend/routers/gastos.py</files>
  <read_first>
    - backend/routers/loanbook.py (full file — understand how CATALOGO_DEFAULT is used)
    - backend/routers/gastos.py (full file — understand how PLAN_CUENTAS_RODDOS is used)
  </read_first>
  <action>
    **loanbook.py changes (per D-04):**

    1. Remove the CATALOGO_DEFAULT list constant (lines 276-313).

    2. Update get_catalogo_planes() endpoint (line 318-332): Remove the `needs_seed` / upsert logic. The endpoint should now simply read from MongoDB:
       ```python
       @router.get("/catalogo-planes")
       async def get_catalogo_planes(current_user=Depends(get_current_user)):
           """Return plan catalog from MongoDB (seeded by init_mongodb_sismo.py)."""
           planes = await db.catalogo_planes.find({}, {"_id": 0}).to_list(20)
           if not planes:
               raise HTTPException(status_code=404, detail="Catalogo de planes no encontrado. Ejecute init_mongodb_sismo.py")
           return planes
       ```

    3. Check for ANY other references to CATALOGO_DEFAULT in loanbook.py and update them to read from db.catalogo_planes instead. If CATALOGO_DEFAULT is used elsewhere (e.g., in loan creation logic), replace with a db.catalogo_planes.find_one() call.

    **gastos.py changes (per D-04):**

    1. Remove the PLAN_CUENTAS_RODDOS list constant (lines 48-83).

    2. Check all usages of PLAN_CUENTAS_RODDOS in gastos.py. If any endpoint reads from this constant, replace with a MongoDB query:
       ```python
       plan_cuentas = await db.plan_cuentas_roddos.find({"activo": True}, {"_id": 0}).to_list(100)
       ```

    3. If PLAN_CUENTAS_RODDOS is imported by other files (server.py already removed in Task 1), ensure those imports are cleaned up too. Search for: `from routers.gastos import PLAN_CUENTAS_RODDOS` — should return zero results after cleanup.

    **IMPORTANT:** Do NOT break any existing endpoint functionality. The data is now in MongoDB (seeded by init_mongodb_sismo.py), so endpoints just read from the collection instead of the inline constant.
  </action>
  <verify>
    <automated>cd "C:/Users/AndresSanJuan/roddos-workspace/SISMO/.claude/worktrees/nostalgic-boyd" && python -c "import ast; ast.parse(open('backend/routers/loanbook.py').read()); ast.parse(open('backend/routers/gastos.py').read()); print('Syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep "CATALOGO_DEFAULT" backend/routers/loanbook.py returns 0 results
    - grep "PLAN_CUENTAS_RODDOS" backend/routers/gastos.py returns 0 results
    - grep -r "from routers.gastos import PLAN_CUENTAS_RODDOS" backend/ returns 0 results
    - grep -r "from backend.routers.gastos import PLAN_CUENTAS_RODDOS" backend/ returns 0 results
    - grep "catalogo_planes" backend/routers/loanbook.py finds MongoDB read (db.catalogo_planes.find)
    - grep "plan_cuentas_roddos" backend/routers/gastos.py finds MongoDB read (db.plan_cuentas_roddos.find)
    - python -c "import ast; ast.parse(open('backend/routers/loanbook.py').read()); ast.parse(open('backend/routers/gastos.py').read())" succeeds
  </acceptance_criteria>
  <done>
    CATALOGO_DEFAULT removed from loanbook.py, PLAN_CUENTAS_RODDOS removed from gastos.py. All endpoints read from MongoDB collections. No file in the codebase imports these constants anymore.
  </done>
</task>

</tasks>

<verification>
- server.py has zero create_index calls in startup
- server.py has zero seed data inserts in startup
- loanbook.py has no CATALOGO_DEFAULT constant
- gastos.py has no PLAN_CUENTAS_RODDOS constant
- No file imports PLAN_CUENTAS_RODDOS from gastos
- All three files parse without syntax errors
</verification>

<success_criteria>
server.py startup only connects and initializes runtime services. loanbook.py and gastos.py read seed data from MongoDB instead of inline constants. init_mongodb_sismo.py (Plan 01) is the single source of truth per D-01.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mongodb-completo/03-02-SUMMARY.md`
</output>
