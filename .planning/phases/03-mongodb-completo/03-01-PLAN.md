---
phase: 03-mongodb-completo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - init_mongodb_sismo.py
autonomous: true
requirements:
  - MDB-01
  - MDB-02
  - MDB-03
  - MDB-04
  - MDB-05
  - MDB-06
  - MDB-07
  - MDB-08
  - MDB-09

must_haves:
  truths:
    - "Running init_mongodb_sismo.py creates 30+ collections with correct indices"
    - "Running init_mongodb_sismo.py twice produces identical results with no errors"
    - "roddos_events has unique index on event_id, compound on (event_type, timestamp_utc), and 90-day TTL"
    - "catalogo_planes contains P39S/P52S/P78S/Contado with multipliers 1.0/2.2/4.4"
    - "plan_cuentas_roddos contains real account IDs, 5495 is absent, fallback is 5493"
    - "sismo_knowledge contains 10 business rules for RAG"
    - "portfolio_summaries and financial_reports collections exist with proper indices"
    - "roddos_events_dlq has indices for retry (next_retry, retry_count)"
    - "loanbook has ESR indices (estado+dpd+score, morosos partial, cola_cobranza partial, chasis unique)"
  artifacts:
    - path: "init_mongodb_sismo.py"
      provides: "Single source of truth for all MongoDB collections, indices, schema validation, and seed data"
      min_lines: 300
  key_links:
    - from: "init_mongodb_sismo.py"
      to: "MongoDB Atlas"
      via: "pymongo sync driver"
      pattern: "create_index|create_collection|update_one.*upsert"
---

<objective>
Rewrite init_mongodb_sismo.py as the single source of truth for ALL MongoDB collections, indices, schema validation, and seed data.

Purpose: Consolidate 30+ collections, ESR indices, schema validation rules, and all seed data (catalogo_planes, plan_cuentas_roddos, sismo_knowledge, users, cfo_config, proveedores_config, catalogo_motos) into one idempotent CLI script. This replaces scattered index creation in server.py and seed data in router files.

Output: Complete rewrite of init_mongodb_sismo.py that can be run repeatedly without errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mongodb-completo/03-CONTEXT.md

Source files to consolidate from:
@init_mongodb_sismo.py
@backend/server.py (lines 93-283 — startup index creation + seed data)
@backend/routers/loanbook.py (lines 274-313 — CATALOGO_DEFAULT)
@backend/routers/gastos.py (lines 48-83 — PLAN_CUENTAS_RODDOS)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite init_mongodb_sismo.py with all collections, indices, and seed data</name>
  <files>init_mongodb_sismo.py</files>
  <read_first>
    - init_mongodb_sismo.py (current script — understand structure)
    - backend/server.py lines 93-283 (all index creation to move here)
    - backend/routers/loanbook.py lines 274-313 (CATALOGO_DEFAULT to embed)
    - backend/routers/gastos.py lines 48-83 (PLAN_CUENTAS_RODDOS to embed)
    - .planning/phases/03-mongodb-completo/03-CONTEXT.md (all decisions D-01 through D-11)
  </read_first>
  <action>
    Rewrite init_mongodb_sismo.py completely. The script uses sync pymongo (per D-02), runs as standalone CLI, and is fully idempotent (per D-03).

    **Structure the script in clear sections:**

    1. **Connection setup** — same MONGO_URL/DB_NAME env var pattern, ping test, pymongo imports

    2. **COLLECTIONS dict** — define ALL 30+ collections. Consolidate from:
       - Current init script: users, chat_messages, loanbook, cartera_pagos, inventario_motos, cfo_informes, cfo_alertas, cfo_instrucciones, cfo_compromisos, cfo_chat_historia, audit_log, alegra_credentials
       - server.py startup: agent_memory, audit_logs, roddos_events, catalogo_motos, conciliacion_extractos_procesados, conciliacion_movimientos_procesados, conciliacion_reintentos, proveedores_config, iva_config, plan_cuentas_roddos, cxc_socios, cxc_clientes, ingresos_registrados, agent_pending_topics, cfo_alertas, cfo_config, catalogo_planes
       - New for Phase 2/3/4: roddos_events_dlq, portfolio_summaries, financial_reports, sismo_knowledge, notifications, user_settings

    3. **INDEXES dict** — define ALL indices per collection. Key indices (per D-08 through D-11):

       roddos_events (per D-08, MDB-02):
       - Unique index on event_id
       - Compound index on (event_type: 1, timestamp_utc: -1)
       - TTL index on timestamp_utc with expireAfterSeconds=7776000 (90 days)

       roddos_events_dlq (per D-09, MDB-09):
       - Index on (next_retry: 1)
       - Index on (retry_count: 1, status: 1)
       - Index on (status: 1)

       loanbook (per D-10, MDB-03):
       - Compound ESR: (estado: 1, dpd: 1, score_pago: -1)
       - Partial index: morosos — filter {dpd: {$gt: 0}} on (dpd: -1, score_pago: 1)
       - Partial index: cola_cobranza — filter {estado: "activo", dpd: {$gt: 7}} on (dpd: -1)
       - Unique sparse: chasis
       - Simple: (codigo: 1), (dpd_bucket: 1), (score_pago: 1)

       portfolio_summaries (per D-11, MDB-07):
       - Unique on (date: 1)
       - Index on (created_at: -1)

       financial_reports (per D-11, MDB-08):
       - Compound unique on (year: 1, month: 1)
       - Index on (created_at: -1)

       Plus ALL existing indices from server.py startup and current init script (agent_memory, audit_logs, chat_messages, inventario_motos, catalogo_motos, conciliacion_*, cxc_*, ingresos_registrados, agent_pending_topics, cfo_alertas, plan_cuentas_roddos).

    4. **create_collections_and_indexes()** function — iterates COLLECTIONS, calls create_index for each. Use create_index() which is idempotent by nature. For TTL and partial indices, use explicit name parameter to avoid conflicts.

    5. **SEED DATA** — all seed functions using upsert pattern (per D-03, D-04):

       a) catalogo_planes (per D-06, MDB-04): Embed CATALOGO_DEFAULT data directly — 4 plans (P39S, P52S, P78S, Contado). Each with multiplicadores: {semanal: 1.0, quincenal: 2.2, mensual: 4.4}. Upsert by plan name.

       b) plan_cuentas_roddos (per D-05, MDB-05): Embed PLAN_CUENTAS_RODDOS data directly — all entries from gastos.py EXCEPT filter out any entry with alegra_id == 5495. That means remove: Marketing/Publicidad (5495), Marketing/Eventos (5495), Otros/Representacion (5495). Keep all others. Upsert by (categoria, subcategoria). The fallback account is 5493 (Gastos generales) which already exists in the data.

       c) sismo_knowledge (per D-07, MDB-06): Seed 10 business rules. Upsert by rule_id. Rules:
          1. "mora_definicion" — DPD > 0 = mora. Mora diaria = $2,000 COP
          2. "mora_buckets" — Current: 0, 1-7, 8-30, 31-60, 61-90, 91+
          3. "retefuente_honorarios_pn" — 10% sobre pagos >= $1,533,000 COP (2025)
          4. "retefuente_honorarios_pj" — 11% sobre pagos >= $1,533,000 COP
          5. "autoretenedor_regla" — AUTECO KAWASAKI es autoretenedor, no aplicar ReteFuente
          6. "iva_cuatrimestral" — RODDOS declara IVA cuatrimestral (Ene-Abr, May-Ago, Sep-Dic)
          7. "ica_bogota" — Tarifa ICA Bogota: 11.04 por mil
          8. "cuenta_fallback" — Si no hay cuenta especifica, usar 5493 (Gastos generales)
          9. "loanbook_estados" — activo, mora, pagado, cancelado, restructurado
          10. "frecuencias_pago" — semanal (x1.0), quincenal (x2.2), mensual (x4.4)

       d) users seed: Same 2 default users (contabilidad@roddos.com admin, compras@roddos.com user). Upsert by email (not insert_many with count check). Use bcrypt with fallback to hashlib.

       e) cfo_config seed: Default config. Upsert by single-doc pattern (upsert with empty filter + $setOnInsert).

       f) proveedores_config seed: AUTECO KAWASAKI as autoretenedor. Upsert by NIT.

       g) catalogo_motos seed: Sport 100 and Raider 125. Upsert by modelo.

       h) alegra_credentials seed: Empty placeholder. Upsert with $setOnInsert.

    6. **main() function** — orchestrates: connect, create_collections_and_indexes, run all seed functions, print summary, close client.

    7. **if __name__ == "__main__"** — call main()

    **Important implementation details:**
    - Use create_index() not ensure_index() (deprecated)
    - For TTL indices, always pass name= parameter explicitly (e.g., name="ttl_timestamp_90d") to avoid MongoDB errors when TTL value changes
    - For partial indices, pass partialFilterExpression in kwargs
    - For unique indices, pass unique=True
    - For sparse indices, pass sparse=True
    - Print summary at end: "N collections, M indices created, K seed documents upserted"
    - All seed functions should return count of upserted docs for the summary
  </action>
  <verify>
    <automated>cd "C:/Users/AndresSanJuan/roddos-workspace/SISMO/.claude/worktrees/nostalgic-boyd" && python -c "import ast; ast.parse(open('init_mongodb_sismo.py').read()); print('Syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "create_index" init_mongodb_sismo.py returns >= 25 (30+ indices defined)
    - grep "event_id.*unique" init_mongodb_sismo.py finds roddos_events unique index on event_id
    - grep "expireAfterSeconds.*7776000" init_mongodb_sismo.py finds 90-day TTL
    - grep "multiplicadores" init_mongodb_sismo.py finds catalogo_planes seed with multipliers
    - grep "5495" init_mongodb_sismo.py returns 0 lines (ID 5495 excluded per D-05)
    - grep "5493" init_mongodb_sismo.py finds fallback account
    - grep "sismo_knowledge" init_mongodb_sismo.py finds knowledge base seed section
    - grep "portfolio_summaries" init_mongodb_sismo.py finds the collection
    - grep "financial_reports" init_mongodb_sismo.py finds the collection
    - grep "roddos_events_dlq" init_mongodb_sismo.py finds DLQ collection
    - grep "partialFilterExpression" init_mongodb_sismo.py finds partial indices for loanbook
    - python -c "import ast; ast.parse(open('init_mongodb_sismo.py').read())" succeeds
  </acceptance_criteria>
  <done>
    init_mongodb_sismo.py is a complete, syntactically valid script containing ALL 30+ collections, ALL indices (including ESR for loanbook, TTL for events, compound for DLQ), ALL seed data (catalogo_planes with 1.0/2.2/4.4, plan_cuentas without 5495, 10 sismo_knowledge rules), and is fully idempotent via create_index + upsert patterns.
  </done>
</task>

</tasks>

<verification>
- Script parses without syntax errors
- All 30+ collections are defined
- All index patterns from server.py are present
- Seed data for catalogo_planes, plan_cuentas, sismo_knowledge embedded
- ID 5495 absent from plan_cuentas data
- Idempotent patterns used (create_index, upsert)
</verification>

<success_criteria>
init_mongodb_sismo.py is a single, complete, idempotent CLI script that creates all 30+ MongoDB collections with ESR indices, schema validation, and seeds all production data including catalogo_planes, plan_cuentas_roddos, and sismo_knowledge.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mongodb-completo/03-01-SUMMARY.md`
</output>
