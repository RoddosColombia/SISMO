---
phase: 06-ingresos-cuotas-cartera
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - backend/routers/cartera.py
  - backend/tests/test_build23_f7_ingresos_cartera.py
autonomous: true
requirements:
  - CARTERA-02
  - CARTERA-03

must_haves:
  truths:
    - "Registrar el mismo loanbook_id+cuota_numero+fecha_pago dos veces retorna HTTP 409 con mensaje 'duplicado'"
    - "Ningun segundo journal creado en Alegra cuando se detecta duplicado (AlegraService nunca llamado)"
    - "CFO puede ver el monto_pago en portfolio_summaries.cartera despues de un pago registrado"
  artifacts:
    - path: "backend/routers/cartera.py"
      provides: "Guard anti-duplicado: consulta cartera_pagos antes de llamar a Alegra"
      contains: "cartera_pagos.find_one"
    - path: "backend/tests/test_build23_f7_ingresos_cartera.py"
      provides: "T7 GREEN + T8 portfolio_summaries"
      contains: "test_t7_duplicado_detectado"
      exports: []
  key_links:
    - from: "backend/routers/cartera.py"
      to: "db.cartera_pagos"
      via: "find_one antes de request_with_verify"
      pattern: "cartera_pagos\\.find_one"
    - from: "backend/services/cfo_agent.py"
      to: "db.cartera_pagos"
      via: "consolidar_datos_financieros lee cartera_pagos_mes"
      pattern: "cartera_pagos\\.find"
---

<objective>
Implementar el guard anti-duplicado en cartera.py (CARTERA-02: T7 pasa GREEN), luego verificar
que el recaudo de cartera_pagos es visible en portfolio_summaries para el CFO (CARTERA-03: T8 pasa).

Purpose: Garantizar que ningun pago puede registrarse dos veces en Alegra + confirmar que el CFO
ve los pagos en sus reportes financieros.
Output: T7 GREEN (anti-duplicado), T8 GREEN (portfolio_summaries wiring), suite T1-T8 completa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-ingresos-cuotas-cartera/06-01-SUMMARY.md

@backend/routers/cartera.py
@backend/tests/test_build23_f7_ingresos_cartera.py
@backend/services/cfo_agent.py
@backend/services/portfolio_pipeline.py
</context>

<interfaces>
<!-- Contratos clave que el executor necesita. Extraidos del codebase. -->

De backend/routers/cartera.py (endpoint existente, flujo critico):
```python
# FLUJO ACTUAL (sin anti-duplicado):
# 1. Validar campos
# 2. Find loanbook
# 3. obtener_cuenta_ingreso()  → ID de plan_ingresos_roddos
# 4. obtener_cuenta_bancaria() → ID de plan_cuentas_roddos
# 5. service.request_with_verify("journals", "POST", journal_payload)  ← ALEGRA
# 6. SOLO si _verificado=True: update loanbook + insert cartera_pagos + publish event

# DONDE INSERTAR EL GUARD (paso 2.5, despues de find loanbook, antes de Alegra):
existing_pago = await db.cartera_pagos.find_one({
    "loanbook_id": payload.loanbook_id.strip(),
    "cuota_numero": cuota_numero,
    "fecha_pago": fecha_pago,
})
if existing_pago:
    raise HTTPException(
        status_code=409,
        detail=f"Pago duplicado detectado: cuota #{cuota_numero} del loanbook {payload.loanbook_id} ya fue registrada el {fecha_pago} (journal: {existing_pago.get('alegra_journal_id', 'N/A')})"
    )
```

De backend/services/cfo_agent.py (lineas 163-171):
```python
# cartera_pagos_mes: pagos reales registrados en el mes
cartera_pagos_mes: list = []
try:
    cartera_pagos_mes = await db.cartera_pagos.find(
        {"fecha_pago": {"$gte": mes_inicio, "$lte": mes_fin}},
        {"_id": 0, "valor_pagado": 1, "monto": 1, "fecha_pago": 1},
    ).to_list(5000)
except Exception as e:
    logger.warning(f"[CFO] cartera_pagos: {e}")
```

NOTA: El query usa campo "monto" o "valor_pagado" pero cartera.py inserta "monto_pago".
Esto causa que CFO vea recaudo=0 aunque haya pagos reales.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar guard anti-duplicado en cartera.py (T7 GREEN)</name>
  <files>backend/routers/cartera.py</files>

  <read_first>
    - backend/routers/cartera.py — leer completo, identificar linea exacta despues del find loanbook
    - backend/tests/test_build23_f7_ingresos_cartera.py — ver exactamente que espera T7 (status 409, "duplicado")
  </read_first>

  <behavior>
    - Guard consulta db.cartera_pagos.find_one con filtro {loanbook_id, cuota_numero, fecha_pago}
    - Si existe pago previo: HTTPException 409 con detail que contiene "duplicado" (case-insensitive)
    - Si no existe: flujo normal continua (Alegra llamado, cuota marcada pagada, etc.)
    - Guard ejecuta ANTES de obtener_cuenta_ingreso, obtener_cuenta_bancaria, y AlegraService
    - Cuando guard dispara: AlegraService.request_with_verify NUNCA es llamado
  </behavior>

  <action>
En backend/routers/cartera.py, en la funcion `registrar_pago_cartera()`, despues del bloque
"FIND OLDEST PENDING CUOTA" (linea ~215, donde ya tenemos `cuota_numero` y `fecha_pago`),
agregar este bloque ANTES del comentario "GET INCOME ACCOUNT ID":

```python
# ── ANTI-DUPLICATE GUARD ──────────────────────────────────────────────────
# CARTERA-02: verificar si ya existe un pago para esta cuota en esta fecha
fecha_pago = payload.fecha_pago or datetime.now(timezone.utc).strftime("%Y-%m-%d")

existing_pago = await db.cartera_pagos.find_one({
    "loanbook_id": payload.loanbook_id.strip(),
    "cuota_numero": cuota_numero,
    "fecha_pago": fecha_pago,
})
if existing_pago:
    journal_prev = existing_pago.get("alegra_journal_id", "N/A")
    logger.warning(
        f"[F7] DUPLICADO DETECTADO: cuota #{cuota_numero} de {payload.loanbook_id} "
        f"ya registrada el {fecha_pago} (journal previo: {journal_prev})"
    )
    raise HTTPException(
        status_code=409,
        detail=(
            f"Pago duplicado detectado: cuota #{cuota_numero} del loanbook "
            f"{payload.loanbook_id} ya fue registrada el {fecha_pago}. "
            f"Journal Alegra previo: {journal_prev}"
        )
    )
```

NOTA: La variable `fecha_pago` ya existe en el codigo mas abajo (linea ~243). Mover esa
asignacion al guard y eliminar la duplicada mas abajo. Verificar que `fecha_pago` este
definido en el scope correcto antes del journal_payload dict.

Despues de este cambio, ejecutar:
```bash
cd backend && python -m pytest tests/test_build23_f7_ingresos_cartera.py::test_t7_duplicado_detectado -v
```
Debe pasar GREEN.

Luego ejecutar suite completa T1-T7:
```bash
cd backend && python -m pytest tests/test_build23_f7_ingresos_cartera.py -v --no-header 2>&1 | tail -15
```
T1-T7 deben estar PASSED.
  </action>

  <verify>
    <automated>cd backend && python -m pytest tests/test_build23_f7_ingresos_cartera.py::test_t7_duplicado_detectado tests/test_build23_f7_ingresos_cartera.py::test_t4_fallo_alegra_no_modifica_loanbook -v --no-header 2>&1 | tail -10</automated>
  </verify>

  <done>
    - T7 PASSED: HTTPException 409 con "duplicado" en detail
    - T4 PASSED: fallo de Alegra no modifica loanbook (regresion no rota)
    - grep "cartera_pagos.find_one" backend/routers/cartera.py retorna exactamente 1 resultado
    - grep "status_code=409" backend/routers/cartera.py retorna exactamente 1 resultado
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verificar y corregir wiring cartera_pagos → portfolio_summaries (T8 GREEN)</name>
  <files>
    backend/services/cfo_agent.py
    backend/tests/test_build23_f7_ingresos_cartera.py
  </files>

  <read_first>
    - backend/services/cfo_agent.py — lineas 163-171 (query cartera_pagos_mes) y lineas 413-430 (uso en analizar_pyg)
    - backend/services/portfolio_pipeline.py — ver como llama a consolidar_datos_financieros
    - backend/tests/test_build23_f7_ingresos_cartera.py — ver fixture existente, agregar T8 al final
  </read_first>

  <behavior>
    T8: dado un documento en cartera_pagos con {"loanbook_id": "LB-2026-0042", "cuota_numero": 1,
    "monto_pago": 192307.69, "fecha_pago": "2026-03-31"}, cuando consolidar_datos_financieros(db)
    es llamado, el resultado["cartera_pagos_mes"] contiene ese documento, y
    analizar_pyg(datos) retorna ingresos_reales_mes > 0.
  </behavior>

  <action>
**Paso 1 — Diagnosticar el mismatch de campos:**

El query en cfo_agent.py linea 167 proyecta: `{"_id": 0, "valor_pagado": 1, "monto": 1, "fecha_pago": 1}`
Pero cartera.py inserta el campo como `"monto_pago"` (no "monto" ni "valor_pagado").
Esto causa que analizar_pyg() calcule ingresos_reales_mes=0 aunque haya pagos reales.

Verificar en cfo_agent.py lineas 413-430 como se suma el recaudo:
```python
# Si el codigo dice algo como:
ingresos_reales = sum(p.get("monto", p.get("valor_pagado", 0)) for p in cartera_pagos)
# Agregar "monto_pago" al fallback:
ingresos_reales = sum(p.get("monto", p.get("valor_pagado", p.get("monto_pago", 0))) for p in cartera_pagos)
```

**Paso 2 — Fix en cfo_agent.py:**

Localizar la linea de proyeccion de la query (linea ~167):
```python
{"_id": 0, "valor_pagado": 1, "monto": 1, "fecha_pago": 1},
```
Cambiar a:
```python
{"_id": 0, "valor_pagado": 1, "monto": 1, "monto_pago": 1, "fecha_pago": 1},
```

Luego localizar donde se suman los pagos (buscar `ingresos_reales` en analizar_pyg).
Si el codigo hace `p.get("monto", p.get("valor_pagado", 0))`, cambiar a:
`p.get("monto_pago", p.get("monto", p.get("valor_pagado", 0)))`.

**Paso 3 — Agregar T8 a test_build23_f7_ingresos_cartera.py:**

Agregar antes de test_resumen_f7_todos_los_tests:

```python
@pytest.mark.asyncio
async def test_t8_cartera_pagos_visible_en_portfolio_summaries(mock_db, mock_user):
    """
    T8: CARTERA-03 — pago registrado en cartera_pagos es visible para el CFO
    via consolidar_datos_financieros() que alimenta portfolio_summaries.

    Esperado:
    - datos["cartera_pagos_mes"] incluye el pago
    - ingresos_reales del mes > 0
    """
    from services.cfo_agent import consolidar_datos_financieros

    hoy = "2026-03-31"

    # Simular un pago registrado en cartera_pagos
    pago_doc = {
        "id": "PAGO-JE-2026-005678",
        "loanbook_id": "LB-2026-0042",
        "cuota_numero": 1,
        "monto_pago": 192307.69,
        "fecha_pago": hoy,
        "alegra_journal_id": "JE-2026-005678",
    }

    # Mock de las colecciones que consolidar_datos_financieros necesita
    mock_db.loanbook = AsyncMock()
    mock_db.loanbook.find = MagicMock(return_value=AsyncMock())
    mock_db.loanbook.find.return_value.to_list = AsyncMock(return_value=[])

    mock_db.cartera_pagos = AsyncMock()
    mock_db.cartera_pagos.find = MagicMock(return_value=AsyncMock())
    mock_db.cartera_pagos.find.return_value.to_list = AsyncMock(return_value=[pago_doc])

    # Mock colecciones adicionales que consolidar_datos_financieros necesita
    for col in ["presupuesto", "catalogo_motos", "cfo_config", "alegra_settings",
                "bills", "transactions", "sismo_knowledge"]:
        coll_mock = AsyncMock()
        coll_mock.find_one = AsyncMock(return_value={})
        coll_mock.find = MagicMock(return_value=AsyncMock())
        coll_mock.find.return_value.to_list = AsyncMock(return_value=[])
        setattr(mock_db, col, coll_mock)

    with patch("services.cfo_agent.AlegraService"):
        datos = await consolidar_datos_financieros(mock_db)

    pagos_mes = datos.get("cartera_pagos_mes", [])
    assert len(pagos_mes) > 0, (
        "cartera_pagos_mes debe incluir el pago registrado. "
        "Verificar que cfo_agent.py proyecta campo 'monto_pago' en la query."
    )

    # El pago debe tener monto_pago accesible
    pago = pagos_mes[0]
    monto = pago.get("monto_pago", pago.get("monto", pago.get("valor_pagado", 0)))
    assert monto == 192307.69, (
        f"monto del pago debe ser 192307.69, got {monto}. "
        "cfo_agent.py debe proyectar 'monto_pago' en el query de cartera_pagos."
    )

    print(f"✅ T8 PASÓ: CFO ve recaudo ${monto:,.2f} en cartera_pagos_mes")
```
  </action>

  <verify>
    <automated>cd backend && python -m pytest tests/test_build23_f7_ingresos_cartera.py -v --no-header 2>&1 | tail -20</automated>
  </verify>

  <done>
    - T1-T8: PASSED (8/8 verde, 0 errores)
    - grep '"monto_pago"' backend/services/cfo_agent.py retorna resultado en la proyeccion del query
    - Suite completa: python -m pytest tests/test_build23_f7_ingresos_cartera.py retorna "8 passed"
  </done>
</task>

</tasks>

<verification>
Ejecutar suite completa y confirmar el commit protocol:

```bash
# Suite completa
cd backend && python -m pytest tests/test_build23_f7_ingresos_cartera.py -v --no-header 2>&1 | tail -20

# Commit protocol checks
grep -rn "app.alegra.com/api/r1" backend/ && echo "FAIL" || echo "OK"
grep -rn "journal-entries" backend/ && echo "FAIL" || echo "OK"
grep -n "status_code=409" backend/routers/cartera.py
grep -n "cartera_pagos.find_one" backend/routers/cartera.py
grep -n "monto_pago" backend/services/cfo_agent.py
```

Criterios de fase completada (CARTERA-01, CARTERA-02, CARTERA-03):
1. POST /cartera/registrar-pago con mocks → journal en Alegra HTTP 200 (T1 GREEN)
2. Mismo pago dos veces → HTTP 409 "duplicado", Alegra nunca llamado segunda vez (T7 GREEN)
3. cartera_pagos alimenta consolidar_datos_financieros → CFO ve recaudo (T8 GREEN)
</verification>

<success_criteria>
- 8 tests PASSED: T1-T8 en test_build23_f7_ingresos_cartera.py
- cartera.py tiene guard anti-duplicado: db.cartera_pagos.find_one ANTES de request_with_verify
- cfo_agent.py proyecta "monto_pago" en query de cartera_pagos
- Zero regresiones en suite existente
</success_criteria>

<output>
After completion, create `.planning/phases/06-ingresos-cuotas-cartera/06-02-SUMMARY.md`
</output>
