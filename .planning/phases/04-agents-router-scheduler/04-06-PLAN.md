---
phase: 04-agents-router-scheduler
plan: 06
type: execute
wave: 2
depends_on:
  - 04-05
files_modified:
  - backend/services/accounting_engine.py
  - backend/ai_chat.py
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
must_haves:
  truths:
    - "clasificar_gasto_chat('Pagamos arriendo $3.614.953') retorna tipo_gasto=arrendamiento, cuenta_debito=5480"
    - "clasificar_gasto_chat con NIT 860024781 retorna es_autoretenedor=True"
    - "clasificar_gasto_chat con CC 80075452 retorna es_socio=True, cuenta_debito=5329"
    - "crear_causacion llama request_with_verify() y retorna ID real del journal en Alegra"
    - "Mensaje de respuesta de crear_causacion incluye el ID del journal creado"
    - "ReteICA 0.414% se aplica automaticamente (aplica_reteica=True por defecto en Bogota)"
  artifacts:
    - path: "backend/services/accounting_engine.py"
      provides: "clasificar_gasto_chat() function"
      contains: "def clasificar_gasto_chat"
    - path: "backend/ai_chat.py"
      provides: "crear_causacion con request_with_verify + ID real en respuesta"
      contains: "request_with_verify"
  key_links:
    - from: "backend/ai_chat.py"
      to: "backend/services/accounting_engine.py"
      via: "import clasificar_gasto_chat"
      pattern: "from services.accounting_engine import clasificar_gasto_chat"
    - from: "backend/ai_chat.py"
      to: "backend/alegra_service.py"
      via: "request_with_verify for crear_causacion"
      pattern: "service\\.request_with_verify"
---

<objective>
Implement clasificar_gasto_chat() in accounting_engine.py and fix crear_causacion in ai_chat.py to use request_with_verify(). GREEN phase — make all 04-05 tests pass.

Purpose: Enable the chat agent to classify expenses using the existing accounting matrix, calculate retentions automatically, and create verified journals in Alegra with real IDs.

Output: Working clasificar_gasto_chat() function + fixed crear_causacion path
</objective>

<execution_context>
@C:\Users\AndresSanJuan\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\AndresSanJuan\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-agents-router-scheduler/04-05-SUMMARY.md

<interfaces>
<!-- Existing contracts from accounting_engine.py -->

Known Autoretenedor NITs:
```python
AUTORETENEDORES = {"860024781"}  # Auteco
```

Known Socios:
```python
SOCIOS_CC = {"80075452", "80086601"}  # Andres, Ivan
```

REGLAS_CLASIFICACION keys relevant to chat classification:
- "arriendo": palabras_clave=["arriendo", "arrendamiento", "alquiler", "calle 127"], cuenta_debito=5480
- "nomina": palabras_clave=["nomina", "pago nomina", "salario", "sueldo"], cuenta_debito=5462
- "servicios_publicos": palabras_clave=["luz", "energia", ...], cuenta_debito=5485
- "telecomunicaciones": palabras_clave=["internet", "telefono", ...], cuenta_debito=5487
- "tecnologia": proveedores=["claude", "anthropic", ...], cuenta_debito=5484
- "gasto_socio": proveedores=["andres sanjuan", "ivan echeverri", ...], cuenta_debito=5329
- "publicidad": palabras_clave=["pauta", "publicidad", ...], cuenta_debito=5495
- "cafeteria": palabras_clave=["cafeteria", "almuerzo", ...], cuenta_debito=5496
- "papeleria": palabras_clave=["papeleria", "utiles", ...], cuenta_debito=5497
- "combustibles": palabras_clave=["gasolina", "tanqueo", ...], cuenta_debito=5498
- "transporte": palabras_clave=["taxi", "uber", "rappi", ...], cuenta_debito=5499

Retention account IDs:
- 5381: Retencion honorarios 10%
- 5382: Retencion honorarios 11%
- 5383: Retencion servicios 4%
- 5386: Retencion arriendo 3.5%
- 5388: Retencion compra 2.5%
- 5392: RteICA 11.04

execute_chat_action lines to modify:
- Line 5345: `result = await service.request(endpoint, method, payload)` -> MUST become conditional: if action_type == "crear_causacion" use request_with_verify(), else keep request()
- Line 5372: `"message": "Ejecutado en Alegra exitosamente"` -> MUST include journal ID
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement clasificar_gasto_chat() in accounting_engine.py</name>
  <files>backend/services/accounting_engine.py</files>
  <read_first>
    - backend/services/accounting_engine.py (lines 1-20 for imports, 90-300 for CUENTAS_* and REGLAS_CLASIFICACION, 585-700 for clasificar_movimiento pattern, 1680-1790 for calcular_retenciones)
    - backend/tests/test_chat_transactional_phase4.py (full file — the tests we must make pass)
  </read_first>
  <behavior>
    - clasificar_gasto_chat("Pagamos arriendo $3.614.953", proveedor="Inmobiliaria", monto=3614953) -> {tipo_gasto: "arrendamiento", cuenta_debito: 5480, es_autoretenedor: False, es_socio: False, aplica_reteica: True}
    - clasificar_gasto_chat("Honorarios a Inversiones XYZ por $500.000", proveedor="Inversiones XYZ", nit="900123456", monto=500000) -> {tipo_gasto: "honorarios", cuenta_debito: 5476}
    - clasificar_gasto_chat("Compra a Auteco por $2.000.000", nit="860024781", monto=2000000) -> {es_autoretenedor: True}
    - clasificar_gasto_chat("Prestamo a socio Andres", proveedor="Andres", nit="80075452", monto=1000000) -> {es_socio: True, cuenta_debito: 5329}
  </behavior>
  <action>
    Add `clasificar_gasto_chat()` function to the END of accounting_engine.py (after the `formatear_retenciones_para_prompt` function, approximately after line 1800).

    The function signature:
    ```python
    # Constants for special cases
    AUTORETENEDORES_NIT = {"860024781"}  # Auteco
    SOCIOS_CC = {"80075452", "80086601"}  # Andres, Ivan

    def clasificar_gasto_chat(
        descripcion: str,
        proveedor: str = "",
        nit: str = "",
        monto: float = 0,
    ) -> dict:
    ```

    Implementation logic (reuses REGLAS_CLASIFICACION matrix, NOT heuristic):

    1. Normalize inputs: `desc_lower = descripcion.lower()`, `prov_lower = proveedor.lower()`
    2. Check special case — socio:
       - If nit in SOCIOS_CC OR any socio keyword in REGLAS_CLASIFICACION["gasto_socio"]["proveedores"] matches prov_lower
       - Return tipo_gasto="socio", cuenta_debito=5329, es_socio=True, aplica_reteica=False
    3. Check special case — autoretenedor:
       - `es_autoretenedor = nit in AUTORETENEDORES_NIT`
    4. Match tipo_gasto from description using REGLAS_CLASIFICACION keywords:
       - Check "arriendo" keywords -> tipo_gasto="arrendamiento", cuenta_debito=5480
       - Check for "honorario" keyword -> tipo_gasto="honorarios", cuenta_debito=5475 (PN) or 5476 (PJ, if NIT starts with 8 or 9 suggesting juridica, or "inversiones"/"ltda"/"sas"/"s.a" in proveedor)
       - Check "servicio" keywords (not servicios_publicos) -> tipo_gasto="servicios", cuenta_debito=5493
       - Check "compra"/"repuesto" keywords -> tipo_gasto="compras", cuenta_debito=5493
       - Iterate through remaining REGLAS_CLASIFICACION entries for other matches
       - Fallback: tipo_gasto="servicios", cuenta_debito=5493 (Gastos Generales, per CLAUDE.md: NUNCA 5495)
    5. Return dict with: tipo_gasto, cuenta_debito, cuenta_credito (5376 default), es_autoretenedor, es_socio, aplica_reteica (True unless es_socio), confianza, razon

    CRITICAL RULES (per CLAUDE.md):
    - Fallback account is ID 5493 (Gastos Generales), NEVER 5495
    - Auteco NIT 860024781 = AUTORETENEDOR, NEVER apply ReteFuente
    - Socios CC 80075452 / CC 80086601 = CXC socios (5329), NEVER gasto operativo
    - aplica_reteica = True for all gastos in Bogota (EXCEPT socio)
  </action>
  <verify>
    <automated>cd C:/Users/AndresSanJuan/roddos-workspace/SISMO/.claude/worktrees/nostalgic-boyd && python -m pytest backend/tests/test_chat_transactional_phase4.py -k "not test_v1" -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def clasificar_gasto_chat" backend/services/accounting_engine.py` returns 1 match
    - `grep "AUTORETENEDORES_NIT" backend/services/accounting_engine.py` returns matches
    - `grep "SOCIOS_CC" backend/services/accounting_engine.py` returns matches
    - `grep "860024781" backend/services/accounting_engine.py` returns matches
    - `grep "80075452" backend/services/accounting_engine.py` returns matches
    - Tests C1-C8 all PASS (GREEN phase)
    - `grep -rn "app.alegra.com/api/r1" backend/services/accounting_engine.py` returns 0 matches (COMMIT PROTOCOL)
  </acceptance_criteria>
  <done>clasificar_gasto_chat() exists, classifies 7+ expense types using REGLAS_CLASIFICACION matrix, detects Auteco autoretenedor and socios, returns structured dict. Tests C1-C8 pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix crear_causacion to use request_with_verify and return real journal ID</name>
  <files>backend/ai_chat.py</files>
  <read_first>
    - backend/ai_chat.py (lines 5218-5375 for crear_causacion handler and generic result handling)
    - backend/alegra_service.py (lines 274-314 for request_with_verify signature and return format)
    - backend/tests/test_chat_transactional_phase4.py (test_v1 specifically)
  </read_first>
  <action>
    Two targeted changes in backend/ai_chat.py:

    **Change 1 — Line ~5345:** Replace the generic `service.request()` call with conditional logic for crear_causacion:

    BEFORE (line 5345):
    ```python
    result = await service.request(endpoint, method, payload)
    ```

    AFTER:
    ```python
    # CHAT-04: crear_causacion MUST use request_with_verify (POST + GET verification)
    if action_type == "crear_causacion":
        result = await service.request_with_verify(endpoint, method, payload)
    else:
        result = await service.request(endpoint, method, payload)
    ```

    **Change 2 — Line ~5368-5374:** Include the journal ID in the success message for crear_causacion:

    BEFORE:
    ```python
    return {
        "success": True,
        "result": result,
        "id": doc_id,
        "message": "Ejecutado en Alegra exitosamente",
        "sync": sync_result,
    }
    ```

    AFTER:
    ```python
    # Build message with real ID for traceability (CHAT-04: nunca simular exito)
    if action_type == "crear_causacion" and doc_id:
        msg = f"Journal {doc_id} creado y verificado en Alegra"
        verificado = result.get("_verificado", False) if isinstance(result, dict) else False
        if verificado:
            msg += " (verificacion HTTP 200 OK)"
    else:
        msg = "Ejecutado en Alegra exitosamente"

    return {
        "success": True,
        "result": result,
        "id": doc_id,
        "message": msg,
        "sync": sync_result,
    }
    ```

    CRITICAL: Do NOT touch any other lines in ai_chat.py. This is a surgical fix on exactly 2 spots.
    CRITICAL: Do NOT use journal-entries endpoint (per CLAUDE.md — gives 403). The endpoint stays "journals".
    CRITICAL: Run COMMIT PROTOCOL greps before declaring done.
  </action>
  <verify>
    <automated>cd C:/Users/AndresSanJuan/roddos-workspace/SISMO/.claude/worktrees/nostalgic-boyd && python -m pytest backend/tests/test_chat_transactional_phase4.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "request_with_verify" backend/ai_chat.py` shows the new line in crear_causacion handler
    - `grep -n "Journal.*creado.*verificado" backend/ai_chat.py` returns 1 match
    - `grep -rn "app.alegra.com/api/r1" backend/ai_chat.py` returns 0 matches (COMMIT PROTOCOL)
    - `grep -rn "journal-entries" backend/ai_chat.py` returns 0 matches (COMMIT PROTOCOL — only allowed in comments/documentation, never as endpoint)
    - ALL tests in test_chat_transactional_phase4.py PASS including test_v1
    - Existing tests in test_build23_f2_chat_transactional.py still pass (no regression)
  </acceptance_criteria>
  <done>crear_causacion uses request_with_verify() for POST+GET verification. Success message includes real journal ID. All Phase 4 tests pass. No regressions in existing test suites.</done>
</task>

</tasks>

<verification>
- ALL tests in test_chat_transactional_phase4.py pass (GREEN phase)
- Existing tests in test_build23_f2_chat_transactional.py still pass
- COMMIT PROTOCOL: `grep -rn "app.alegra.com/api/r1" backend/` returns 0
- COMMIT PROTOCOL: `grep -rn "journal-entries" backend/ai_chat.py` returns 0 (only in comments)
- clasificar_gasto_chat() uses REGLAS_CLASIFICACION matrix (CHAT-01: no heuristica manual)
- Auteco NIT 860024781 detected as autoretenedor (CHAT-05)
- Socios CC 80075452/80086601 -> CXC 5329, never gasto operativo (CHAT-05)
- crear_causacion uses request_with_verify, returns real ID (CHAT-04)
</verification>

<success_criteria>
- clasificar_gasto_chat() classifies arriendo, honorarios, servicios, compras, transporte, socio correctly
- Auteco NIT returns es_autoretenedor=True
- Socios CC return es_socio=True, cuenta_debito=5329
- crear_causacion uses request_with_verify() (not request())
- Success message includes journal ID
- All 9+ tests pass
- No regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-agents-router-scheduler/04-06-SUMMARY.md`
</output>
