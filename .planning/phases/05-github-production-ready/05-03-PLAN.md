---
phase: 05-github-production-ready
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - GIT-05
  - GIT-06

must_haves:
  truths:
    - "README.md describes SISMO as orquestador de agentes IA for RODDOS fintech de movilidad sostenible"
    - "README.md contains no references to Emergent, BUILD 18, concesionario, Auteco, or React 18"
    - "README.md has 4 required sections: identity, stack, agents, setup"
    - "CLAUDE.md documents bus.emit() protocol and worktrees workflow"
  artifacts:
    - path: "README.md"
      provides: "BUILD 24 project documentation"
      contains: "BUILD 24"
    - path: "CLAUDE.md"
      provides: "Updated developer instructions with bus protocol"
      contains: "bus.emit"
  key_links: []
---

<objective>
Full rewrite of README.md to reflect BUILD 24 architecture and update CLAUDE.md with new bus protocol, worktrees workflow, and known errors.

Purpose: Documentation accurately represents the current system for contributors and CI checks.
Output: README.md (full rewrite) + CLAUDE.md (targeted additions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-github-production-ready/05-CONTEXT.md
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full rewrite of README.md for BUILD 24 (per D-07, D-08, GIT-05)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content — all wrong identity references to find and eliminate)
    - .planning/PROJECT.md (accurate project identity and description)
    - .planning/phases/05-github-production-ready/05-CONTEXT.md (D-07 and D-08 requirements)
    - CLAUDE.md (Technology Stack section for accurate stack info)
  </read_first>
  <action>
    FULL REWRITE of README.md (per D-07 — not surgical cleanup). Delete all current content and write from scratch.

    **Forbidden strings** (per D-07, CONTEXT specifics): "Emergent", "BUILD 18", "concesionario", "Auteco", "React 18", "emergent.sh", "EMERGENT_LLM_KEY", "Emergent LLM Key", "Agente Contador con IA integrado con Alegra ERP", "RODDOS Contable IA".

    **Required sections (per D-08):**

    **Section 1: Que es SISMO / RODDOS** (per D-08.1):
    - Title: `# SISMO — Sistema Inteligente de Soporte y Monitoreo Operativo`
    - SISMO = plataforma de orquestacion de agentes IA especializados para RODDOS S.A.S.
    - RODDOS = fintech de movilidad sostenible en Bogota, Colombia
    - Financia motocicletas con cobro 100% remoto (WhatsApp + transferencias)
    - Equipo pequeno (2-5 personas) gestiona 10 loanbooks activos, cartera de $94M COP, 34 motos TVS con VINs reales
    - Principio rector: Soberania Digital — ninguna plataforma de terceros es duena del corazon operativo
    - Current build: BUILD 24 — Cimientos Definitivos

    **Section 2: Stack BUILD 24** (per D-08.2):
    Table with accurate values:
    | Capa | Tecnologia |
    |------|-----------|
    | Frontend | React 19 + TypeScript + Tailwind CSS + Shadcn/UI |
    | Backend | FastAPI (Python 3.11) + Motor (async MongoDB) |
    | Base de datos | MongoDB Atlas |
    | IA | Claude Sonnet via Anthropic SDK |
    | Contabilidad | Alegra API v1 (sistema contable de record) |
    | WhatsApp | Mercately |
    | Hosting | Render.com |
    | CI/CD | GitHub Actions (pytest + smoke test) |

    **Section 3: Los 4 agentes core** (per D-08.3):
    Brief description of each:
    - **Contador** — Automatiza contabilidad en Alegra: facturas, journals, conciliacion bancaria, clasificacion de movimientos con confianza.
    - **CFO** — Analisis financiero estrategico: semaforo (caja, cartera, ventas, roll rate, impuestos), PyG mensual, plan de deudas, proyecciones.
    - **RADAR** — Cobranza inteligente via WhatsApp: cola semanal, gestiones, PTPs, recordatorios automaticos, alertas de mora.
    - **Loanbook** — Ciclo completo de credito: solicitud, entrega, plan de cuotas (semanal/quincenal/mensual), registro de pagos, estados de cartera.

    **Section 4: Como correr el proyecto** (per D-08.4):
    ```
    ## Como correr el proyecto

    ### Requisitos
    - Node.js 18+ y Yarn
    - Python 3.11+
    - MongoDB Atlas (o MongoDB local 6.0+)
    - Cuenta Alegra con token API
    - ANTHROPIC_API_KEY (Claude Sonnet)

    ### Setup local

    # Backend
    cd backend
    cp .env.example .env  # Editar con valores reales
    pip install -r requirements.txt
    uvicorn server:app --host 0.0.0.0 --port 8001 --reload

    # Frontend (en otra terminal)
    cd frontend
    yarn install
    yarn start
    ```

    **Variables de entorno obligatorias** (backend/.env):
    ```
    MONGO_URL=mongodb+srv://...
    DB_NAME=sismo
    ALEGRA_EMAIL=email@empresa.com
    ALEGRA_TOKEN=tu-token-alegra
    ALEGRA_WEBHOOK_SECRET=roddos-webhook-2026
    JWT_SECRET=secreto-largo-seguro
    ANTHROPIC_API_KEY=sk-ant-...
    APP_URL=https://tu-dominio.com
    CORS_ORIGINS=http://localhost:3000
    ```

    Frontend: `REACT_APP_BACKEND_URL=http://localhost:8001`

    **Section 5: Licencia** (keep existing):
    `Uso interno — RODDOS S.A.S. — Bogota D.C., Colombia.`

    DO NOT include: Build history table, Mercately setup instructions, conciliacion bancaria details, endpoint lists, MongoDB collections list. Keep README concise (~80-120 lines). The old README was 433 lines — the new one should be focused.
  </action>
  <verify>
    <automated>python3 -c "
import re
content = open('README.md').read()
forbidden = ['Emergent', 'BUILD 18', 'concesionario', 'Auteco', 'React 18']
for word in forbidden:
    assert word not in content, f'README still contains: {word}'
required = ['BUILD 24', 'SISMO', 'RODDOS', 'Soberania Digital', 'React 19', 'Claude Sonnet', 'Anthropic', 'Contador', 'CFO', 'RADAR', 'Loanbook']
for word in required:
    assert word in content, f'README missing: {word}'
print('README.md validation passed')
" 2>/dev/null || python -c "
import re
content = open('README.md').read()
forbidden = ['Emergent', 'BUILD 18', 'concesionario', 'Auteco', 'React 18']
for word in forbidden:
    assert word not in content, f'README still contains: {word}'
required = ['BUILD 24', 'SISMO', 'RODDOS', 'Soberania Digital', 'React 19', 'Claude Sonnet', 'Anthropic', 'Contador', 'CFO', 'RADAR', 'Loanbook']
for word in required:
    assert word in content, f'README missing: {word}'
print('README.md validation passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - README.md does NOT contain "Emergent"
    - README.md does NOT contain "BUILD 18"
    - README.md does NOT contain "concesionario"
    - README.md does NOT contain "Auteco"
    - README.md does NOT contain "React 18"
    - README.md contains "SISMO" in the title
    - README.md contains "BUILD 24"
    - README.md contains "React 19" in stack table
    - README.md contains "Claude Sonnet" (not "Emergent LLM Key")
    - README.md contains "Anthropic" (SDK reference)
    - README.md contains "Soberania Digital"
    - README.md contains descriptions of all 4 agents: Contador, CFO, RADAR, Loanbook
    - README.md contains "ANTHROPIC_API_KEY" in env vars section
    - README.md is under 150 lines (concise)
  </acceptance_criteria>
  <done>README.md fully rewritten for BUILD 24 with correct identity, stack, agents, and setup instructions — no forbidden references</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with bus protocol, worktrees, and known errors (per D-09, GIT-06)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current content — large file, focus on end sections)
    - .planning/phases/05-github-production-ready/05-CONTEXT.md (D-09 requirements)
    - backend/services/event_bus_service.py (bus.emit API signature for documentation)
  </read_first>
  <action>
    ADD new sections to CLAUDE.md (per D-09, GIT-06). Do NOT rewrite existing content — append at the end, before any final blank lines.

    **Section 1: Event Bus Protocol (BUILD 24)**
    ```markdown
    ## Event Bus Protocol (BUILD 24)

    Use `bus.emit()` for ALL event publishing. The old patterns are DELETED:
    - `emit_event()` — REMOVED, do not use
    - `emit_state_change()` — REMOVED from shared_state.py
    - `from backend.event_bus import` — REMOVED, file deleted

    Correct pattern:
    ```python
    from services.event_bus_service import EventBusService

    bus = EventBusService(db)
    await bus.emit(
        event_type="payment_received",   # Must be in EVENT_TYPES catalog
        agent="loanbook",                # Agent name
        payload={"loanbook_id": "...", "amount": 150000},
        source="routers/loanbook.py"
    )
    ```

    Key rules:
    - event_type MUST be one of the 28 types in `backend/models/event_models.py` EVENT_TYPES
    - Failed emits go to DLQ (roddos_events_dlq), never block the caller
    - Bus health: GET /api/health/bus returns dlq_pending, events_last_hour, status
    ```

    **Section 2: Worktrees Workflow**
    ```markdown
    ## Worktrees Workflow

    This project uses git worktrees for parallel development:
    ```bash
    # Create a worktree for a feature branch
    git worktree add .claude/worktrees/<branch-name> -b <branch-name>

    # Work in the worktree
    cd .claude/worktrees/<branch-name>

    # Clean up after merge
    git worktree remove .claude/worktrees/<branch-name>
    ```

    Each worktree has its own working directory. Changes in one worktree do not affect others.
    ```

    **Section 3: Known Errors and Solutions**
    ```markdown
    ## Known Errors and Solutions

    ### Motor AsyncIOMotorClient import
    Error: `cannot import name 'AsyncIOMotorClient'`
    Fix: Use `from motor.motor_asyncio import AsyncIOMotorClient` (not `from motor import`)

    ### pytest-asyncio deprecation warning
    Warning: `PytestUnraisableExceptionWarning`
    Fix: Add `asyncio_mode = "auto"` in pytest.ini or use `@pytest.mark.asyncio` on each test

    ### MongoDB index creation on existing collection
    Error: `IndexOptionsConflict` when running init script twice
    Fix: Use `create_index()` with `background=True` — it's idempotent (unlike `createIndex` with different options)

    ### Pydantic V2 model_dump vs dict
    Error: `AttributeError: 'RoddosEvent' object has no attribute 'dict'`
    Fix: Use `.model_dump()` instead of `.dict()` (Pydantic V2)
    ```

    These three sections should be appended at the end of the existing CLAUDE.md content.
  </action>
  <verify>
    <automated>python3 -c "
content = open('CLAUDE.md').read()
checks = ['bus.emit', 'emit_event', 'emit_state_change', 'Worktrees Workflow', 'Known Errors', 'EventBusService']
for c in checks:
    assert c in content, f'CLAUDE.md missing: {c}'
print('CLAUDE.md validation passed')
" 2>/dev/null || python -c "
content = open('CLAUDE.md').read()
checks = ['bus.emit', 'emit_event', 'emit_state_change', 'Worktrees Workflow', 'Known Errors', 'EventBusService']
for c in checks:
    assert c in content, f'CLAUDE.md missing: {c}'
print('CLAUDE.md validation passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "## Event Bus Protocol (BUILD 24)" section header
    - CLAUDE.md contains "bus.emit()" as the correct pattern
    - CLAUDE.md contains "emit_event() — REMOVED" or equivalent deprecation notice
    - CLAUDE.md contains "emit_state_change() — REMOVED"
    - CLAUDE.md contains "EventBusService" import example
    - CLAUDE.md contains "## Worktrees Workflow" section header
    - CLAUDE.md contains "git worktree" command examples
    - CLAUDE.md contains "## Known Errors and Solutions" section header
    - CLAUDE.md contains "AsyncIOMotorClient" error documentation
    - CLAUDE.md contains "model_dump" Pydantic V2 fix
    - All existing CLAUDE.md content is preserved (file grew, did not shrink)
  </acceptance_criteria>
  <done>CLAUDE.md has 3 new sections documenting bus protocol, worktrees workflow, and known errors for BUILD 24</done>
</task>

</tasks>

<verification>
- README.md contains no forbidden strings (Emergent, BUILD 18, concesionario, Auteco, React 18)
- README.md contains all 4 required sections (identity, stack, agents, setup)
- CLAUDE.md contains bus.emit protocol, worktrees, and known errors sections
- Both files pass their validation scripts
</verification>

<success_criteria>
- README.md is a complete BUILD 24 document with correct identity, stack, agents, and setup
- CLAUDE.md documents the new bus protocol, worktrees workflow, and common error solutions
- No references to deprecated patterns (Emergent, emit_event, BUILD 18)
</success_criteria>

<output>
After completion, create `.planning/phases/05-github-production-ready/05-03-SUMMARY.md`
</output>
