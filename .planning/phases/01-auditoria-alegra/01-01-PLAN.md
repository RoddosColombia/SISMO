---
phase: 01-auditoria-alegra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-auditoria-alegra/ALEGRA-CODE-AUDIT.md
autonomous: true
requirements: [AUDIT-01, AUDIT-03, AUDIT-04]

must_haves:
  truths:
    - "El equipo sabe que backend/alegra_service.py es la unica fuente de verdad para llamadas Alegra — no existe utils/alegra.py"
    - "Cada modulo que importa alegra_service esta documentado con su import exacto"
    - "request_with_verify() usa ALEGRA_BASE_URL = https://api.alegra.com/api/v1 — confirmado con grep"
    - "Todas las variantes de URL incorrectas (app.alegra.com/api/r1) estan identificadas con archivo y linea"
    - "ACTION_MAP tiene 12 acciones registradas y las 5 acciones de lectura faltantes estan listadas"
  artifacts:
    - path: ".planning/phases/01-auditoria-alegra/ALEGRA-CODE-AUDIT.md"
      provides: "Auditoria estatica del codigo Alegra"
      contains: "## Arquitectura: Fuente de Verdad"
  key_links:
    - from: "backend/alegra_service.py"
      to: "ALEGRA_BASE_URL"
      via: "constant at line 16"
      pattern: "ALEGRA_BASE_URL.*api\\.alegra\\.com/api/v1"
---

<objective>
Auditar estaticamente todo el codigo que interactua con Alegra API para documentar la arquitectura actual, confirmar URLs, e inventariar ACTION_MAP.

Purpose: Establecer la linea base de que funciona, que esta roto, y que falta — sin hacer requests HTTP reales.
Output: ALEGRA-CODE-AUDIT.md con hallazgos de arquitectura, URLs, imports, y ACTION_MAP completo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/alegra_service.py
@backend/ai_chat.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar arquitectura Alegra y confirmar URLs</name>
  <files>.planning/phases/01-auditoria-alegra/ALEGRA-CODE-AUDIT.md</files>
  <read_first>
    - backend/alegra_service.py (completo — 492 lineas, la fuente de verdad)
    - backend/ai_chat.py lineas 3870-3883 (ACTION_MAP)
  </read_first>
  <action>
Ejecutar los siguientes comandos grep para recopilar evidencia y documentar en ALEGRA-CODE-AUDIT.md:

1. **Fuente de verdad:** Confirmar que NO existe `utils/alegra.py` ni `services/alegra_service.py`:
   - `find backend/ -name "alegra*.py" -type f` — debe retornar SOLO `backend/alegra_service.py`, `backend/routers/alegra.py`, `backend/routers/alegra_webhooks.py`
   - Documentar que la clase `AlegraService` vive en `backend/alegra_service.py` (no en services/)

2. **Mapa de imports:** Ejecutar `grep -rn "from alegra_service import\|import alegra_service" backend/` para listar TODOS los modulos que importan alegra_service. Documentar cada uno con:
   - Archivo exacto
   - Que importa (AlegraService, ALEGRA_BASE_URL, o ambos)
   - Si usa `request()`, `request_with_verify()`, o acceso directo a `ALEGRA_BASE_URL`

3. **Verificacion URL correcta:** Ejecutar `grep -rn "ALEGRA_BASE_URL\|api\.alegra\.com\|app\.alegra\.com" backend/` para confirmar:
   - Linea 16 de alegra_service.py: `ALEGRA_BASE_URL = "https://api.alegra.com/api/v1"` (CORRECTA)
   - Todos los usos de ALEGRA_BASE_URL pasan por esta constante
   - Identificar CUALQUIER referencia a `app.alegra.com/api/r1` (URL INCORRECTA prohibida)

4. **Verificacion request_with_verify():** Leer lineas 274-314 de alegra_service.py y confirmar:
   - Usa `self.request()` internamente (que a su vez usa `ALEGRA_BASE_URL`)
   - Hace POST + GET de verificacion
   - Retorna `_verificado: True` solo con HTTP 200

5. **Inventario ACTION_MAP:** Leer lineas 3870-3883 de ai_chat.py y documentar las 12 acciones:
   ```
   crear_factura_venta        -> POST /invoices
   registrar_factura_compra   -> POST /bills
   crear_causacion            -> POST /journals
   registrar_pago             -> POST /payments
   registrar_pago_cartera     -> POST /cartera/registrar-pago (interno)
   registrar_nomina           -> POST /nomina/registrar (interno)
   registrar_abono_socio      -> POST /cxc/socios/abono (interno)
   consultar_saldo_socio      -> GET /cxc/socios/saldo (interno)
   registrar_ingreso_no_operacional -> POST /ingresos/no-operacional (interno)
   crear_contacto             -> POST /contacts
   crear_nota_credito         -> POST /credit-notes
   crear_nota_debito          -> POST /debit-notes
   ```
   Clasificar cada accion: Alegra-directa vs endpoint-interno.

   Listar las 5 acciones de LECTURA faltantes:
   - `consultar_facturas` (GET /invoices) — NO EXISTE en ACTION_MAP
   - `consultar_categorias` (GET /categories) — NO EXISTE
   - `consultar_pagos` (GET /payments) — NO EXISTE
   - `consultar_journals` (GET /journals) — NO EXISTE
   - `consultar_contactos` (GET /contacts) — NO EXISTE

6. **Acciones especiales fuera de ACTION_MAP:** Documentar `diagnosticar_contabilidad` (lineas 3886-3934) y `guardar_pendiente` (linea 3937+) que se manejan como casos especiales.

Escribir ALEGRA-CODE-AUDIT.md con estas secciones:
- `## 1. Arquitectura: Fuente de Verdad`
- `## 2. Mapa de Imports`
- `## 3. Verificacion de URLs`
- `## 4. Analisis request_with_verify()`
- `## 5. Inventario ACTION_MAP`
- `## 6. Acciones Faltantes`
- `## 7. Hallazgos Criticos`
  </action>
  <verify>
    <automated>grep -c "## 1. Arquitectura" .planning/phases/01-auditoria-alegra/ALEGRA-CODE-AUDIT.md && grep -c "## 5. Inventario ACTION_MAP" .planning/phases/01-auditoria-alegra/ALEGRA-CODE-AUDIT.md && grep -c "## 6. Acciones Faltantes" .planning/phases/01-auditoria-alegra/ALEGRA-CODE-AUDIT.md && grep -c "request_with_verify" .planning/phases/01-auditoria-alegra/ALEGRA-CODE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Fuente de Verdad" ALEGRA-CODE-AUDIT.md returns 1+ match
    - grep "ACTION_MAP" ALEGRA-CODE-AUDIT.md returns 3+ matches
    - grep "api.alegra.com/api/v1" ALEGRA-CODE-AUDIT.md returns 1+ match
    - grep "consultar_facturas" ALEGRA-CODE-AUDIT.md returns 1+ match (accion faltante documentada)
    - grep "request_with_verify" ALEGRA-CODE-AUDIT.md returns 1+ match
    - File has at least 7 H2 sections (## headings)
  </acceptance_criteria>
  <done>
    ALEGRA-CODE-AUDIT.md existe con 7 secciones completas. Cada modulo que importa alegra_service esta listado. Las 12 acciones de ACTION_MAP y las 5 faltantes estan documentadas. Todas las URLs incorrectas identificadas con archivo y linea.
  </done>
</task>

</tasks>

<verification>
- ALEGRA-CODE-AUDIT.md exists and has 7+ sections
- Every `from alegra_service import` in the codebase is documented in the report
- ACTION_MAP 12 actions listed with endpoint and method
- 5 missing read actions explicitly called out
- URL verification: ALEGRA_BASE_URL confirmed correct, any `app.alegra.com/api/r1` flagged
</verification>

<success_criteria>
1. ALEGRA-CODE-AUDIT.md has grep-verifiable sections for architecture, imports, URLs, request_with_verify, and ACTION_MAP
2. Zero ambiguity about which file is the Alegra source of truth
3. Every import of alegra_service documented with file path and what it imports
4. URL audit complete with evidence from grep output
</success_criteria>

<output>
After completion, create `.planning/phases/01-auditoria-alegra/01-01-SUMMARY.md`
</output>
