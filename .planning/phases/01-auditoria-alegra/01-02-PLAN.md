---
phase: 01-auditoria-alegra
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .planning/ALEGRA-AUDIT.md
autonomous: true
requirements: [AUDIT-02, AUDIT-05]

must_haves:
  truths:
    - "GET /categories retorna HTTP 200 con lista de cuentas — documentado con status code y extracto de 3 cuentas"
    - "GET /invoices retorna HTTP 200 con lista de facturas — documentado con status code y extracto"
    - "GET /payments retorna HTTP 200 con lista de pagos — documentado con status code y extracto"
    - "POST /journals con payload minimo retorna HTTP 200 o HTTP 400 con mensaje claro — documentado"
    - "ALEGRA-AUDIT.md existe en .planning/ con hallazgos, evidencia HTTP, y lista priorizada"
  artifacts:
    - path: ".planning/ALEGRA-AUDIT.md"
      provides: "Reporte final de auditoria Alegra con evidencia HTTP real"
      contains: "## Resultados por Endpoint"
  key_links:
    - from: "backend/alegra_service.py"
      to: "https://api.alegra.com/api/v1"
      via: "request() method"
      pattern: "HTTP (200|400|403|401)"
---

<objective>
Probar los endpoints criticos de Alegra con requests HTTP reales y producir el reporte final ALEGRA-AUDIT.md que consolida toda la auditoria.

Purpose: Obtener evidencia real (no suposiciones) de que funciona y que no en la integracion con Alegra API.
Output: .planning/ALEGRA-AUDIT.md — el entregable principal de la fase, con hallazgos de codigo + evidencia HTTP + lista priorizada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auditoria-alegra/01-01-SUMMARY.md
@.planning/phases/01-auditoria-alegra/ALEGRA-CODE-AUDIT.md
@backend/alegra_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probar endpoints Alegra con requests HTTP reales</name>
  <files>.planning/phases/01-auditoria-alegra/ALEGRA-ENDPOINT-RESULTS.md</files>
  <read_first>
    - .planning/phases/01-auditoria-alegra/ALEGRA-CODE-AUDIT.md (hallazgos de Plan 01)
    - backend/alegra_service.py lineas 16-18 (ALEGRA_BASE_URL, ALEGRA_EMAIL, ALEGRA_TOKEN)
  </read_first>
  <action>
Crear un script Python temporal `backend/tests/test_alegra_audit.py` que pruebe cada endpoint critico y registre resultados. El script NO debe modificar datos en produccion (solo GETs y un POST de journals con dry-run si es posible).

**Prerequisito:** Verificar que ALEGRA_EMAIL y ALEGRA_TOKEN estan configurados en el entorno. Si NO estan presentes, documentar "CREDENCIALES AUSENTES" en el resultado y usar el output del modo demo de AlegraService como evidencia parcial.

**Endpoints a probar (en este orden):**

1. **GET /categories** (equivalente a cuentas — el endpoint correcto, NO /accounts que da 403):
   - URL: `https://api.alegra.com/api/v1/categories`
   - Auth: Basic base64(ALEGRA_EMAIL:ALEGRA_TOKEN)
   - Registrar: HTTP status, numero de cuentas retornadas, extracto de 3 primeras cuentas (id, name, code)

2. **GET /invoices?limit=3** (facturas de venta recientes):
   - URL: `https://api.alegra.com/api/v1/invoices?limit=3`
   - Registrar: HTTP status, numero de facturas, extracto de primera factura (id, date, total, status)

3. **GET /payments?limit=3** (pagos recientes):
   - URL: `https://api.alegra.com/api/v1/payments?limit=3`
   - Registrar: HTTP status, numero de pagos, extracto de primer pago (id, date, amount)

4. **GET /journals?limit=3** (asientos contables recientes):
   - URL: `https://api.alegra.com/api/v1/journals?limit=3`
   - Registrar: HTTP status, numero de journals, extracto de primer journal (id, date, observations)

5. **GET /contacts?limit=3** (contactos):
   - URL: `https://api.alegra.com/api/v1/contacts?limit=3`
   - Registrar: HTTP status, numero de contactos

6. **GET /company** (datos de la empresa — sanity check):
   - URL: `https://api.alegra.com/api/v1/company`
   - Registrar: HTTP status, nombre de empresa

7. **GET /accounts** (ESTE DEBE FALLAR con 403 — confirmar que la restriccion es real):
   - URL: `https://api.alegra.com/api/v1/accounts`
   - Registrar: HTTP status (esperado: 403), mensaje de error

**Para cada endpoint, registrar en ALEGRA-ENDPOINT-RESULTS.md:**
```
### GET /categories
- **URL:** https://api.alegra.com/api/v1/categories
- **HTTP Status:** {200|403|401|timeout}
- **Resultado:** {numero de items retornados}
- **Extracto:** {primeros 3 items o mensaje de error}
- **Veredicto:** FUNCIONA | FALLA | BLOQUEADO
```

**Script test_alegra_audit.py debe:**
- Usar httpx directamente (no AlegraService) para evidencia pura
- Capturar timeout de 15 segundos
- NO lanzar excepciones — capturar todo y registrar
- Escribir resultados a stdout en formato parseable
- Funcionar sin credenciales (reportar "401 - Credenciales no configuradas")

Ejecutar el script: `cd backend && python -m tests.test_alegra_audit` o `python tests/test_alegra_audit.py`

Escribir resultados en ALEGRA-ENDPOINT-RESULTS.md.

**Despues de ejecutar, eliminar test_alegra_audit.py** — es un script de auditoria one-shot, no un test permanente.
  </action>
  <verify>
    <automated>grep -c "HTTP Status" .planning/phases/01-auditoria-alegra/ALEGRA-ENDPOINT-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - ALEGRA-ENDPOINT-RESULTS.md tiene al menos 7 secciones (una por endpoint)
    - Cada seccion tiene "HTTP Status:" con un valor numerico o "timeout" o "CREDENCIALES AUSENTES"
    - Cada seccion tiene "Veredicto:" con FUNCIONA, FALLA, o BLOQUEADO
    - GET /accounts muestra 403 o documenta por que no
  </acceptance_criteria>
  <done>
    7 endpoints probados con evidencia HTTP real. Cada resultado tiene status code, extracto de payload, y veredicto claro.
  </done>
</task>

<task type="auto">
  <name>Task 2: Consolidar reporte final ALEGRA-AUDIT.md</name>
  <files>.planning/ALEGRA-AUDIT.md</files>
  <read_first>
    - .planning/phases/01-auditoria-alegra/ALEGRA-CODE-AUDIT.md (Plan 01 — auditoria estatica)
    - .planning/phases/01-auditoria-alegra/ALEGRA-ENDPOINT-RESULTS.md (Task 1 — resultados HTTP)
  </read_first>
  <action>
Consolidar ambos documentos en el reporte final `.planning/ALEGRA-AUDIT.md` con estas secciones exactas:

```markdown
# Auditoria Alegra — SISMO BUILD 24

**Fecha:** 2026-03-30
**Fase:** 01-auditoria-alegra
**Ejecutor:** Claude (auditoria automatizada)

## 1. Arquitectura Actual

### Fuente de Verdad
{De ALEGRA-CODE-AUDIT.md seccion 1 — que archivo es la fuente, que NO existe utils/alegra.py}

### Mapa de Imports
{Tabla con cada archivo que importa alegra_service, que importa, y como lo usa}

## 2. Verificacion de URLs

### URL Base Correcta
- `ALEGRA_BASE_URL = "https://api.alegra.com/api/v1"` en `backend/alegra_service.py:16`
- `request_with_verify()` usa `self.request()` que construye URL con esta constante

### URLs Incorrectas Encontradas
{Tabla de cada referencia a app.alegra.com/api/r1 con archivo:linea}

### Endpoints Prohibidos Confirmados
- `/accounts` → 403 (usar `/categories` en su lugar)
- `/journal-entries` → usar `/journals`

## 3. Resultados por Endpoint

{De ALEGRA-ENDPOINT-RESULTS.md — tabla resumen:}
| Endpoint | Metodo | HTTP Status | Veredicto | Notas |
|----------|--------|-------------|-----------|-------|
| /categories | GET | {status} | {veredicto} | {nota} |
| /invoices | GET | {status} | {veredicto} | {nota} |
| /payments | GET | {status} | {veredicto} | {nota} |
| /journals | GET | {status} | {veredicto} | {nota} |
| /contacts | GET | {status} | {veredicto} | {nota} |
| /company | GET | {status} | {veredicto} | {nota} |
| /accounts | GET | {status} | BLOQUEADO | Usar /categories |

{Detalle por endpoint con extracto de payload}

## 4. Inventario ACTION_MAP

### Acciones Registradas (12)
{Tabla de ACTION_MAP con accion, endpoint, metodo, tipo (Alegra-directa vs interno)}

### Acciones Especiales (fuera de ACTION_MAP)
- diagnosticar_contabilidad — accounting_engine local
- guardar_pendiente — MongoDB local

### Acciones de Lectura FALTANTES (5)
| Accion Sugerida | Endpoint Alegra | Prioridad | Razon |
|-----------------|-----------------|-----------|-------|
| consultar_facturas | GET /invoices | ALTA | CFO necesita leer facturas |
| consultar_categorias | GET /categories | ALTA | Clasificacion contable |
| consultar_pagos | GET /payments | ALTA | Reconciliacion bancaria |
| consultar_journals | GET /journals | MEDIA | Auditoria de asientos |
| consultar_contactos | GET /contacts | MEDIA | CRM sync |

## 5. Analisis request_with_verify()

{De ALEGRA-CODE-AUDIT.md seccion 4}
- Flujo: POST → extraer ID → GET verificacion → retornar _verificado: True/False
- Usa ALEGRA_BASE_URL correcta (heredada de self.request())
- Maneja demo mode (retorna _verificado: True sin HTTP)

## 6. Lista Priorizada de Issues para Fases 2-8

### Criticos (Bloquean operaciones)
{Issues que impiden operaciones contables correctas}

### Importantes (Afectan calidad)
{Issues que degradan confiabilidad pero no bloquean}

### Mejoras (Nice to have)
{Optimizaciones y cleanup}

## 7. Recomendaciones para BUILD 24

{Que construir primero basado en los hallazgos}
```

El reporte debe ser FACTUAL — solo hallazgos con evidencia, no suposiciones. Cada afirmacion debe tener referencia al archivo:linea o HTTP status como evidencia.
  </action>
  <verify>
    <automated>grep -c "## 1. Arquitectura" .planning/ALEGRA-AUDIT.md && grep -c "## 3. Resultados por Endpoint" .planning/ALEGRA-AUDIT.md && grep -c "## 4. Inventario ACTION_MAP" .planning/ALEGRA-AUDIT.md && grep -c "## 6. Lista Priorizada" .planning/ALEGRA-AUDIT.md && grep -c "api.alegra.com/api/v1" .planning/ALEGRA-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/ALEGRA-AUDIT.md has 7 H2 sections (## 1 through ## 7)
    - grep "Fuente de Verdad" .planning/ALEGRA-AUDIT.md returns 1+ match
    - grep "HTTP Status" .planning/ALEGRA-AUDIT.md returns 7+ matches (one per endpoint)
    - grep "ACTION_MAP" .planning/ALEGRA-AUDIT.md returns 2+ matches
    - grep "consultar_facturas" .planning/ALEGRA-AUDIT.md returns 1+ match
    - grep "api.alegra.com/api/v1" .planning/ALEGRA-AUDIT.md returns 1+ match
    - grep "Lista Priorizada" .planning/ALEGRA-AUDIT.md returns 1+ match
    - File is at least 100 lines long
  </acceptance_criteria>
  <done>
    ALEGRA-AUDIT.md en .planning/ con 7 secciones completas: arquitectura, URLs, resultados HTTP con evidencia real, ACTION_MAP inventariado, acciones faltantes listadas, y lista priorizada de issues para fases 2-8.
  </done>
</task>

</tasks>

<verification>
- .planning/ALEGRA-AUDIT.md exists with 7+ sections
- Every tested endpoint has HTTP status evidence
- ACTION_MAP fully inventoried (12 registered + 5 missing)
- Prioritized issue list exists for phases 2-8
- No subjective claims without evidence
</verification>

<success_criteria>
1. ALEGRA-AUDIT.md has real HTTP evidence for GET /categories, /invoices, /payments, /journals (or documents why credentials were unavailable)
2. ACTION_MAP section lists all 12 registered actions and 5 missing read actions
3. Prioritized issue list gives clear guidance for BUILD 24 phases 2-8
4. Report is factual — every claim backed by file:line reference or HTTP status code
</success_criteria>

<output>
After completion, create `.planning/phases/01-auditoria-alegra/01-02-SUMMARY.md`
</output>
