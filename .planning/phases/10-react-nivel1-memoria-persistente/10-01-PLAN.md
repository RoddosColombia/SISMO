---
phase: 10-react-nivel1-memoria-persistente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_phase10.py
  - backend/tool_executor.py
  - backend/routers/chat.py
autonomous: true
requirements:
  - REACT-01
  - REACT-02
  - REACT-03
  - REACT-04

must_haves:
  truths:
    - "Colección agent_plans activa en MongoDB con schema completo"
    - "create_plan() crea documento con status pending_approval y retorna plan_id + descripción natural"
    - "execute_plan() itera acciones en orden, marca completed/failed, para en el primer fallo"
    - "POST /api/chat/approve-plan con confirmed=False cancela el plan"
    - "T1: create_plan() crea documento en agent_plans con status pending_approval"
    - "T2: execute_plan() ejecuta acciones en orden y marca completed"
    - "T3: execute_plan() para en step fallido y retorna error descriptivo"
    - "T4: POST /api/chat/approve-plan con confirmed=False cancela el plan"
  artifacts:
    - path: "backend/tests/test_phase10.py"
      provides: "Test suite T1-T9 para Phase 10 (T1-T4 verdes en este plan)"
      min_lines: 120
    - path: "backend/tool_executor.py"
      provides: "create_plan() + execute_plan() añadidos al módulo existente"
      exports: ["create_plan", "execute_plan"]
  key_links:
    - from: "backend/tests/test_phase10.py"
      to: "backend/tool_executor.py"
      via: "import create_plan, execute_plan"
      pattern: "from tool_executor import"
    - from: "backend/routers/chat.py"
      to: "backend/tool_executor.py"
      via: "import execute_plan"
      pattern: "from tool_executor import"
---

<objective>
Phase 10A: Implementar ReAct Nivel 1 — plan multi-acción + ejecución autónoma.

Construir el sistema de planes multi-acción sobre el tool_executor existente:
1. TDD: tests T1-T4 para agent_plans + create_plan + execute_plan + approve-plan
2. Implementar create_plan() en tool_executor.py
3. Implementar execute_plan() en tool_executor.py
4. Añadir endpoint POST /api/chat/approve-plan en routers/chat.py
5. Modificar ai_chat.py TOOL_USE_ENABLED branch para detectar múltiples tool_calls

NO tocar el flujo XML existente. NO tocar agent_memory todavía (es Phase 10B).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/tool_executor.py (módulo completo — 158 líneas)
@backend/ai_chat.py (líneas 3850-3973: TOOL_USE_ENABLED branch completo)
@backend/routers/chat.py (líneas 1-50: estructura del router)
@backend/tests/test_phase9_tool_use.py (patrón de tests: sys.path, mocks, async fixtures)

<schema_agent_plans>
Cada documento en agent_plans:
{
  plan_id: UUID string,
  session_id: string,
  user_id: string,
  created_at: datetime UTC isoformat,
  status: "pending_approval" | "approved" | "executing" | "completed" | "failed" | "cancelled",
  original_request: string,
  actions: [
    {
      step: int (1-based),
      tool_name: string,
      tool_input: dict,
      description: string (lenguaje natural),
      status: "pending" | "executing" | "completed" | "failed" | "skipped",
      result: dict | null,
      alegra_id: string | null,
      error: string | null,
      executed_at: datetime isoformat | null
    }
  ],
  completed_steps: int,
  total_steps: int,
  summary: string | null
}
</schema_agent_plans>

<reglas_criticas>
- NUNCA usar /journal-entries — siempre /journals
- NUNCA usar app.alegra.com/api/r1 — siempre api.alegra.com/api/v1
- NUNCA reportar éxito sin HTTP 200 (request_with_verify obligatorio)
- Fallback cuentas Alegra: ID 5493 NUNCA ID 5495
- execute_plan() llama execute_chat_action() — NUNCA llama Alegra directo
</reglas_criticas>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD RED — tests T1-T4 para agent_plans + create_plan + execute_plan + approve-plan</name>
  <files>backend/tests/test_phase10.py</files>
  <action>
Crear `backend/tests/test_phase10.py` con tests T1-T9 completos.

En este plan (Wave 1) solo T1-T4 deben pasar GREEN. T5-T9 deben marcarse con
`@pytest.mark.xfail(reason="RED: Phase 10B — agent_memory not implemented yet")`.

Patrón de test: seguir exactamente backend/tests/test_phase9_tool_use.py:
- sys.path.insert(0, ...) para importar desde backend/
- Mock de MongoDB via AsyncMock / MagicMock
- Fixtures async con @pytest.mark.asyncio
- Stub anthropic si se necesita para proceso de extracción de memoria (T5-T8)

**T1: create_plan() crea documento en agent_plans con status pending_approval**
```python
@pytest.mark.asyncio
async def test_t1_create_plan_creates_document():
    from tool_executor import create_plan
    db = MagicMock()
    db.agent_plans = AsyncMock()
    db.agent_plans.insert_one = AsyncMock(return_value=MagicMock(inserted_id="abc"))

    tool_calls = [
        {"tool_name": "crear_causacion", "tool_input": {"description": "Gasto internet", "date": "2026-04-01", "entries": []}},
        {"tool_name": "registrar_pago_cartera", "tool_input": {"loanbook_id": "LB-0001", "monto": 150000, "fecha": "2026-04-01", "banco_origen": "Bancolombia"}},
    ]
    result = await create_plan(
        request="Registra un gasto de internet y un pago de cartera",
        tool_calls=tool_calls,
        session_id="test-session-001",
        db=db,
        user={"id": "user-001", "email": "test@roddos.com"},
    )

    assert "plan_id" in result
    assert "description" in result  # lenguaje natural con pasos numerados
    assert "Paso 1" in result["description"] or "paso 1" in result["description"].lower()
    assert db.agent_plans.insert_one.called
    # Verificar que el doc insertado tiene status pending_approval
    call_args = db.agent_plans.insert_one.call_args[0][0]
    assert call_args["status"] == "pending_approval"
    assert call_args["total_steps"] == 2
    assert len(call_args["actions"]) == 2
```

**T2: execute_plan() ejecuta acciones en orden y marca completed**
```python
@pytest.mark.asyncio
async def test_t2_execute_plan_executes_in_order():
    from tool_executor import execute_plan
    import uuid
    plan_id = str(uuid.uuid4())
    plan_doc = {
        "plan_id": plan_id,
        "session_id": "sess-001",
        "user_id": "user-001",
        "status": "pending_approval",
        "original_request": "test",
        "actions": [
            {"step": 1, "tool_name": "crear_causacion", "tool_input": {"description": "Test", "date": "2026-04-01", "entries": []}, "description": "Paso 1", "status": "pending", "result": None, "alegra_id": None, "error": None, "executed_at": None},
            {"step": 2, "tool_name": "consultar_cartera", "tool_input": {}, "description": "Paso 2", "status": "pending", "result": None, "alegra_id": None, "error": None, "executed_at": None},
        ],
        "completed_steps": 0,
        "total_steps": 2,
        "summary": None,
    }

    db = MagicMock()
    db.agent_plans = AsyncMock()
    db.agent_plans.find_one = AsyncMock(return_value=plan_doc)
    db.agent_plans.update_one = AsyncMock()

    user = {"id": "user-001"}

    with patch("tool_executor.execute_chat_action_for_plan") as mock_exec:
        mock_exec.side_effect = [
            {"success": True, "alegra_id": "J-001", "result": {"id": "J-001"}},
            {"success": True, "alegra_id": None, "result": {"cartera": []}},
        ]
        result = await execute_plan(plan_id, db, user)

    assert result["status"] == "completed"
    assert result["completed_steps"] == 2
    assert mock_exec.call_count == 2
    # Verificar orden de llamadas
    first_call = mock_exec.call_args_list[0]
    assert first_call[0][0] == "crear_causacion"  # primer argumento = tool_name
```

**T3: execute_plan() para en step fallido y retorna error descriptivo**
```python
@pytest.mark.asyncio
async def test_t3_execute_plan_stops_on_failure():
    from tool_executor import execute_plan
    import uuid
    plan_id = str(uuid.uuid4())
    plan_doc = {
        "plan_id": plan_id,
        "session_id": "sess-002",
        "user_id": "user-001",
        "status": "pending_approval",
        "original_request": "test fallo",
        "actions": [
            {"step": 1, "tool_name": "crear_causacion", "tool_input": {"description": "Gasto", "date": "2026-04-01", "entries": []}, "description": "Paso 1", "status": "pending", "result": None, "alegra_id": None, "error": None, "executed_at": None},
            {"step": 2, "tool_name": "registrar_pago_cartera", "tool_input": {"loanbook_id": "LB-0001", "monto": 100000, "fecha": "2026-04-01", "banco_origen": "X"}, "description": "Paso 2", "status": "pending", "result": None, "alegra_id": None, "error": None, "executed_at": None},
        ],
        "completed_steps": 0,
        "total_steps": 2,
        "summary": None,
    }

    db = MagicMock()
    db.agent_plans = AsyncMock()
    db.agent_plans.find_one = AsyncMock(return_value=plan_doc)
    db.agent_plans.update_one = AsyncMock()
    user = {"id": "user-001"}

    with patch("tool_executor.execute_chat_action_for_plan") as mock_exec:
        mock_exec.return_value = {"success": False, "error": "HTTP 422 de Alegra", "alegra_id": None}
        result = await execute_plan(plan_id, db, user)

    assert result["status"] == "failed"
    assert "error" in result
    assert "HTTP 422" in result["error"] or "Paso 1" in result["error"]
    # Solo se ejecutó el paso 1 (falló), el paso 2 NO se ejecutó
    assert mock_exec.call_count == 1
```

**T4: POST /api/chat/approve-plan con confirmed=False cancela el plan**
```python
@pytest.mark.asyncio
async def test_t4_approve_plan_false_cancels():
    # Test the endpoint logic directly (import the handler function)
    from tool_executor import cancel_plan
    import uuid
    plan_id = str(uuid.uuid4())

    db = MagicMock()
    db.agent_plans = AsyncMock()
    db.agent_plans.update_one = AsyncMock()

    result = await cancel_plan(plan_id, db)

    assert result["cancelled"] is True
    assert db.agent_plans.update_one.called
    call_args = db.agent_plans.update_one.call_args
    assert call_args[0][1]["$set"]["status"] == "cancelled"
```

**T5-T9 (xfail — Phase 10B):**
Marcar todos con `@pytest.mark.xfail(reason="RED: Phase 10B not implemented yet")`.
Escribir el body de T5-T8 con la lógica de agent_memory pero sin implementación real aún.
T9 requiere 2 acciones reales end-to-end — también xfail.

Estructura mínima de T5-T9:
- T5: `extract_and_save_memory()` guarda con confidence >= 0.7
- T6: `extract_and_save_memory()` hace upsert (no duplica si key existe)
- T7: `build_agent_prompt()` incluye sección MEMORIA PERSISTENTE cuando hay registros
- T8: plan con una sola lectura NO crea agent_plans — ejecuta directo
- T9: execute_plan() con 2 acciones reales retorna 2 alegra_ids
  </action>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && python -m pytest tests/test_phase10.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
test_phase10.py existe con T1-T4 green + T5-T9 xfail.
Cero errores inesperados. T1-T4 pasan porque aún son estructurales (aún no llaman las funciones reales — las mockeamos en Task 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar create_plan() y execute_plan() en tool_executor.py</name>
  <files>backend/tool_executor.py</files>
  <action>
Añadir las siguientes funciones al final de `backend/tool_executor.py` (después de `confirm_pending_action`):

```python
import uuid as _uuid  # añadir al bloque de imports si no existe

async def create_plan(
    request: str,
    tool_calls: list,
    session_id: str,
    db,
    user: dict,
) -> dict:
    """
    Crea un plan multi-acción en agent_plans con status pending_approval.
    NO ejecuta nada — solo persiste el plan y retorna descripción natural.

    Args:
        request:    El mensaje original del usuario
        tool_calls: Lista de dicts {tool_name, tool_input, description?}
        session_id: ID de la sesión activa
        db:         Motor AsyncIOMotorDatabase
        user:       Dict del usuario autenticado

    Returns:
        {"plan_id": str, "description": str, "total_steps": int}
    """
    from tool_definitions import TOOL_DEFS

    plan_id = str(_uuid.uuid4())
    now = datetime.now(timezone.utc)

    actions = []
    desc_lines = []
    for i, tc in enumerate(tool_calls, start=1):
        tool_name = tc.get("tool_name") or tc.get("name", "")
        tool_input = tc.get("tool_input") or tc.get("input", {})
        tool_def = TOOL_DEFS.get(tool_name, {})
        natural = tc.get("description") or tool_def.get("description", tool_name)
        actions.append({
            "step": i,
            "tool_name": tool_name,
            "tool_input": tool_input,
            "description": natural,
            "status": "pending",
            "result": None,
            "alegra_id": None,
            "error": None,
            "executed_at": None,
        })
        desc_lines.append(f"Paso {i}: {natural}")

    plan_doc = {
        "plan_id": plan_id,
        "session_id": session_id,
        "user_id": user.get("id"),
        "created_at": now.isoformat(),
        "status": "pending_approval",
        "original_request": request,
        "actions": actions,
        "completed_steps": 0,
        "total_steps": len(actions),
        "summary": None,
    }

    await db.agent_plans.insert_one(plan_doc)

    description = (
        f"Plan de {len(actions)} pasos para: {request}\n\n"
        + "\n".join(desc_lines)
    )

    return {
        "plan_id": plan_id,
        "description": description,
        "total_steps": len(actions),
    }


async def execute_chat_action_for_plan(tool_name: str, tool_input: dict, db, user: dict) -> dict:
    """
    Wrapper de execute_chat_action para uso dentro de execute_plan().
    Retorna siempre {"success": bool, "alegra_id": str|None, "result": dict, "error": str|None}.
    """
    from ai_chat import execute_chat_action
    try:
        result = await execute_chat_action(tool_name, tool_input, db, user)
        success = result.get("success", True)
        alegra_id = (
            result.get("alegra_id")
            or result.get("journal_id")
            or result.get("invoice_id")
            or result.get("payment_id")
        )
        return {
            "success": success,
            "alegra_id": str(alegra_id) if alegra_id else None,
            "result": result,
            "error": result.get("error") if not success else None,
        }
    except Exception as exc:
        return {"success": False, "alegra_id": None, "result": {}, "error": str(exc)}


async def execute_plan(plan_id: str, db, user: dict) -> dict:
    """
    Carga un plan de agent_plans y ejecuta sus acciones en orden.

    Reglas:
    - Cambia status a executing
    - Por cada acción: ejecuta via execute_chat_action_for_plan()
    - Si success: marca completed, guarda alegra_id, incrementa completed_steps
    - Si failure: marca failed en la acción Y en el plan, PARA y retorna error descriptivo
    - Cuando todos completan: status completed + genera summary

    Returns:
        {"status": "completed"|"failed", "completed_steps": int, "total_steps": int,
         "summary": str|None, "error": str|None, "alegra_ids": list[str]}
    """
    plan = await db.agent_plans.find_one({"plan_id": plan_id})
    if not plan:
        return {"status": "failed", "error": f"Plan no encontrado: {plan_id}", "completed_steps": 0, "total_steps": 0, "summary": None, "alegra_ids": []}

    total = plan.get("total_steps", len(plan.get("actions", [])))

    # Marcar como executing
    await db.agent_plans.update_one(
        {"plan_id": plan_id},
        {"$set": {"status": "executing"}},
    )

    alegra_ids = []
    completed = 0

    for action in plan.get("actions", []):
        step = action["step"]
        tool_name = action["tool_name"]
        tool_input = action["tool_input"]

        # Marcar acción como executing
        await db.agent_plans.update_one(
            {"plan_id": plan_id, "actions.step": step},
            {"$set": {"actions.$.status": "executing"}},
        )

        result = await execute_chat_action_for_plan(tool_name, tool_input, db, user)

        executed_at = datetime.now(timezone.utc).isoformat()

        if result["success"]:
            alegra_id = result.get("alegra_id")
            if alegra_id:
                alegra_ids.append(alegra_id)
            completed += 1
            await db.agent_plans.update_one(
                {"plan_id": plan_id, "actions.step": step},
                {"$set": {
                    "actions.$.status": "completed",
                    "actions.$.result": result.get("result", {}),
                    "actions.$.alegra_id": alegra_id,
                    "actions.$.executed_at": executed_at,
                    "completed_steps": completed,
                }},
            )
        else:
            error_msg = f"Paso {step} ({tool_name}) falló: {result.get('error', 'error desconocido')}"
            await db.agent_plans.update_one(
                {"plan_id": plan_id, "actions.step": step},
                {"$set": {
                    "actions.$.status": "failed",
                    "actions.$.error": result.get("error"),
                    "actions.$.executed_at": executed_at,
                    "status": "failed",
                }},
            )
            return {
                "status": "failed",
                "completed_steps": completed,
                "total_steps": total,
                "summary": None,
                "error": error_msg,
                "alegra_ids": alegra_ids,
            }

    # Todos los pasos completados
    summary = (
        f"Plan completado: {completed}/{total} pasos. "
        f"IDs Alegra: {', '.join(alegra_ids) if alegra_ids else 'ninguno (solo lecturas)'}."
    )
    await db.agent_plans.update_one(
        {"plan_id": plan_id},
        {"$set": {"status": "completed", "summary": summary, "completed_steps": completed}},
    )

    return {
        "status": "completed",
        "completed_steps": completed,
        "total_steps": total,
        "summary": summary,
        "error": None,
        "alegra_ids": alegra_ids,
    }


async def cancel_plan(plan_id: str, db) -> dict:
    """Cancela un plan cambiando su status a cancelled."""
    await db.agent_plans.update_one(
        {"plan_id": plan_id},
        {"$set": {"status": "cancelled"}},
    )
    return {"cancelled": True, "plan_id": plan_id, "message": "Plan cancelado por el usuario"}
```

**IMPORTANTE:** No modificar las funciones existentes `execute_tool()` y `confirm_pending_action()`.
Solo AÑADIR al final del archivo.
  </action>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && python -c "from tool_executor import create_plan, execute_plan, cancel_plan, execute_chat_action_for_plan; print('OK: todas las funciones importan')"</automated>
  </verify>
  <done>
create_plan(), execute_plan(), cancel_plan(), execute_chat_action_for_plan() importan correctamente.
Funciones existentes intactas.
  </done>
</task>

<task type="auto">
  <name>Task 3: Añadir POST /api/chat/approve-plan endpoint en routers/chat.py</name>
  <files>backend/routers/chat.py</files>
  <action>
Leer el archivo `backend/routers/chat.py` completo para entender la estructura.

Añadir el endpoint `POST /api/chat/approve-plan` siguiendo el patrón existente del router.

El endpoint debe:
1. Recibir `{plan_id: str, session_id: str, confirmed: bool}`
2. Si `confirmed == True`: llamar `execute_plan(plan_id, db, user)`
3. Si `confirmed == False`: llamar `cancel_plan(plan_id, db)` y retornar mensaje
4. Retornar el resultado con summary y alegra_ids como evidencia

```python
class ApprovePlanRequest(BaseModel):
    plan_id: str
    session_id: str
    confirmed: bool

@router.post("/approve-plan")
async def approve_plan(
    req: ApprovePlanRequest,
    db=Depends(get_db),
    current_user=Depends(get_current_user),
):
    from tool_executor import execute_plan, cancel_plan
    if not req.confirmed:
        result = await cancel_plan(req.plan_id, db)
        return result
    result = await execute_plan(req.plan_id, db, current_user)
    return result
```

Asegurarse de que el router de chat esté incluido en server.py con prefix="/api/chat".
Verificar con grep que `approve-plan` o `approve_plan` ya no exista antes de añadir.
  </action>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && grep -n "approve.plan\|approve_plan" routers/chat.py | head -10</automated>
  </verify>
  <done>
Endpoint /api/chat/approve-plan existe en routers/chat.py con ApprovePlanRequest model.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Correr T1-T4 y verificar todos verdes</name>
  <files>backend/tests/test_phase10.py</files>
  <action>
Ejecutar el test suite para verificar que T1-T4 pasan.

Ajustar los tests si fallan por discrepancias menores (nombres de argumentos, etc.)
pero NO cambiar la lógica del contrato — solo ajustar mocks/assertions para que
coincidan con la implementación real.

```bash
cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && python -m pytest tests/test_phase10.py -v --tb=short 2>&1
```

También verificar que los tests BUILD existentes siguen pasando:
```bash
cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && python -m pytest tests/test_permissions.py tests/test_event_bus.py tests/test_mongodb_init.py tests/test_phase4_agents.py -v --tb=short 2>&1 | tail -20
```
  </action>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && python -m pytest tests/test_phase10.py::test_t1_create_plan_creates_document tests/test_phase10.py::test_t2_execute_plan_executes_in_order tests/test_phase10.py::test_t3_execute_plan_stops_on_failure tests/test_phase10.py::test_t4_approve_plan_false_cancels -v --tb=short 2>&1</automated>
  </verify>
  <done>
T1, T2, T3, T4 pasan GREEN. T5-T9 son xfail (expected). BUILD tests sin regresión.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest backend/tests/test_phase10.py -v` — T1-T4 green, T5-T9 xfail
2. `python -c "from tool_executor import create_plan, execute_plan, cancel_plan"` — importa OK
3. `grep -n "approve-plan" backend/routers/chat.py` — endpoint existe
4. `python -m pytest backend/tests/test_permissions.py backend/tests/test_event_bus.py backend/tests/test_mongodb_init.py backend/tests/test_phase4_agents.py -v` — sin regresión
5. `grep -rn "journal-entries\|app.alegra.com/api/r1" backend/tool_executor.py` — 0 resultados
</verification>

<success_criteria>
- create_plan() y execute_plan() implementados en tool_executor.py
- cancel_plan() implementado
- POST /api/chat/approve-plan endpoint en routers/chat.py
- T1-T4 pasan GREEN, T5-T9 son xfail
- Tests BUILD existentes sin regresión
- Zero menciones de /journal-entries o app.alegra.com/api/r1 en nuevos archivos
</success_criteria>

<output>
Después de completar, crear `.planning/phases/10-react-nivel1-memoria-persistente/10-01-SUMMARY.md`
</output>
