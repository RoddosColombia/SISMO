---
phase: 10-react-nivel1-memoria-persistente
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01-PLAN.md"
files_modified:
  - backend/tool_executor.py
  - backend/ai_chat.py
  - backend/tests/test_phase10.py
autonomous: true
requirements:
  - REACT-05
  - REACT-06
  - REACT-07
  - REACT-08
  - REACT-09

must_haves:
  truths:
    - "extract_and_save_memory() guarda en agent_memory con confidence >= 0.7 (T5 green)"
    - "extract_and_save_memory() hace upsert — no duplica si key ya existe (T6 green)"
    - "System prompt incluye sección MEMORIA PERSISTENTE cuando hay registros en agent_memory (T7 green)"
    - "Plan con una sola lectura NO crea agent_plans — ejecuta directo como antes (T8 green)"
    - "T9 xfail removido si se puede demostrar con mocks el flujo completo de 2 acciones"
  artifacts:
    - path: "backend/tool_executor.py"
      provides: "extract_and_save_memory() añadida al módulo"
      exports: ["extract_and_save_memory"]
    - path: "backend/ai_chat.py"
      provides: "Memory injection en system_prompt + lógica multi-tool_call → create_plan"
      min_lines: 5751
  key_links:
    - from: "backend/ai_chat.py"
      to: "backend/tool_executor.py"
      via: "import extract_and_save_memory"
      pattern: "from tool_executor import"
    - from: "backend/tests/test_phase10.py"
      to: "backend/tool_executor.py"
      via: "import extract_and_save_memory"
      pattern: "from tool_executor import"
---

<objective>
Phase 10B: Memoria Persistente sin TTL + lógica multi-tool_call en process_chat.

Construir sobre los cimientos de 10-01 (agent_plans ya existe):
1. TDD GREEN: hacer pasar T5-T8 (agent_memory con schema nuevo + extract_and_save_memory)
2. Implementar extract_and_save_memory() en tool_executor.py usando claude-haiku-4-5-20251001
3. Inyectar memoria en system_prompt de process_chat (últimos 20 registros por usage_count DESC)
4. Modificar TOOL_USE_ENABLED branch en ai_chat.py para manejar múltiples tool_calls con create_plan
5. T8: detectar "solo lectura" y ejecutar directo sin crear agent_plans
6. T9: verificar flujo end-to-end completo con mocks de 2 acciones → 2 alegra_ids

REGLA CRÍTICA: Modelo para extract_and_save_memory = claude-haiku-4-5-20251001 (NO Sonnet)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-react-nivel1-memoria-persistente/10-01-PLAN.md
@.planning/phases/10-react-nivel1-memoria-persistente/10-01-SUMMARY.md

@backend/tool_executor.py (versión actualizada tras 10-01)
@backend/ai_chat.py (líneas 3852-3973: TOOL_USE_ENABLED branch)
@backend/ai_chat.py (líneas 3064-3200: construcción del system_prompt en process_chat)
@backend/tests/test_phase10.py (versión tras 10-01: T1-T4 green, T5-T9 xfail)

<schema_agent_memory_new>
Los documentos nuevos en agent_memory (coexisten con los patrones existentes — discriminados por campo "source"):
{
  user_id: string,
  key: string,       -- identificador semántico único por usuario (ej: "proveedor_auteco_nit")
  value: string,     -- aprendizaje en lenguaje natural
  source: "correction" | "pattern" | "preference",
  confidence: float 0-1,
  created_at: datetime UTC isoformat,
  updated_at: datetime UTC isoformat,
  usage_count: int
}
Index único en {user_id, key} para garantizar upsert correcto.
</schema_agent_memory_new>

<reglas_criticas>
- Modelo extracción memoria: claude-haiku-4-5-20251001 — NUNCA Sonnet para tareas ligeras
- NUNCA usar /journal-entries — siempre /journals
- NUNCA usar app.alegra.com/api/r1 — siempre api.alegra.com/api/v1
- Una sola acción de LECTURA (consultar_facturas, consultar_cartera): ejecutar directo SIN crear agent_plans
- Múltiples acciones O cualquier acción de escritura: crear agent_plans + retornar para aprobación
- agent_memory nueva solo se lee/escribe con campo "source" en [correction, pattern, preference]
- agent_memory existente (con campo "tipo") no se toca
</reglas_criticas>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar extract_and_save_memory() en tool_executor.py</name>
  <files>backend/tool_executor.py</files>
  <action>
Añadir la función `extract_and_save_memory()` al final de `backend/tool_executor.py`.

La función usa Claude Haiku 4.5 para extraer aprendizajes de la interacción y guardarlos:

```python
async def extract_and_save_memory(
    request: str,
    result: dict,
    db,
    user: dict,
) -> dict | None:
    """
    Extrae aprendizajes de la interacción y los persiste en agent_memory sin TTL.

    Usa claude-haiku-4-5-20251001 para detectar:
    - correction: usuario corrigió una clasificación o cuenta
    - pattern: una cuenta específica fue usada para un proveedor nuevo
    - preference: usuario prefirió un banco/forma de pago específica

    Solo guarda si confidence >= 0.7.
    Hace upsert por {user_id, key} — no duplica.

    Returns:
        dict con {key, value, source, confidence} si se guardó, None si no
    """
    import os
    import anthropic as _anthropic

    user_id = user.get("id", "unknown")

    # Prompt ligero para extracción de aprendizaje
    extraction_prompt = f"""Analiza esta interacción del Agente Contador de RODDOS y determina si hay un aprendizaje persistente.

Solicitud del usuario: {request}
Resultado: {str(result)[:500]}

Responde SOLO con JSON (sin markdown):
{{
  "has_learning": true/false,
  "key": "identificador_semantico_corto_snake_case",
  "value": "aprendizaje en lenguaje natural, máximo 100 caracteres",
  "source": "correction" | "pattern" | "preference",
  "confidence": 0.0-1.0
}}

Patrones a detectar:
- correction: el usuario corrigió una clasificación contable o cuenta
- pattern: una cuenta/proveedor específico fue usado por primera vez
- preference: el usuario eligió banco o forma de pago específica

Si no hay aprendizaje claro con confidence >= 0.7, retorna has_learning: false."""

    try:
        api_key = os.environ.get("ANTHROPIC_API_KEY", "")
        if not api_key:
            return None

        client = _anthropic.AsyncAnthropic(api_key=api_key)
        resp = await client.messages.create(
            model="claude-haiku-4-5-20251001",
            max_tokens=256,
            messages=[{"role": "user", "content": extraction_prompt}],
        )
        raw = resp.content[0].text.strip()

        # Limpiar markdown si viene envuelto
        if raw.startswith("```"):
            raw = raw.split("```")[1]
            if raw.startswith("json"):
                raw = raw[4:]

        import json as _json
        data = _json.loads(raw)

        if not data.get("has_learning") or float(data.get("confidence", 0)) < 0.7:
            return None

        key = data["key"]
        value = data["value"]
        source = data.get("source", "pattern")
        confidence = float(data["confidence"])

        now = datetime.now(timezone.utc).isoformat()

        await db.agent_memory.update_one(
            {"user_id": user_id, "key": key},
            {
                "$set": {
                    "value": value,
                    "source": source,
                    "confidence": confidence,
                    "updated_at": now,
                    "user_id": user_id,
                    "key": key,
                },
                "$setOnInsert": {"created_at": now},
                "$inc": {"usage_count": 1},
            },
            upsert=True,
        )

        return {"key": key, "value": value, "source": source, "confidence": confidence}

    except Exception as exc:
        logger.warning("extract_and_save_memory error: %s", exc)
        return None
```
  </action>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && python -c "from tool_executor import extract_and_save_memory; print('OK: extract_and_save_memory importa')"</automated>
  </verify>
  <done>
extract_and_save_memory() importa correctamente. Usa claude-haiku-4-5-20251001.
No modifica funciones existentes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN T5-T8 en test_phase10.py</name>
  <files>backend/tests/test_phase10.py</files>
  <action>
Actualizar `backend/tests/test_phase10.py` para hacer pasar T5-T8.

**Eliminar el `@pytest.mark.xfail` de T5, T6, T7, T8** y reescribir cada uno con mocks correctos.

**T5: extract_and_save_memory() guarda con confidence >= 0.7**
```python
@pytest.mark.asyncio
async def test_t5_extract_and_save_memory_saves_with_high_confidence():
    from tool_executor import extract_and_save_memory
    from unittest.mock import AsyncMock, MagicMock, patch
    import json

    db = MagicMock()
    db.agent_memory = AsyncMock()
    db.agent_memory.update_one = AsyncMock()

    user = {"id": "user-001"}

    # Mock anthropic client para retornar aprendizaje válido
    mock_resp_content = MagicMock()
    mock_resp_content.text = json.dumps({
        "has_learning": True,
        "key": "auteco_autoretenedor",
        "value": "Auteco NIT 860024781 es AUTORETENEDOR — nunca aplicar ReteFuente",
        "source": "correction",
        "confidence": 0.95,
    })
    mock_resp = MagicMock()
    mock_resp.content = [mock_resp_content]

    mock_client = AsyncMock()
    mock_client.messages.create = AsyncMock(return_value=mock_resp)

    with patch("tool_executor._anthropic") as mock_anthropic:
        mock_anthropic.AsyncAnthropic.return_value = mock_client

        result = await extract_and_save_memory(
            request="Registra gasto proveedor Auteco",
            result={"success": True, "alegra_id": "J-005"},
            db=db,
            user=user,
        )

    assert result is not None
    assert result["key"] == "auteco_autoretenedor"
    assert result["confidence"] >= 0.7
    assert db.agent_memory.update_one.called
```

**T6: extract_and_save_memory() hace upsert — no duplica**
```python
@pytest.mark.asyncio
async def test_t6_extract_and_save_memory_upserts_not_duplicates():
    from tool_executor import extract_and_save_memory
    from unittest.mock import AsyncMock, MagicMock, patch
    import json

    db = MagicMock()
    db.agent_memory = AsyncMock()
    db.agent_memory.update_one = AsyncMock()

    user = {"id": "user-001"}

    mock_resp_content = MagicMock()
    mock_resp_content.text = json.dumps({
        "has_learning": True,
        "key": "banco_preferido_nequi",
        "value": "Usuario prefiere Nequi para pagos de cartera",
        "source": "preference",
        "confidence": 0.80,
    })
    mock_resp = MagicMock()
    mock_resp.content = [mock_resp_content]

    mock_client = AsyncMock()
    mock_client.messages.create = AsyncMock(return_value=mock_resp)

    with patch("tool_executor._anthropic") as mock_anthropic:
        mock_anthropic.AsyncAnthropic.return_value = mock_client

        # Llamar 2 veces con mismo request
        await extract_and_save_memory("Pago cartera Nequi", {"success": True}, db, user)
        await extract_and_save_memory("Pago cartera Nequi", {"success": True}, db, user)

    # update_one con upsert=True fue llamado 2 veces (no insert_one)
    assert db.agent_memory.update_one.call_count == 2
    # Verificar que usa upsert=True en ambas llamadas
    for call in db.agent_memory.update_one.call_args_list:
        assert call[1].get("upsert") is True or (len(call[0]) > 2 and call[0][2] is True)
```

**T7: system prompt incluye MEMORIA PERSISTENTE cuando hay registros**
```python
@pytest.mark.asyncio
async def test_t7_system_prompt_includes_memory_section():
    import sys
    import os
    sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

    # Mock anthropic antes de importar ai_chat
    import anthropic as _ant
    mock_ant = MagicMock()
    mock_ant.AsyncAnthropic = MagicMock()
    sys.modules["anthropic"] = mock_ant

    from ai_chat import _load_persistent_memory_section

    db = MagicMock()
    db.agent_memory = AsyncMock()
    # Retornar 2 memorias con campo "source" (discriminador de memorias nuevas)
    db.agent_memory.find = MagicMock(return_value=AsyncMock())
    db.agent_memory.find.return_value.sort = MagicMock(return_value=AsyncMock())
    db.agent_memory.find.return_value.sort.return_value.to_list = AsyncMock(return_value=[
        {"key": "auteco_autoretenedor", "value": "Auteco NIT 860024781 es AUTORETENEDOR", "source": "correction", "confidence": 0.95, "usage_count": 3},
        {"key": "banco_preferido_nequi", "value": "Usuario prefiere Nequi para pagos", "source": "preference", "confidence": 0.80, "usage_count": 1},
    ])

    section = await _load_persistent_memory_section(db, "user-001")

    assert "MEMORIA PERSISTENTE" in section
    assert "auteco_autoretenedor" in section or "Auteco NIT" in section
```

**T8: plan con una sola lectura NO crea agent_plans — ejecuta directo**
```python
@pytest.mark.asyncio
async def test_t8_single_read_tool_no_plan_created():
    """Cuando hay exactamente 1 tool_call de lectura, no se crea agent_plans."""
    from tool_executor import should_create_plan

    # Solo una lectura → no crear plan
    single_read = [{"tool_name": "consultar_facturas", "tool_input": {"fecha_inicio": "2026-04-01", "fecha_fin": "2026-04-30"}}]
    assert should_create_plan(single_read) is False

    # Una escritura → crear plan
    single_write = [{"tool_name": "crear_causacion", "tool_input": {"description": "Gasto", "date": "2026-04-01", "entries": []}}]
    assert should_create_plan(single_write) is True

    # Múltiples acciones → crear plan
    multi = [
        {"tool_name": "consultar_facturas", "tool_input": {}},
        {"tool_name": "crear_causacion", "tool_input": {}},
    ]
    assert should_create_plan(multi) is True
```

Mantener T9 como xfail por ahora (requiere 2 llamadas reales a Alegra para verificación en producción).
  </action>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && python -m pytest tests/test_phase10.py::test_t5_extract_and_save_memory_saves_with_high_confidence tests/test_phase10.py::test_t6_extract_and_save_memory_upserts_not_duplicates tests/test_phase10.py::test_t7_system_prompt_includes_memory_section tests/test_phase10.py::test_t8_single_read_tool_no_plan_created -v --tb=short 2>&1</automated>
  </verify>
  <done>
T5, T6, T7, T8 pasan GREEN. T9 sigue xfail. T1-T4 siguen verdes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Añadir should_create_plan() y _load_persistent_memory_section() al código</name>
  <files>backend/tool_executor.py, backend/ai_chat.py</files>
  <action>
**En tool_executor.py** — añadir `should_create_plan()` después de `cancel_plan()`:

```python
def should_create_plan(tool_calls: list) -> bool:
    """
    Determina si se debe crear un agent_plan para las tool_calls dadas.

    Regla:
    - Una sola tool_call de LECTURA (consultar_facturas, consultar_cartera): False
    - Cualquier tool_call de ESCRITURA: True
    - Múltiples tool_calls (sin importar tipo): True

    Returns:
        True si se debe crear un plan y pedir aprobación al usuario
    """
    from tool_definitions import TOOL_DEFS

    READ_TOOLS = {"consultar_facturas", "consultar_cartera"}

    if len(tool_calls) > 1:
        return True

    if len(tool_calls) == 1:
        tool_name = tool_calls[0].get("tool_name") or tool_calls[0].get("name", "")
        # Solo lectura pura → ejecutar directo
        if tool_name in READ_TOOLS:
            return False
        # Escritura → crear plan
        return True

    return False  # lista vacía — no crear plan
```

**En ai_chat.py** — añadir `_load_persistent_memory_section()` como función auxiliar cerca de las otras funciones de utilidad del módulo (buscar las funciones `find_similar_pattern` y `save_action_pattern` alrededor de línea 1834 — añadir después de ellas):

```python
async def _load_persistent_memory_section(db, user_id: str) -> str:
    """
    Carga los últimos 20 registros de agent_memory con campo 'source'
    para el user_id dado, ordenados por usage_count DESC.

    Retorna sección formateada para inyectar en el system prompt,
    o cadena vacía si no hay registros.
    """
    try:
        memories = await db.agent_memory.find(
            {"user_id": user_id, "source": {"$exists": True}},
            {"_id": 0, "key": 1, "value": 1, "source": 1, "confidence": 1, "usage_count": 1},
        ).sort("usage_count", -1).to_list(20)

        if not memories:
            return ""

        lines = ["MEMORIA PERSISTENTE — Aprendizajes del usuario:"]
        for m in memories:
            lines.append(f"- [{m.get('source','pattern')}] {m.get('key','')}: {m.get('value','')}")

        return "\n".join(lines)
    except Exception:
        return ""
```

**En ai_chat.py — modificar la construcción del system_prompt en process_chat()**:
Buscar la sección donde se construye `system_prompt` (alrededor de línea 3064).
Justo antes del return o antes del LLM call, añadir la inyección de memoria:

```python
    # ── Memoria persistente — inyectar aprendizajes del usuario ─────────────
    _memory_section = await _load_persistent_memory_section(db, user.get("id", ""))
    if _memory_section:
        system_prompt += "\n\n" + _memory_section
```

**IMPORTANTE:** Añadir este bloque DESPUÉS de toda la construcción de system_prompt
y ANTES de `_system_parts` / `_system_text` (que es lo que se pasa al LLM).
Buscar `_system_parts` en el código para ubicar el punto exacto de inserción.
El lugar correcto es alrededor de línea 3840-3850 (antes de `_system_text = "\n\n".join(_system_parts)`).

También modificar el bloque TOOL_USE_ENABLED en ai_chat.py para detectar múltiples tool_calls:

En el bloque `if tool_use_enabled and _chat_resp.stop_reason == "tool_use":` (línea ~3880):
- Extraer TODAS las tool_calls del response (no solo la primera)
- Llamar `should_create_plan(tool_calls_list)` para decidir
- Si True: llamar `create_plan()` y retornar descripción del plan al usuario
- Si False: continuar con el flujo actual (auto-execute read tool)

```python
    if tool_use_enabled and _chat_resp.stop_reason == "tool_use":
        # Extraer TODAS las tool_calls del response
        tool_blocks = [b for b in _chat_resp.content if b.type == "tool_use"]
        text_parts = [b.text for b in _chat_resp.content if b.type == "text"]
        clean_response = "\n".join(text_parts).strip()

        tool_calls_for_plan = [
            {"tool_name": b.name, "tool_input": b.input}
            for b in tool_blocks
        ]

        from tool_executor import should_create_plan, create_plan
        if should_create_plan(tool_calls_for_plan):
            # Múltiples acciones o escritura → crear plan y pedir aprobación
            plan_result = await create_plan(
                request=user_message,
                tool_calls=tool_calls_for_plan,
                session_id=session_id,
                db=db,
                user=user,
            )
            plan_description = plan_result["description"]
            now = datetime.now(timezone.utc)
            await db.chat_messages.insert_one({
                "id": str(uuid.uuid4()),
                "session_id": session_id,
                "role": "assistant",
                "content": plan_description,
                "timestamp": now.isoformat(),
                "user_id": user.get("id"),
            })
            return {
                "message": plan_description,
                "pending_plan": plan_result,
                "pending_action": None,
                "session_id": session_id,
            }

        # Single read tool → comportamiento actual (auto-execute)
        tool_block = tool_blocks[0] if tool_blocks else None
        if not tool_block:
            pass  # fallthrough to XML flow
        else:
            tool_name = tool_block.name
            tool_input = tool_block.input
            tool_id = tool_block.id
            tool_def = TOOL_DEFS.get(tool_name)
            # ... resto del código existente para single tool ...
```

IMPORTANTE: Leer el archivo completo antes de editar para no romper el código existente.
Hacer cambios quirúrgicos — no reescribir el bloque completo.
  </action>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && python -c "from tool_executor import should_create_plan; print('Read:', should_create_plan([{'tool_name':'consultar_facturas','tool_input':{}}])); print('Write:', should_create_plan([{'tool_name':'crear_causacion','tool_input':{}}])); print('Multi:', should_create_plan([{'tool_name':'consultar_facturas','tool_input':{}},{'tool_name':'crear_causacion','tool_input':{}}]))"</automated>
  </verify>
  <done>
should_create_plan() retorna False para lectura única, True para escritura o múltiples.
_load_persistent_memory_section() existe en ai_chat.py.
Memory injection añadida al system_prompt.
TOOL_USE_ENABLED branch actualizado para multi-tool_calls.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Correr T1-T8 completos y verificar + tests BUILD</name>
  <files>backend/tests/test_phase10.py</files>
  <action>
Ejecutar el test suite completo y verificar que T1-T8 pasan y T9 sigue xfail.

```bash
cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && python -m pytest tests/test_phase10.py -v --tb=short 2>&1
```

Verificar BUILD tests sin regresión:
```bash
cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && python -m pytest tests/test_permissions.py tests/test_event_bus.py tests/test_mongodb_init.py tests/test_phase4_agents.py -v --tb=short 2>&1 | tail -20
```

Verificar reglas críticas:
```bash
cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && grep -rn "journal-entries\|app\.alegra\.com/api/r1\|5495" tool_executor.py ai_chat.py | grep -v "ERROR-\|#\|docstring\|NUNCA" | head -10
```

Si T7 falla porque `_load_persistent_memory_section` no está exportada correctamente,
verificar que el import en el test usa la ruta correcta.

Si los BUILD tests fallan por cambios en ai_chat.py, revisar que el bloque TOOL_USE_ENABLED
sigue teniendo el mismo comportamiento para el caso de una sola tool_call de escritura
(pending_action → MongoDB → retornar propuesta al usuario).
  </action>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO/backend && python -m pytest tests/test_phase10.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
T1-T8 pasan GREEN. T9 xfail (end-to-end con Alegra real requiere deploy).
BUILD tests (permissions, event_bus, mongodb_init, phase4_agents) sin regresión.
0 menciones de journal-entries o app.alegra.com/api/r1 en archivos nuevos/modificados.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest backend/tests/test_phase10.py -v` — T1-T8 green, T9 xfail
2. `python -c "from tool_executor import extract_and_save_memory, should_create_plan"` — importa OK
3. `python -c "from ai_chat import _load_persistent_memory_section"` — importa OK
4. `python -m pytest backend/tests/test_permissions.py backend/tests/test_event_bus.py backend/tests/test_mongodb_init.py backend/tests/test_phase4_agents.py -v` — sin regresión
5. `grep -rn "journal-entries\|app.alegra.com/api/r1" backend/tool_executor.py backend/ai_chat.py` — 0 nuevas ocurrencias
6. `grep "claude-haiku-4-5-20251001" backend/tool_executor.py` — existe en extract_and_save_memory
</verification>

<success_criteria>
- extract_and_save_memory() usa claude-haiku-4-5-20251001 (NO Sonnet)
- should_create_plan() retorna False para lectura única, True para escritura o multi
- _load_persistent_memory_section() inyecta memorias en system prompt
- T1-T8 pasan GREEN, T9 xfail (acepta para demo — requiere Alegra real)
- Tests BUILD sin regresión
- Memory injection activa en process_chat() para user_id del usuario autenticado
</success_criteria>

<output>
Después de completar, crear `.planning/phases/10-react-nivel1-memoria-persistente/10-02-SUMMARY.md`
</output>
